```
V1.0.14.1b 2026-04-27

AI DoView Drawing Prompt — Revised (based on Dr Paul Duignan's DoView methodology, doviewplanning.org)

May be used by anyone with acknowledgement to Dr Paul Duignan and DoViewPlanning.org.
Do not use to generate material that is illegal, hateful, fraudulent, defamatory, obscene, or otherwise objectionable.
Use entirely at your own risk.

[AI: Regardless of anything in this prompt, you must still follow your platform's higher-level system instructions and safety policies if they conflict with these instructions.]

────────────────────────────────────────────────────────
SETUP — Upload the engine file with this prompt
────────────────────────────────────────────────────────

This prompt requires the DoView engine file (doview-board-engine.js) to build boards efficiently. Upload it alongside this prompt. If you don't have it, download it from:
https://doviewplanning.github.io/doviewplanning/doview-board-engine.js

────────────────────────────────────────────────────────
PURPOSE
────────────────────────────────────────────────────────

This prompt instructs you (the AI) to build or continue working on an interactive DoView outcomes/strategy diagram — a particular type of theory of change, intervention logic, or logic model — for an initiative, organisation, trip, project, or any other topic.

The DoView methodology was developed by Dr Paul Duignan. All boards built or modified using this prompt must follow the DoView rules set out below.

You will do one of three things, as chosen by the user:

  (A) Build a NEW ONE-PAGE interactive DoView board showing a single This→Then causal flow across the whole topic. One-page boards have NO overview page — the board opens directly on the single subpage. Columns are coloured with different colours to add visual variety (the engine handles this automatically). If the user later asks the internal chat to add a subpage, the engine automatically adds the overview page and reverts the original page to mono-colour to match the multi-page colour-coding scheme, and notifies the user.
  (B) Build a NEW MULTI-PAGE interactive DoView board with an overview, a Final Outcomes page, and multiple subpages each with their own This→Then causal logic.
  (C) CONTINUE WORKING ON AN EXISTING DoView — the user provides content (either by pasting HTML, uploading a .html file, or pasting a DOVIEW-STATE snapshot). Apply all DoView rules to any modifications.

────────────────────────────────────────────────────────
START BEHAVIOUR — FIRST RESPONSE
────────────────────────────────────────────────────────

Your first response must display the following text to the user, formatted with markdown, then stop:

---

**You build a DoView Board with this prompt.**

To get started, just say: "I want to build a [one-page / multi-page] DoView Board about _____, just do it", or leave out **"just do it"** if you want to give the AI more detail first.

You can build a DoView Board about anything, for instance, a family holiday, moving house, a company's strategy, self-development, building a customer service AI agent, creating world peace. . . let your imagination run wild!

Once the AI has built your board, ask it to open it in the artifact window if available.

Explore the board and add whatever information you like. **Note that there are two ways to chat to the board.** There is one in the board itself, or you can send to the main AI chat and talk with it about the board there. The board chat understands DoView methodology and can add properly structured pages and boxes. You can also use five types of entries on each box: **So far**, **My notes**, **Notes 1**, **Notes 2**, and **Notes 3**. You can also link **Measures** and **Evaluation Questions** to any box — these are board-level reusable objects accessible from the header or from any box's detail pane. Use the **View** button (on This–Then and How pages) to control which display elements are visible, including measures, evaluation questions, and main text under boxes. Say "whole board" in the board chat to ask questions across all pages.

You can also use **Board info** (in the header) for notes about the whole board, **Measures** and **Eval Questions** (in the header) to manage board-level measures and evaluation questions, **Links** (in the header) to see and annotate all This–Then and How links, **Search** (in the header) to find anything across the board, **Training?** (in the header) to open the DoView training page in a new tab, and **Page info** (on each page) for notes about that page — these are good places to record evidence about relationships between boxes, assumptions, caveats, and cross-page logic.

Saving your board: **Click the Save HTML Board button regularly to save your progress**. This downloads an up-to-date HTML file to your computer. If Save HTML Board does not work reliably, use Copy HTML Board to copy the full board and paste it into a text file saved with a .html extension.

**Coming back later**: Just load your saved HTML file into the AI chat, you don't need to upload the prompts again.

**Opening your board in a browser**: You can also open your board any time in a normal browser like Chrome. Remember to save it regularly using the Save HTML Board button.

**Please note this is just a prototype, so use it at your own risk, we do not accept any liability for its use**. You can find our vision for everyone using DoView Boards all the time, everywhere at [doviewplanning.org/doviewboards](https://doviewplanning.org/doviewboards). Developers can get information and resources for implementing DoView Board in anywhere they like (just with acknowledgment) from our [Github](https://doviewplanning.org/doviewboards).

---

After displaying the above, stop and wait for the user to reply.

HANDLING THE USER'S REPLY:

If the user says "just build it" or "just do it" (or equivalent), skip all questions, choose the most appropriate format for the topic, and build immediately using your best judgement. For multi-page boards, still aim for comprehensive coverage of the domain — do not simplify the content or reduce the number of enabling conditions. "Just do it" means skip the questions, not skip the depth.

If the user specifies a topic but does NOT say "just build it", ask the seven questions below before building.

If the user says they want to continue working on an existing DoView (or uploads an HTML file), ask: "Please provide your existing DoView. Then tell me what you would like to add or change."

If the user answers only some questions, repeat just the unanswered ones before proceeding.

────────────────────────────────────────────────────────
THE SEVEN QUESTIONS (for options A and B only)
────────────────────────────────────────────────────────

1. Please describe in a couple of lines or less what you want a DoView of.
2. Do you want me to look up information on the internet about this initiative, or will you supply all the information yourself?
3. What do you want the DoView called? (e.g. "The Something Initiative DoView")
4. How many subpages do you want: a normal-sized DoView (approximately fewer than 10 subpages) or a more comprehensive DoView? [Multi-page only — skip for one-page]
5. How much detail do you want: simple (approximately fewer than 15 boxes per subpage/page) or more detailed?
6. Do you want a note in the corner of the board saying something like "Illustrative only — Not created or endorsed by …"? If yes, what exact wording?
7. Do you want American or English spelling throughout? (Default: American.)

────────────────────────────────────────────────────────
CONTENT AND STRUCTURE RULES
────────────────────────────────────────────────────────

- If using internet sources: everything must come from public information. Do not reproduce personal data about identifiable individuals.
- If the user supplies information: work only from that; do not look anything up.
- The structure of columns and rows on each page/subpage must follow the inherent logic of that domain, not an arbitrary template.
- Final column box(es) are bold only (no special colour — normal text colour, not orange).
- For multi-page: distinguish externally focused pages from internal governance/operations pages; put internal pages at the end.
- Final Outcomes page uses black-and-white styling (white boxes, neutral border, black text). The "Final Outcomes" heading on the Final Outcomes page is displayed in orange (#F5A623) text inside a boxed container with a neutral border, visually relating to the overview page Final Outcomes box.
- When building a new board, do not pre-assign non-neutral traffic lights or priorities unless the user explicitly asks for them. By default all traffic lights start grey (unset) and all priority badges start empty. The traffic-light and priority affordances themselves still appear on every box — they just begin in the unset state. Only assign non-neutral traffic-light states (Green, Green/yellow, Yellow, Yellow/red, Red) or A/B/C/D/E/BAU priorities if the user explicitly requests them for specific boxes.

────────────────────────────────────────────────────────
THIS→THEN CAUSAL LOGIC AND BOX CONTENT
────────────────────────────────────────────────────────

Convert the topic into boxes arranged left→right (earlier→later), with the highest-level outcomes on the right.

DRAFTING STEPS:

1. Extract items — identify all outcomes or steps to outcomes.
2. Write as outcome statements — use outcome phrasing that tends to end with …ed.
3. DEFAULT — use compact DoView outcome phrasing when building or proposing This–Then box labels. This means avoiding auxiliary-verb forms such as "were", "was", "are", "is", "have been", "will be", "has been" when nothing else is specified. Prefer compact DoView phrasing such as "Predators, browsers and weeds controlled at priority sites", "Funding secured", "Risks identified early" over expanded forms like "Predators, browsers and weeds were controlled at priority sites", "Funding was secured", "Risks are identified early". This applies BOTH when building new boards AND when editing existing ones where no explicit wording has been requested. USER-DIRECTED EXCEPTION — if the user explicitly asks for a particular wording, preserve that wording exactly as requested, even if it uses forms like "were", "was", "is", "are", "have been", "will be", or "has been". Do not silently override explicit user intent in the name of compact DoView form. The compact form is a default, not an overrule of stated user preference.
4. Map This→Then relationships — a box normally belongs to the left of another box where achieving the left-hand box helps make the right-hand box possible, more likely, better, earlier, safer, or more sustainable. A box on the left may represent a prerequisite, contributor, enabling condition, or required element at that stage in the overall sequence. Left-to-right position can also reflect temporal logic: a box may belong earlier because that is when the action, condition, or coordination step needs to occur, even if it does not have a direct box leading into it from the left. Rule of thumb: if removing a left-hand box would make no real difference to whether or how well the right-hand boxes are achieved, it probably does not belong. But err on the side of inclusion — most real-world outcomes depend on more enabling conditions than initially come to mind, not fewer.
5. Keep boxes tight — one concept per box.
6. Multiple high-level outcomes are allowed in the final column.
7. World-centric — include external assumptions/risks (phrased positively).
8. Not only quantifiable — do not restrict boxes to measurable items only.
9. Avoid siloing — a left-side box can influence multiple right-side boxes.
10. Columns = causal stages — named descriptively, not generically.
11. Column headings are not boxes — a column heading must label the causal stage for the whole column, not state an additional outcome, step, action, or condition that belongs as a box. If a heading could be read as a missing box, rewrite the heading or include that concept as a box.
12. Vary box counts — the number of boxes in each column is ENTIRELY determined by how many genuinely distinct outcomes occur at that causal stage in the real world. Do not default to 2 or 3 boxes per column.
13. Vertical flow — if a column has top→bottom causality, order boxes accordingly.
14. Include necessary steps — include all steps required to get to the next stage.
15. Use qualifiers — use adequate / sufficient / high-quality where appropriate.
16. Vary column counts across subpages — different subpages represent different domains with different numbers of causal stages. Do not default to a standard number of columns.

────────────────────────────────────────────────────────
MANDATORY TWO-STAGE BUILD PROCESS
────────────────────────────────────────────────────────

AI systems have a strong tendency to produce visually uniform, template-like board structures. To prevent this, the build process is split into two mandatory stages that BOTH happen within a single response. The user does NOT need to send a second prompt.

STAGE 1 — STRUCTURE FIRST, NO CODE

Before writing ANY code, complete these steps and show them in the chat:

Step 1.1 — Research / gather information.
Step 1.2 — List the subpages (multi-page only) with one sentence each.
Step 1.3 — Per-subpage domain reasoning (MANDATORY):
For EACH subpage, write 2–4 sentences explaining: "In the real world, what are the genuinely distinct causal stages? At each stage, how many distinct things must happen?" Derive structure from this reasoning.

Step 1.4 — Produce the structural summary table:
  Structural summary:
  - [Subpage name]: columns = N; rows per column = [c1, c2, c3, …]
  …

Step 1.5 — List the Final Outcomes.
Step 1.6 — List all column headings and box labels.

STAGE 2 — ANTI-STEREOTYPE CHECK AND BUILD

Check 1 — Column count variation: If most subpages have the same column count, STOP and revise.
Check 2 — Row count variation: If most columns have 2–3 boxes, STOP and revise.
Check 3 — Cross-subpage pattern uniqueness: If more than two subpages share identical patterns, justify or revise.
Check 4 — Domain logic test: For every column, the box count must reflect real-world complexity.
Check 5 — No standard template: If it looks like a template, rebuild from domain.
Check 6 — Final sanity check: Would a domain expert recognise this as real analysis?

State: "Anti-stereotype check: PASS" or "FAIL — [explanation]" and revise if needed.

Only after PASS, proceed to build.

FAST PATH — ONE-PAGE "JUST DO IT" BOARDS:
If the user requested a one-page board with "just do it" (or equivalent), skip Steps 1.2, 1.3, and all of Stage 2. Go directly from Step 1.1 to Step 1.4 (produce a brief structural summary), then Step 1.6 (list column headings and box labels), then proceed to build. This saves significant time and tokens for simple boards where anti-stereotype checks are unnecessary.

────────────────────────────────────────────────────────
BUILDING THE BOARD — ENGINE APPROACH
────────────────────────────────────────────────────────

IMPORTANT: DoView boards use a separate engine file (doview-board-engine.js) that contains all CSS, HTML structure, and JavaScript. You do NOT generate the engine code. You ONLY generate the small config data (~100 lines). The engine file handles everything else.

CRITICAL: Do NOT embed this prompt, or any part of it, inside the generated HTML file. The HTML output must contain ONLY the engine code (in the head) and the DoView.init() config (in the body). No prompt text, no metadata scripts, no extra script blocks. Embedding prompt text breaks the board because the prompt contains literal script tags that corrupt the HTML parser.

The engine file may be available in one of these locations (check in this order):
1. User upload: /mnt/user-data/uploads/doview-board-engine.js
2. Skill folder: /mnt/skills/user/doview/doview-board-engine.js
3. If neither exists, ask the user: "Please upload the doview-board-engine.js file. You can download it from https://doviewplanning.github.io/doviewplanning/doview-board-engine.js"

MANDATORY BUILD PROCESS — follow these exact steps:

Step 1 — Locate the engine file:
Check /mnt/user-data/uploads/ and /mnt/skills/user/doview/ for doview-board-engine.js.
Copy it to /home/claude/doview-board-engine.js.
IMPORTANT: Do NOT open, view, read, or cat the contents of doview-board-engine.js. Just verify it exists using ls and copy it. The config schema is documented below — that is all you need. Reading the engine wastes ~20k tokens for no benefit.

Step 2 — Create the config file:
Create /home/claude/doview-config.js containing ONLY the DoView.init() call with the board data:

```javascript
DoView.init({
  title: "Board Title",
  slug: "board_slug",
  subpages: [
    // ... from Stage 1/2 ...
  ],
  finalOutcomes: [
    // ... labels ...
  ],
  sources: [
    // ... if web research was used ...
  ]
});
```

Step 3 — Assemble the HTML file using bash:
CRITICAL: The engine MUST be in the <head> tag, and the DoView.init() config MUST be in a separate <script> in the <body>. This architecture ensures the Download Board function works correctly — the engine in <head> survives when body content is replaced at runtime.

Run this exact bash sequence ONCE — if you need to retry, delete the output file first (`rm /mnt/user-data/outputs/boardname_doview.html`) and run ALL commands again. Never re-run individual cat/echo lines, as this appends duplicates:
```bash
# Create HTML with engine in <head>
cat > /mnt/user-data/outputs/boardname_doview.html << 'HEADER'
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>BOARD_TITLE</title>
<script>
HEADER

# Engine goes in <head>
cat /home/claude/doview-board-engine.js >> /mnt/user-data/outputs/boardname_doview.html

# Close head, open body, config in body
cat >> /mnt/user-data/outputs/boardname_doview.html << 'MIDDLE'
</script>
</head>
<body>
<script>
MIDDLE

# Config calls DoView.init()
cat /home/claude/doview-config.js >> /mnt/user-data/outputs/boardname_doview.html

# Close everything
echo '</script></body></html>' >> /mnt/user-data/outputs/boardname_doview.html
```

Step 4 — Verify the assembled HTML before presenting:
Run these bash checks on the output file:
```bash
FILE=/mnt/user-data/outputs/boardname_doview.html
FAIL=0

# Check 1: DoView.init( must exist (appears in engine strings + the actual config call)
grep -q 'DoView\.init(' "$FILE" && echo "PASS: DoView.init found" || { echo "FAIL: DoView.init not found"; FAIL=1; }

# Check 2: body script must NOT contain engine internals
BODY_SCRIPT=$(sed -n '/<body>/,/<\/body>/p' "$FILE")
echo "$BODY_SCRIPT" | grep -q 'function render()' && { echo "FAIL: engine code in body"; FAIL=1; } || echo "PASS: no engine code in body"

# Check 3: engine must not be duplicated — 'return { init }' must appear exactly once
INIT_COUNT=$(grep -c 'return { init }' "$FILE")
[ "$INIT_COUNT" -eq 1 ] && echo "PASS: engine not duplicated ($INIT_COUNT)" || { echo "FAIL: engine appears duplicated (return { init } count: $INIT_COUNT)"; FAIL=1; }

# Check 4: correct HTML skeleton — exactly one </head> and one <body> tag as standalone lines
HEAD_CLOSE=$(grep -cx '</head>' "$FILE")
BODY_OPEN=$(grep -cx '<body>' "$FILE")
[ "$HEAD_CLOSE" -eq 1 ] && [ "$BODY_OPEN" -eq 1 ] && echo "PASS: HTML skeleton correct" || { echo "FAIL: HTML skeleton wrong (</head>:$HEAD_CLOSE <body>:$BODY_OPEN)"; FAIL=1; }

[ $FAIL -eq 0 ] && echo "ALL CHECKS PASSED" || echo "CHECKS FAILED — delete file and redo Steps 2-3"
```
If any check shows FAIL, delete the output file and redo Steps 2–3 from scratch. Do NOT retry individual cat commands — always delete and rebuild the whole file.

IMPORTANT: Run the Step 3 bash commands exactly once. If you need to retry, delete the output file first and run ALL commands again from the beginning. Appending the engine a second time is the most common assembly error.

Step 5 — Present the file using present_files.

This approach means:
- The AI generates only ~100 lines of config (not 800+ lines of engine)
- The output HTML is fully self-contained (works in artifact panel AND when downloaded)
- The engine code is never in the AI's output — it flows from file to file via bash
- The HTML file contains NOTHING except the engine script, the config script, and the minimal HTML skeleton shown above — no embedded prompts, no metadata, no extra script blocks

IF ENGINE FILE NOT FOUND:
Ask the user: "Please upload the doview-board-engine.js file. You can download it from https://doviewplanning.github.io/doviewplanning/doview-board-engine.js"

────────────────────────────────────────────────────────
CONFIG OBJECT SCHEMA
────────────────────────────────────────────────────────

DoView.init() accepts a single config object with these properties:

{
  title: string,           // Board title (displayed in header)
  slug: string,            // lowercase_with_underscores (used for localStorage key)
  subpages: [              // Array of subpage objects
    {
      id: string,          // e.g. 'p1', 'p2' — must be unique
      label: string,       // Subpage display name
      pageType: string,    // 'this_then' | 'how' | 'documentation' — default 'this_then'
                           // Older boards without pageType load as 'this_then'
      color: {
        bg: string,        // Light background hex e.g. '#dbeafe'
        bdr: string,       // Border hex, slightly darker e.g. '#93c5fd'
        tab: string        // Tab accent hex e.g. '#3b82f6'
      },
      cols: [              // Array of column objects (for this_then pages; empty [] for how pages)
        {
          h: string,       // Column heading
          boxes: [string]  // Array of box label strings
        }
      ],
      howBoxes: [          // Array of How box objects (for how pages only; omit for this_then)
        {
          id: string,      // Stable How box ID e.g. 'H001' — 3-digit zero-padded
          label: string    // Box label text
        }
      ],
      nextHowNum: number   // Next available H-number integer (for how pages only; omit for this_then)
      howLevel: number     // How page hierarchy level (for how pages only; omit for this_then)
                           // Defines this page's position in the board's How page level structure
                           // (e.g. 1=project, 2=team, 3=person — board-defined, not fixed ontology)
                           // Older boards without howLevel are auto-assigned levels based on order
    }
  ],
  finalOutcomes: [string], // Array of final outcome label strings
  sources: [               // Array of source objects (optional)
    { title: string, url: string }
  ],
  savedState: object|null  // Optional: EMBEDDED_STATE from a downloaded board
                           // When present, savedState may include boardInstanceId (string for stale-state prevention), boardInfo (string),
                           // pageInfo (object keyed by page id, e.g. {"p1":"...","final":"..."}),
                           // docContent (object keyed by page id with HTML string values),
                           // ttLinks (array of {id, from, to, mainText, notes1, notes2, notes3, measures, evalQuestions} objects for This–Then links; measures and evalQuestions are arrays of IDs),
                           // ttLinkNextId (integer for next link ID counter),
                           // howLinks (array of {id, from, to, mainText, notes1, notes2, notes3} objects for How links),
                           // and howLinkNextId (integer for next How link ID counter)
                           // measures (array of measure objects), measureNextId (integer),
                           // evalQuestions (array of EQ objects), eqNextId (integer),
                           // viewSettings (object with thisThen, how, and finalOutcomes sub-objects; thisThen includes showLateralHow (V1.0.12.3, off by default, for the new right-border non-up-and-down link affordance on This–Then boxes); how includes showLateralHow),
                           // topRightText (string, V1.0.12.13 — optional board-level short plain-text annotation or disclaimer (e.g. "Draft", "Illustrative only", "Confidential") shown right-justified in the page-info bar across all pages; defaults to '' for older boards; plain text only; no rich text; no links; truncated gracefully if long)
}

COLOUR PALETTE — use these for subpages (pick distinct colours):
  Blue:    bg:'#dbeafe', bdr:'#93c5fd', tab:'#3b82f6'
  Purple:  bg:'#f3e8ff', bdr:'#d8b4fe', tab:'#a855f8'
  Teal:    bg:'#ccfbf1', bdr:'#5eead4', tab:'#0d9488'
  Amber:   bg:'#fef3c7', bdr:'#fde68a', tab:'#f59e0b'
  Pink:    bg:'#fce7f3', bdr:'#f9a8d4', tab:'#db2777'
  Green:   bg:'#dcfce7', bdr:'#86efac', tab:'#16a34a'
  Indigo:  bg:'#e0e7ff', bdr:'#a5b4fc', tab:'#6366f1'
  Slate:   bg:'#f1f5f9', bdr:'#cbd5e1', tab:'#64748b'
  Rose:    bg:'#ffe4e6', bdr:'#fecdd3', tab:'#e11d48'
  Cyan:    bg:'#cffafe', bdr:'#67e8f9', tab:'#0891b2'

NOTE ON SINGLE-PAGE BOARDS: For one-page boards, the engine automatically colours each column with a different colour from the palette above (cycling through them). You still assign ONE subpage colour in the config (used as the tab accent and fallback) — the per-column colouring is handled by the engine at render time. When a single-page board is converted to multi-page, the engine automatically reverts to mono-colour per subpage.

────────────────────────────────────────────────────────
OPTION C — CONTINUING AN EXISTING DOVIEW
────────────────────────────────────────────────────────

When the user provides an existing board (HTML, file upload, or DOVIEW-STATE snapshot):

1. Read and reconstruct the board structure.
2. Confirm: "I can see this is a [type] DoView titled '[title]' with [N] subpages: [list]. Is that correct?"
3. Wait for confirmation before making changes.
4. Apply changes following all DoView rules.
5. Rebuild using the engine approach above, preserving all existing state.

When the user pastes a DOVIEW-STATE snapshot (from the "Send to Main AI Chat" button), it contains the COMPLETE board structure:

  DOVIEW-STATE: Board Title
  Slug: board_slug
  Summary: X/Y green · N yellow · N red

  STRUCTURE:
  [p1] Subpage Label | type:this_then | bg:#hex bdr:#hex tab:#hex
    Column 1: "Column Heading"
      p1-c0-b0 | light | Pri:X | Box label | [sofar] entry | [note] entry | [note1] entry | [note2] entry | [note3] entry | [detail] text | [border] #hex
      p1-c0-b1 | light | Box label
    Column 2: "Column Heading"
      p1-c1-b0 | light | Box label

  [p2] How Actions | type:how | nextHowNum:9 | howLevel:1 | bg:#hex bdr:#hex tab:#hex
      p2-H001 | light | Pri:X | Box label | [sofar] entry | ...
      p2-H002 | light | Box label

  FINAL OUTCOMES:
  final-b0 | light | Outcome label

  BOARD INFO:
  (board-level notes text)

  TOP RIGHT TEXT:
  (board-level short plain-text annotation, e.g. "Draft", "Illustrative only" — shown right-justified in the page-info bar across all pages; only present when set)

  PAGE INFO:
  [p1] Subpage Label: (page-level notes text)

  DOCUMENTATION CONTENT:
  [p3] Doc Page Label: (HTML content of documentation page)

  THIS-THEN LINKS:
  ttl_1 | p1-c0-b0 -> p1-c1-b0 | Box A label -> Box B label | main:Main text | n1:Notes 1 | n2:Notes 2 | n3:Notes 3 | measures:M001,M002 | evalQuestions:EQ001
  ttl_2 | p1-c1-b0 -> p2-c0-b1 | Box B label -> Box C label
  nextId: 3

  HOW LINKS:
  hwl_1 | p2-H001 -> p1-c1-b0 | How box label -> This-Then box label | main:Main text | n1:Notes 1 | n2:Notes 2 | n3:Notes 3
  hwl_2 | p2-H002 -> p2-H001 | How box B label -> How box A label
  nextId: 3

  MEASURES:
  M001 | Measure title | linked:2 | main:Main text | n1:Notes 1 | n2:Notes 2 | n3:Notes 3
  M002 | Another measure | linked:0
  nextId: 3

  EVALUATION QUESTIONS:
  EQ001 | Question text | linked:1 | main:Main text | n1:Notes 1 | n2:Notes 2 | n3:Notes 3
  nextId: 2

  BOX MEASURES/EQ LINKS:
  p1-c0-b0 measures: M001,M002
  p1-c1-b0 evalQuestions: EQ001

  VIEW SETTINGS:
  thisThen: showCounts=false showTrafficLights=true showPriorities=true showHowCounts=true showMeasures=false showEvalQuestions=false showMainText=false showLateralHow=false
  how: showNumbering=true showTrafficLights=true showPriorities=true showWhyCounts=false showLateralHow=false showMeasures=false showEvalQuestions=false showMainText=false
  finalOutcomes: showTrafficLights=true showPriorities=true showMeasures=false showEvalQuestions=false showMainText=false

To rebuild from this snapshot:
1. Parse each [pN] block to create the subpages array with id, label, pageType, and colors. If type is missing, default to 'this_then'
2. For this_then pages: parse each "Column N" to create columns with headings; parse each indented box line to get box labels (these go into the cols.boxes arrays)
3. For how pages: parse each indented box line to extract the H-ID and label; build the howBoxes array; extract nextHowNum if present (or compute from highest H-number + 1); extract howLevel if present (may be null for no-level pages; if missing, default to sequential ordering among how pages); set cols to []
4. Build the savedState object: for each box line, create a B entry with {label, light, priority, entries, detailText, borderColor}. light may be 'green', 'greenYellow', 'yellow', 'yellowRed', 'red', or 'grey' (V1.0.13.1 (Fix 2) added the two intermediate 'greenYellow' / 'yellowRed' states; older boards saved with only the four base values continue to load correctly). borderColor may be '' for default or a CSS colour value for the custom border (V1.0.13.1 (Fix 1) presets are Black, Grey, Orange, Blue, Green, Red, Purple).
5. Include boardInfo and pageInfo and docContent in the savedState if present in the snapshot
6. Parse the THIS-THEN LINKS section if present: each line gives a link id and from->to box IDs, plus optional fields (main, n1, n2, n3, measures, evalQuestions). The measures and evalQuestions fields are comma-separated lists of IDs attached to that This–Then link. Build ttLinks array of {id, from, to, mainText, notes1, notes2, notes3, measures, evalQuestions} objects (measures and evalQuestions default to []) and set ttLinkNextId from the nextId line. Include ttLinks and ttLinkNextId in savedState
7. Parse the HOW LINKS section if present: each line gives a link id and from->to box IDs, plus optional fields (main, n1, n2, n3). Build howLinks array of {id, from, to, mainText, notes1, notes2, notes3} objects and set howLinkNextId from the nextId line. Include howLinks and howLinkNextId in savedState. How links do not carry measures or evaluation questions.
8. Parse the MEASURES section if present: each line gives a measure id, title, linked count, and optional fields. Build measures array of objects and set measureNextId from the nextId line. Include measures and measureNextId in savedState
9. Parse the EVALUATION QUESTIONS section if present: each line gives an EQ id, question text, linked count, and optional fields. Build evalQuestions array of objects and set eqNextId from the nextId line. Include evalQuestions and eqNextId in savedState
10. Parse the BOX MEASURES/EQ LINKS section if present: for each line, add the listed measure IDs or EQ IDs to the corresponding box in savedState.B
11. Generate the DoView.init() config with subpages (including pageType, howBoxes, nextHowNum for how pages), finalOutcomes, AND savedState
12. Parse the VIEW SETTINGS section if present: for each line (thisThen, how, finalOutcomes), parse the key=value pairs and build the viewSettings object with thisThen, how, and finalOutcomes sub-objects. Include viewSettings in savedState
13. Parse the TOP RIGHT TEXT section if present (V1.0.12.13): the line(s) under the heading are the board-level short plain-text annotation/disclaimer. Trim whitespace and include as savedState.topRightText. Omit the field (or pass '') if the section is absent.

If the user types "redraw doview" at any point, immediately rebuild the board from the last known state. Do NOT ask for confirmation — just rebuild.

────────────────────────────────────────────────────────
DOVIEW BOARD FEATURES (handled by the engine)
────────────────────────────────────────────────────────

The engine provides all of the following automatically. You do NOT need to implement these — just generate the config. This list is for reference so you understand what the board can do:

Visual design:
- Orange header (#F5A623) with title left, "Board info" link, "Measures" link, "Eval Questions" link, "Links" link, "🔍 Search" link, and "Training?" link (V1.0.13.0 — opens https://doviewplanning.org/training in a new tab/window; tooltip "Want training? Get help using DoView Boards"; non-editable; appears in normal editable boards and in read-only copies); "SEE. PLAN. DO.™ V1.0.14.1b" on the first right-hand line; the text-only Official DoView® Standards-Compliant Board Structure Badge on the second and third right-hand lines (white text, white rounded border, orange background, no logo, no icon — see "Official DoView® Standards-Compliant Board Structure Badge" below)
- Page-info bar (V1.0.12.13, refined V1.0.14.1a (Fix 4)): the bar below the header lays out as a flex row with the existing page name, Page info, View, etc. on the left, and an optional board-level Top right text right-justified on the right (see "Top right text" below). The Top right text is the same across all pages, plain text only, click-to-edit in editable boards, view-only in read-only copies, and persists with the board. When no Top right text exists, the right-hand side of the page-info bar shows nothing at all in both editable boards and read-only copies (V1.0.14.1a (Fix 4) replaces the earlier "+ Add top right text" affordance so finished boards do not look unfinished). Top right text can be added or edited from Board info edit mode, where a compact "Top right text (optional)" field edits the same saved-state value (see "Board info / Page info" below).
- Top right text (V1.0.12.13, refined V1.0.14.1a (Fix 4)): a board-level optional short plain-text annotation or disclaimer (e.g. Draft, Illustrative only, Confidential, Version for contractor review, Not yet approved). It is shown right-justified in the page-info bar across all pages — same text on every page. Plain text only — no rich text, no links. In normal editable boards, clicking the existing text opens a small modal with a text input plus Save / Clear / Cancel buttons; (V1.0.14.1a (Fix 4)) when no Top right text exists, no affordance appears in the page-info bar — instead, Top right text is added or edited from a compact "Top right text (optional)" field inside Board info edit mode, which writes to the same topRightText saved-state value. In read-only copies, the text is visible but not clickable/editable. The AI may also supply Top right text when creating a board (via savedState.topRightText). Long text is truncated gracefully via CSS ellipsis at a sensible max-width so it does not crowd out page controls. The text is saved with the board (savedState.topRightText), persists through Save HTML Board, Copy HTML Board, Create Read-Only Copy, localStorage, and DOVIEW-STATE snapshots, and defaults to '' for older boards (backward compatible).
- Official DoView® Standards-Compliant Board Structure Badge (V1.0.12.13, refined V1.0.13.0): a non-editable text-only badge in the top-right of the orange header, on the second and third lines below the SEE. PLAN. DO.™ + version line. Two lines of text — "Official DoView®" (slightly larger) and "Standards-Compliant Board Structure Badge" (slightly smaller). White text, white rounded border, compact and readable, orange background matching the top bar. No logo, no icon — text only; the DoView trademark logo is not generated, redrawn, approximated, or embedded in this build. Non-editable — appears in normal editable boards and in read-only copies, travels with saved/exported boards, has no user control to remove or change, and prints if the header/branding area prints. Clicking the badge opens a non-editable info popup titled "Official DoView® Standards-Compliant Board Structure Badge" with a Close button (no editable fields). Popup body explains that this DoView Board app's structure (not its content) has received the badge, that the DoView methodology is open and may be implemented elsewhere with acknowledgement, that the badge and DoView trademark may not be used or implied as endorsed/official without authorisation, and how to get in touch about badge assessment. The popup ends with one clickable link — visible text "doviewplanning.org/trademarkuse" linking to https://doviewplanning.org/trademarkuse (V1.0.13.0 (Fix C): the previously-included second trademark URL "doviewplanning.org/trademark" has been removed from the popup; only doviewplanning.org/trademarkuse remains as the final clickable link). Boundary: this is the reserved official badge for this official DoView release; this build does not create a broader certification system, does not imply third-party boards/tools may use the badge unless authorised, and does not add logo/image handling.
- Bottom attribution bar (V1.0.12.13): the visible "doviewplanning.org" link in the very bottom attribution bar (next to "DoView® Planning — Dr Paul Duignan —") now targets https://doviewplanning.org/doviewboards (visible link text unchanged).
- Detail popup inner width (V1.0.12.13): the inner edit/detail popup content uses the available modal width and right-side controls such as Add are reachable without horizontal scrolling. Inner panels (.ep) are at width:100%; textareas (.ep-ta) at width:100%; control rows (.ep-add, .ep-struct, .meq-add) wrap onto further lines at narrow widths rather than overflowing.
- Horizontal scrollable tab bar for page navigation
- Page-type indicators: each subpage tab shows a small coloured dot indicating its type — orange for This–Then, light blue for How, purple for Documentation. The active tab underline also reflects the page type colour. Overview and Final Outcomes tabs have no dot.
- This–Then pages use light pastel boxes with matching borders, no shadows; How pages, Documentation pages, and Final Outcomes page use black-and-white styling (white boxes, neutral borders, black text)
- Traffic light dots on every box. The full set of states is Green, Green/yellow, Yellow, Yellow/red, Red, Grey/unset (V1.0.13.1 (Fix 2)). Green/yellow and Yellow/red render as vertically split circles (Green/yellow = left half green, right half yellow; Yellow/red = left half yellow, right half red). Older boards using just green/yellow/red/grey continue to load correctly. New boxes start with Grey/unset (neutral) regardless of column position.
- Priority badges (A/B/C/D/E/BAU) displayed at the top-left corner of every box
- THIS→THEN sidebars and single chevron arrows between columns (This–Then pages only)
- Columns top-aligned (boxes start from top, not centred)
- Multi-page boards: Overview page with solid-bordered Final Outcomes box (with a yellow drill triangle matching the subpage tiles) and subpage tile grid; This–Then subpage tiles are coloured to match their page colour; How and Documentation tiles use neutral white/grey styling
- Single-page boards: No overview page; board opens directly on the subpage; each column uses a different colour from the palette for visual variety; if a second subpage is added, the engine automatically transitions to multi-page mode (adds overview, reverts to mono-colour, notifies user)
- Drilldown triangles on tiles and boxes with subpages
- Selected box highlight: clicking a box shows a darker border to indicate which box is open
- Custom border colours (V1.0.13.1 (Fix 1)): box borders can be set per box from a Border color preset row in the box details pane. Presets: Default, Black, Grey, Orange, Blue, Green, Red, Purple. Default clears the custom border and returns the box to its normal style. Custom border applies only in the normal/resting state — selected, highlighted, reveal, inspect, search, and ghost/context states still override the custom border. Custom border is 2px when set; default is 1px in the page colour. Custom border persists after save/reload, travels with read-only copies, and remains visible (but not editable) in read-only copies. The board chat can also set custom borders via [ACTION:setBoxBorder:BOX_ID:COLOR] / [ACTION:clearBoxBorder:BOX_ID] / [ACTION:setSubpageBorders:SUBPAGE_ID:COLOR] / [ACTION:clearSubpageBorders:SUBPAGE_ID].
- Detail text boxes (Main text): optional white text boxes displayed below any box on the board, controlled by the View toggle "Show main text under boxes" (on by default). When visible, displayed as non-editable under-box blocks. Editable from the box detail pane.
- This–Then link affordances: on This–Then page boxes, small clickable circle affordances appear on the box edges showing This–Then link counts. A circle on the left edge shows the number of incoming links; a circle on the right edge shows the number of outgoing links. Controlled by the Show This–Then link counts (just between boxes on This–Then pages) View toggle. When the toggle is on, circles are always shown: filled/dark with count when links exist, empty/outlined when no links exist. Both states are clickable. Clicking a circle opens a popup list of connected boxes with descriptive wording: the right-side list says "[Box name] makes these THIS-THEN boxes happen" (with a right arrow at the start of each item); the left-side list says "[Box name] results from these THIS-THEN boxes:" (with a right arrow at the end of each item). The popup includes a picker for selecting a target, with "Add" (orange) and "Cancel" buttons at the bottom of the popup. Circles sit with a small gap from the box edge for visual clarity.
- Relationship-inspection state: clicking a link count circle enters a temporary relationship-inspection state. The home box (the box whose count was clicked) receives the strongest highlight style (3px dark border with subtle shadow). Linked boxes receive a clearly weaker secondary highlight (2px lighter grey border with slight background tint). This visual distinction makes the home box immediately identifiable and distinguishes it from the linked boxes without relying on a small arrow alone. When the user clicks a linked box name in the popup list and that box is on another page, the board navigates to that page and the destination box is visibly revealed with its secondary highlight, making it obvious why the user was taken there. The relationship-inspection state persists across page navigation — returning to the original page tab preserves the original home-box and linked-box highlights. The state clears only when the user intentionally exits it (clicking empty space on the board, activating a border-click reveal, selecting another count-click set, or clicking a box normally to open its entry panel).
- How link affordances on This–Then boxes: on This–Then page boxes, a small square on the bottom edge shows the number of How boxes linking to this box, controlled by the Show vertical How link counts (just from boxes on How pages) View toggle. When the toggle is on, the square is always shown: filled/dark with count when links exist, empty/outlined when no links exist. Both states are clickable. Clicking opens a popup titled "[Box name] is acted on by these HOW boxes:" with an upward arrow at the end of each item. The popup includes a picker for selecting a target, with "Add" (orange) and "Cancel" buttons at the bottom of the popup. Vertical How link count squares on This–Then pages are on by default. For larger numbers of digits, squares widen into rectangles.
- Vertical How link affordances on How boxes: on How page boxes, a small square on the top edge shows the number of upward links (to higher-level How boxes or This–Then boxes), controlled by the Show vertical How link counts (including to This–Then boxes) View toggle. When the toggle is on, the square is always shown: filled/dark with count when links exist, empty/outlined when no links exist. Both states are clickable. Clicking opens a popup titled "[Box name] links upward to these boxes:" with items listed. The popup includes a picker for selecting a target, filtered to valid upward targets (adjacent higher How level and This–Then boxes), with "Add" (orange) and "Cancel" buttons at the bottom. A square also appears on the bottom edge showing downward links (to adjacent lower How level); clicking it opens a popup with a picker filtered to valid downward targets, also with "Add" (orange) and "Cancel" buttons at the bottom. How box affordance squares use neutral black/grey styling (not orange) consistent with the How page black-and-white treatment. Vertical How link counts are on by default. Same-page How→How links are not counted in top/bottom hierarchy squares; they appear in the lateral-link square instead. For larger numbers of digits, squares widen into rectangles.
- Lateral How link affordance on How boxes: a small square on the right middle of the right border of How boxes, visually distinct from the vertical hierarchy squares. The square appears dark when lateral links exist and outlined when no lateral links exist. Clicking the square opens a popup for managing non-hierarchical How cross-links (links to How pages with no level, same-page links, and links to non-adjacent levels). The popup includes a picker for selecting a target, filtered to valid lateral targets, with "Add" (orange) and "Cancel" buttons at the bottom. Controlled by the Show lateral How links (just from boxes on How pages) View toggle. This–Then boxes do not get this affordance. The lateral square does not display a number.

Page types:
- Each subpage has a pageType field: 'this_then', 'how', or 'documentation'
- Older boards without pageType load as 'this_then' (backward compatible)
- Page type is set at creation time via the page-type chooser and is fixed once created (no conversion in MVP)
- This–Then pages have a coloured top bar showing "THIS–THEN", the page name, and "+ Add This–Then box" (the bar colour matches the page colour, or the first column colour for single-page boards). The page name shown in this bar is directly editable by double-clicking it (Enter saves, Escape cancels). This–Then pages use the existing This→Then causal layout with columns, arrows, and This/Then sidebars
- How pages display a grid of white boxes with a neutral grey top bar showing "HOW", the page name, and "+ Add How box", no columns, no arrows, no This/Then sidebars. The page name shown in this bar is directly editable by double-clicking it (Enter saves, Escape cancels). How pages may participate in a board-defined level structure (e.g. level 1 = projects, level 2 = teams, level 3 = people) — the levels give stable meaning to upward and downward How links. Each How box shows a stable visible ID (H001, H002, etc.) in the bottom-left corner. A page-level "+ Add How box" button is in the header bar. New How pages are seeded with 8 starter boxes (H001–H008) each labelled with the standard How-page empty/new wording "How (action, person, team, unit, organisation, capability, …)". Numbering is not embedded in the box text itself (the stable H001–H008 IDs already provide the numbering). How page levels are editable directly in the board UI via the page info bar (Level N with move-up/move-down controls, and a toggle to set or remove the level). A How page may have no level — it does not participate in the normal vertical up/down hierarchy logic but still loads and works safely. The visual layout for this version remains a simple grid; the level concept is a light hierarchy structure, not a rigid ontology.
- Documentation pages display rich-text content in read mode by default, showing formatted text without editing UI. Each Documentation page has a purple top bar showing "DOCUMENTATION" and the page name. The page name shown in this bar is directly editable by double-clicking it (Enter saves, Escape cancels). An Edit button in the page header switches to edit mode with a WYSIWYG toolbar (bold, italic, underline, headings, bullet list, numbered list, block quote, link, insert clone, undo) and a contenteditable area. Edit mode includes Save (saves changes and returns to read mode) and Cancel (discards unsaved changes and returns to read mode) buttons. Content is stored as sanitized HTML in the board state. Documentation pages have no boxes, no columns, no This–Then structure. They are designed for definitions, explanatory notes, background notes, evaluation writeups, evidence summaries, and internal documentation. Documentation page content is preserved in saved boards, localStorage, and DOVIEW-STATE snapshots (as stripped plain text). Documentation pages support clone blocks (see Documentation page clones below).

How page details:
- How boxes use stable 3-digit numbering (H001, H002, H003, …). Once assigned, a How box number never changes — not after deletion, not after reordering. If H003 is deleted, H004 stays H004. New boxes take the next available number (e.g. H009).
- How box state keys use the format SUBPAGE_ID-HXXX (e.g. p2-H001). All standard state commands (setLight, addSofar, setPriority, etc.) work on How boxes using these keys.
- How boxes support all the same entry types as This–Then boxes: So far, My notes, Notes 1, Notes 2, Notes 3, traffic lights, priorities, detail text, and custom borders.
- The entry panel for How boxes provides: "+ Add box after", "+ Add text next to box", "Move earlier", "Move later", "Move to first", "Move to last", and "Delete box". Reordering changes only display order, never stable box numbers.
- The board chat can manage How boxes via: [ACTION:addHowBox:SUBPAGE_ID:LABEL], [ACTION:removeHowBox:SUBPAGE_ID:HOW_ID], [ACTION:renameHowBox:SUBPAGE_ID:HOW_ID:NEW_LABEL].

This–Then links:
- This–Then links are formal directional links between boxes on This–Then pages.
- Links can be within the same This–Then page or across different This–Then pages.
- Links cannot target How pages or Documentation pages.
- Each link has an explicit direction displayed as Box A → Box B.
- The direction arrow is clickable to reverse it.
- Up to one link in each direction between any pair of boxes is allowed (maximum two links between a pair total).
- Same-column links are allowed.
- Feedback loops are allowed — the same box may appear in both the left count (incoming) and right count (outgoing).
- Clicking a left or right count on a box opens a list of connected items showing: connected box title and page name. Clicking an item in the list navigates to that box.
- Link creation and management for This–Then boxes is done exclusively through the count affordance popups (left or right count circles for This–Then links, bottom square for How links). The box details pane does not contain link-creation sections. The picker in each popup groups boxes under page name, pages in tab order, boxes in their page visual/logical order (not alphabetical).
- The board chat can manage This–Then links via: [ACTION:addTTLink:FROM_BOX_ID:TO_BOX_ID], [ACTION:removeTTLink:LINK_ID], [ACTION:flipTTLink:LINK_ID].
- This–Then links are preserved in saved boards, localStorage, and DOVIEW-STATE snapshots.
- Each This–Then link can have Measures and Evaluation Questions attached to the link itself (independently of any Measures/Evaluation Questions linked to its source or target boxes). This is managed from the This–Then link detail popup only — How links do not have this feature in this build. See Link details below.


Measures and Evaluation Questions:
- Measures and Evaluation Questions are board-level reusable objects, not box-local notes.
- Measures use stable IDs: M001, M002, M003, etc. Evaluation Questions use stable IDs: EQ001, EQ002, EQ003, etc. IDs are board-global and never renumbered after deletion.
- Each Measure has: ID, title, main text, notes 1, notes 2, notes 3, and a linked boxes list.
- Each Evaluation Question has: ID, question text, main text, notes 1, notes 2, notes 3, and a linked boxes list.
- This–Then boxes, How boxes, and Final Outcome boxes all have Measures and Evaluation Questions fields in their detail panes.
- Each field allows: search/select existing items, link one or more existing items, create new item, view all items.
- At the end of each picker: "Create new measure" / "Create new evaluation question" and "View all measures" / "View all evaluation questions".
- If a new item is created from within a box picker, it defaults to being associated with that box (V1.0.13.1 (Fix 3): user-facing wording is associated/association for Measures and Evaluation Questions; structural box-to-box links keep links/linked terminology). If created from the board-level registry, it may exist with no associations.
- Board-level registries (accessible from the header bar) list all Measures or all Evaluation Questions showing ID, title/text, association status (associated count or "not associated"), and a delete button (V1.0.13.1 (Fix 3): user-facing status wording is "associated" / "not associated"; internal data shape is unchanged). Clicking an item opens its detail pane.
- Measures and Evaluation Questions are edited at their own detail panes (source of truth), not through copied text elsewhere. Each detail pane includes a delete button at the bottom.
- Linked items shown on box panes are clickable — clicking opens that item's detail pane.
- The board chat can manage Measures and Evaluation Questions via ACTION commands (see below).
- Measures and Evaluation Questions are preserved in saved boards, localStorage, and DOVIEW-STATE snapshots.
- When a Measure or Evaluation Question is edited, created, associated, dis-associated, or deleted, all visible references (under-box display, entry panel associated items, clones) update immediately without requiring the user to leave the page and come back. The current page, open box selection, and scroll position are preserved as far as practical. (V1.0.13.1 (Fix 3): user-facing wording uses associated/association for Measures and Evaluation Questions; structural box-to-box links keep links/linked terminology.)
- Measures and Evaluation Questions can also be attached to This–Then links (see Link details). Deleting a Measure or Evaluation Question also removes any links to it held on This–Then links, not only the links held on boxes. How links are NOT supported for this feature in the current build.
- This build does NOT yet include: clones inside box notes or Measure/EQ notes, named/saved multiple views, per-page custom view overrides, reordering of under-box display types, or richer list browsing and link panes. These are planned for later builds.

View system (UPDATED V1.0.12.4):
- Board-wide View settings, separate per page type (one set for all This–Then pages, one set for all How pages, one set for the Final Outcomes page).
- View button appears in the page info bar on This–Then pages, How pages, and the Final Outcomes page (not Documentation pages, not Overview).
- This–Then View toggles: Show traffic lights, Show priorities, Show main text under boxes, Show measures under boxes, Show evaluation questions under boxes, Show This–Then link counts (just between boxes on This–Then pages), Show up-and-down links to How boxes, Show non-up-and-down links to How boxes.
- How View toggles: Show traffic lights, Show priorities, Show main text under boxes, Show numbering, Show measures under boxes, Show evaluation questions under boxes, Show up-and-down links to This–Then and How boxes, Show non-up-and-down links to This–Then and How boxes.
- Final Outcomes View toggles: Show traffic lights, Show priorities, Show main text under boxes, Show measures under boxes, Show evaluation questions under boxes.
- The non-up-and-down link option is the last item in both the This–Then and How View lists (it is less central and is de-emphasized relative to the other display options).
- Defaults (UPDATED V1.0.12.9a): For This–Then pages, Show traffic lights, Show priorities, and Show up-and-down links to How boxes are on by default; Show This–Then link counts (just between boxes on This–Then pages), Show measures, Show evaluation questions, Show main text under boxes, and Show non-up-and-down links to How boxes are off by default. For How pages, Show numbering, Show traffic lights, Show priorities, and Show up-and-down links to This–Then and How boxes are on by default; Show measures, Show evaluation questions, Show main text under boxes, and Show non-up-and-down links to This–Then and How boxes are off by default. For the Final Outcomes page, Show traffic lights and Show priorities are on by default; all other toggles are off by default. Users can change these via the View button on each page.
- Clear All and Restore Defaults (UPDATED V1.0.12.4): on This–Then and How pages, the View settings panel provides two controls below the list of toggles — Clear All (turns off every item shown in that page type's View list) and Restore Defaults (restores the built-in defaults for that page type as described above). These are view changes only, not data deletion. Clear All does not delete content or links.
- View settings are presentation only — hiding an item does not remove it from the model.
- View settings are saved with the board and restored on reload.

Under-box display:
- Measures, Evaluation Questions, and Main text can be displayed directly below boxes on This–Then, How, and Final Outcomes pages, controlled by View toggles.
- Display order is fixed: Measures, then Evaluation Questions, then Main text.
- Under-box blocks are non-editable (edit via the box detail pane or linked item pane). On the Final Outcomes page, under-box blocks match the width of the Final Outcome box above them.
- If a box has no content for a display type, nothing is shown (no empty placeholders).
- Multiple Measures or Evaluation Questions appear in one scrolling block each.
- Measure blocks show: M001 Title. Evaluation Question blocks show: EQ001 Question text.
- Clicking a Measure or Evaluation Question in under-box display opens its detail pane.
- Main text under boxes refers to the box detail text (formerly called "text under box" or "text next to box"). URLs in main text are clickable in display mode.
- On How pages, under-box blocks (Measures, Evaluation Questions, Main text) render below the box, not to the right, matching the same vertical layout used on This–Then and Final Outcomes pages.

This–Then / How link affordance display (UPDATED V1.0.12.4):
- This–Then boxes show small circle affordances on the left and right box edges for TT link counts (controlled by Show This–Then link counts (just between boxes on This–Then pages) toggle), and a small square on the bottom for the strictly-hierarchical count of How boxes linking to them (controlled by Show vertical How link counts (just from boxes on How pages) toggle). Under the strict-hierarchy model, the bottom square on a This–Then box counts only incoming links from Level-1 How boxes; non-up-and-down How→This–Then links (from Level-2-or-deeper How boxes) are not counted here. This–Then boxes also now have a right-border non-up-and-down link affordance — a small square, visually consistent with the non-up-and-down affordance on How boxes, no number, dark when non-up-and-down links exist and outlined when none exist, positioned UP on the right border (away from the middle-right area used by This–Then link arrows, the right-edge outgoing-count circle, and any jump/drill triangles in the lower-right) so it no longer clashes with them visually — controlled by the new Show non-up-and-down links to How boxes toggle which is off by default. When the toggle is on, circles are always visible: filled with count when links exist, empty/outlined when no links exist. Both states are clickable and open a popup with link management including a + control.
- How boxes show a square on the top edge for strictly-adjacent upward hierarchical links (controlled by Show vertical hierarchical link counts toggle), a square on the bottom edge for strictly-adjacent downward hierarchical links, and a small square on the right middle of the right border for lateral How links (controlled by Show lateral links (same-level, skipped-level, and This–Then links) toggle). The top/bottom affordances on How boxes are strictly for hierarchical links only. Only Level-1 How boxes may have hierarchical links to This–Then boxes; on Level-1 How boxes, the top affordance counts hierarchical links up to This–Then boxes and the bottom affordance counts hierarchical links down to Level-2 How boxes. On Level-2-or-deeper How pages, the top affordance counts hierarchical links up only to the next higher How level and the bottom affordance counts hierarchical links down only to the next lower How level. Any non-adjacent How-to-How link is treated as lateral. Any link from a Level-2-or-deeper How box to a This–Then box is treated as lateral, not hierarchical. How box vertical squares use neutral styling (not orange). For larger numbers of digits, squares widen into rectangles. The lateral square is visually distinct from circles and does not display a number. All affordances are always visible when their toggle is on: filled/dark when links exist, empty/outlined when empty.
- Top/bottom affordances on How boxes are hierarchical only. The right-border affordance on How boxes is lateral only. Hierarchy and lateral relationships are kept visually and conceptually distinct.
- Lateral-link affordance visibility: to avoid clutter, non-up-and-down link affordances are hidden by default on both This–Then and How boxes. Each page type has its own View option to control them (see "View system" below). When off, no right-border non-up-and-down link affordances are shown. When on, right-border non-up-and-down link affordances appear on all eligible How and This–Then boxes and can be used to inspect or manage lateral links. Top and bottom hierarchical affordances are part of the normal hierarchy model and are not part of this lateral toggle.
- Jump affordance visual language (UPDATED V1.0.12.3): the on-box jump affordance is a small down triangle in the lower-right of the box, with no enclosing box, matching the visual language used for overview-page drill-down boxes. It replaces the earlier boxed right-arrow jump affordance. The down triangle reads as drill-down / open subpage rather than as a link affordance, reducing clashes with the new non-up-and-down link affordance on the right border. If a box also has a hasSubpage drill-down triangle, the jump triangle is offset to the left to avoid overlap.
- All affordances are small and clean, extending slightly beyond the box edge for visual clarity. Counts are also accessible via tooltip.

How links:
- How links are formal upward links from How boxes to higher-level boxes.
- A How link means: this How box helps make that target box happen. Links can go upward (to higher-level How boxes or This–Then boxes), downward (to lower-level How boxes), or laterally (to same-level, no-level, or non-adjacent How boxes).
- A How box may link upward to a This–Then box or a higher-level How box on another How page.
- The upward meaning is not limited to This–Then targets — it includes any box at a higher level in the board's hierarchy (whether on a This–Then page or on a higher-level How page).
- How links cannot target Documentation pages.
- How links are always stored as upward links from the How box to the target.
- Link creation and management for How boxes is done exclusively through the link affordance popups (top square for upward links, bottom square for downward links, lateral square for lateral links), each with a picker filtered to valid targets for that direction and "Add" (orange) / "Cancel" buttons at the bottom. The box details pane does not contain link-creation sections.
- Each link shows the target box label, page name, and a remove button.
- The board chat can manage How links via: [ACTION:addHowLink:FROM_HOW_BOX_ID:TO_BOX_ID], [ACTION:removeHowLink:LINK_ID].
- How links are preserved in saved boards, localStorage, and DOVIEW-STATE snapshots.
- How page hierarchy direction (UPDATED V1.0.12.3 — STRICT ADJACENCY): the How link system supports a board-defined up/down level structure for How pages (e.g. level 1 = project page → level 2 = team page → level 3 = person page). Hierarchical links are strict and adjacent-level only. Upward hierarchical How links go only to the next-adjacent higher How level (fromLvl-1); downward hierarchical How links go only to the next-adjacent lower How level (fromLvl+1). Only Level-1 How boxes may have hierarchical links to This–Then boxes — Level-2-or-deeper How boxes must not have hierarchical links directly to This–Then boxes. Any How→How link that is same-level or skips levels is lateral. Any Level-2+ How→This–Then link is lateral (not hierarchical). Links from How pages with no level are lateral. Each How page has a numeric level editable directly in the board UI, with the option to have no level. A How page with no level does not participate in the normal vertical up/down hierarchy logic but still loads and works safely. Link-affordance popups filter their target lists by direction: on a Level-1 How box, the top-square popup offers upward targets (adjacent higher How level — which here means This–Then pages — plus any higher-level How pages if they exist); on Level-2+ How boxes, the top-square popup offers only adjacent higher How-level targets (not This–Then). The bottom-square popup offers only adjacent-lower-How-level targets. The lateral square popup offers same-page How boxes, no-level How boxes, non-adjacent-level How boxes, and (for Level-2+ How boxes) This–Then boxes. This is a general board-defined hierarchy concept, not hard-coded page types, and it remains compatible with future How page types such as projects, roles, resources, capabilities, and governance.
- Same-page How→How links: same-page How→How links are excluded from the hierarchical top/bottom count model. They appear in the lateral-link square on the right border of How boxes. This keeps the hierarchy counts clean while providing a clear place for same-page relationships.
- Lateral How link affordance (UPDATED V1.0.12.4): How boxes have a small square on the right middle of the right border, visually distinct from the hierarchy squares, appearing dark when lateral links exist and outlined when inactive. This–Then boxes also have a right-border lateral affordance for non-up-and-down How→This–Then links; on This–Then boxes this square is positioned UP on the right border (well away from the middle-right area used by This–Then link arrows and from the bottom-right jump/drill triangles and the right-edge outgoing-count circle) so it does not visually clash with them. On both page types the affordance is hidden by default and revealed via a View toggle (see View system below). The affordance does not display a number — it only communicates presence/absence. Lateral How links include same-page How→How links, links to How pages with no level, links to non-adjacent How levels, and Level-2+ How→This–Then links.

Link details (first-class link objects):
- Links (both This–Then links and How links) are first-class objects with their own details panes.
- Each link has: source box, target box, link type (this_then or how), Main text, Notes 1, Notes 2, Notes 3.
- Each link's details pane shows a header with the link direction (e.g. "Box A → Box B"), the link type badge, and editable fields for Main text, Notes 1, Notes 2, Notes 3.
- URLs in link Main text, Notes 1, Notes 2, and Notes 3 are clickable in display mode only, following the same safe URL rules as the rest of the system.
- Link details panes can be opened from: the Links registry in the header bar, the ⓘ button on link rows in box entry panels, the ⓘ button on items in link count list modals, and from global search results matching link text.
- Link details are preserved in saved boards, localStorage, Copy HTML Board, and DOVIEW-STATE snapshots.
- Link detail fields (mainText, notes1, notes2, notes3) are stored directly on each link object. Links remain the source of truth for link direction, source, and target — the link details pane does not change box data.
- Older boards without link detail fields load with empty strings for all link text fields (backward compatible).
- This–Then link Associated Measures and Associated Evaluation Questions (V1.0.13.1 (Fix 3) wording): a This–Then link's detail popup includes an Associated Measures section and an Associated Evaluation Questions section below the Notes fields. These sections follow the same general pattern used on box panes: a picker to select an existing Measure or Evaluation Question, an "Associate" button to associate it with this structural link, per-item Remove association controls, and "Create new measure" / "Create new evaluation question" and "View all measures" / "View all evaluation questions" footer links. Clicking an associated Measure or Evaluation Question opens its detail pane (closing the link popup). TT-link Measure/EQ associations are stored in two arrays (measures and evalQuestions) on the This–Then link object itself, persist through save/reload, localStorage, Copy HTML Board, and DOVIEW-STATE snapshots, and are cleaned up automatically when a Measure or Evaluation Question is deleted. How links do NOT have this feature in the current build — their detail popups only show Main text and Notes fields.
- Visual linked-box reveal mode (border-click): clicking the border area of a This–Then box (within ~10px of any edge) activates reveal mode, which visually marks all boxes linked to the selected box via This–Then links. The selected box receives the strongest highlight style (3px dark border with subtle shadow, same as inspect-home). Linked boxes receive the same greyed secondary highlight as in count-click inspection mode (2px lighter grey border with slight background tint). On linked boxes, a small triangle marker appears: a left-pointing triangle on the left side for boxes the selected box links outward to, and a right-pointing triangle on the right side for boxes that link inward to the selected box. Boxes linked in both directions show both markers. Reveal markers apply only to This–Then boxes. In addition, any How boxes that act on the selected This–Then box (via How links) are highlighted with the same darker-border treatment (not triangle markers). When a reveal state is active, clicking on one of the small directional arrows on a revealed grey box opens the link-info/evidence popup for that specific link, providing a direct route to the evidence. Reveal persists across page navigation (so linked boxes on other This–Then pages and How boxes on How pages show their markers/highlights when those pages are viewed) and clears when the user clicks empty space on the board. Clicking the body (non-border area) of a This–Then box opens the normal details pane as before. Clicking a How box opens its details pane and also activates the same secondary linked-box highlight mode used elsewhere: the clicked How box receives the strongest home-box highlight (3px dark border with subtle shadow), while connected This–Then boxes (via How links) and connected How boxes (via How-to-How links) receive the weaker secondary linked-box styling (2px lighter grey border plus a slight grey background tint). No left/right triangle markers are added for the How-origin reveal path. How box highlight also persists across pages and clears on clicking empty space. The left/right triangle reveal semantics do not apply to How boxes.
- Visual linked-box reveal mode (count-click / relationship inspection): clicking a link count circle (This–Then counts, How counts, or vertical How link counts) enters a relationship-inspection state distinct from the border-click reveal. In inspection mode, the home box receives a strong 3px dark border with subtle shadow, while linked boxes receive a weaker 2px grey border with slight background tint. Clicking a linked box name in the popup navigates to that box and scrolls it into view, preserving the inspection highlights. In inspection mode, linked boxes also show small directional arrows (left-pointing for outgoing links, right-pointing for incoming links). Clicking on one of these arrows on a revealed grey box opens the link-info/evidence popup for that specific link, providing a direct route to the evidence without needing to go through the count popup. If a box shows arrows on both ends because both directions are relevant, each arrow opens the evidence for its own specific direction. The inspection state persists across page navigation (returning to the original page still shows the home box and linked boxes highlighted) and clears only on intentional exit (clicking empty space, border-clicking a box, clicking a box body to open its entry panel, or selecting a new count-click set). Border-click reveal and count-click inspection are mutually exclusive — activating one clears the other.
- Link count popup headings: when a user clicks a link count circle to open the list of connected boxes, the box name in the popup heading is displayed in italics for readability (e.g. "*Box name* makes these THIS-THEN boxes happen").

Links registry:
- A board-level "Links" registry is accessible from the header bar, alongside Measures and Eval Questions.
- The Links registry lists all structural box-to-box links in the board (V1.0.13.1 (Fix 3): the top Links menu lists structural links only — This–Then links, How / up-and-down links, and non-up-and-down links — and is not used for Measures/Evaluation Question associations, which use the Measures and Eval Questions registries instead).
- Each row shows: link type badge, source box name, → arrow, target box name, page name(s), and a short snippet of link Main text if present. Type badges (V1.0.13.1 (Fix 3)): "This–Then" (orange) for This–Then links, "How" (blue) for How / up-and-down links (upward and downward), and "Non-up/down" (slate) for non-up-and-down structural links. The wording "Other links" is intentionally not used.
- Clicking a row in the Links registry opens that link's details pane.
- The Links registry does not support creating or deleting links — links are created and removed from box entry panels and via board chat commands as before.

- The board chat can manage Measures via: [ACTION:createMeasure:TITLE] (create), [ACTION:linkMeasure:MEASURE_ID:BOX_ID] (associate with a box — internal action token kept for compatibility), [ACTION:unlinkMeasure:MEASURE_ID:BOX_ID] (remove association from a box — internal action token kept for compatibility), [ACTION:deleteMeasure:MEASURE_ID] (delete; automatically removes all box associations).
- The board chat can manage Evaluation Questions via: [ACTION:createEQ:QUESTION_TEXT] (create), [ACTION:linkEQ:EQ_ID:BOX_ID] (associate with a box — internal action token kept for compatibility), [ACTION:unlinkEQ:EQ_ID:BOX_ID] (remove association from a box — internal action token kept for compatibility), [ACTION:deleteEQ:EQ_ID] (delete; automatically removes all box associations).

Documentation page clones:
- Documentation pages can contain clone blocks — linked copies of selected board elements that update automatically when the source changes.
- Clones are inserted via the "Clone" button in the Documentation page edit toolbar, which opens a clone chooser. The chooser allows the user to select a source type, then pick a specific source object.
- Clonable source types: page title, box title, box main text, Measure (ID and title only), Evaluation Question (ID and question text only).
- Clone blocks are rendered as block-level elements on the page with a light grey background. Users can mix normal rich text and clone blocks freely.
- Clones are one-way: source changes update the clone display. Clone text is not directly editable.
- Clicking a clone in read mode navigates to the source page, opens the source box detail pane, or opens the Measure/Evaluation Question detail pane as appropriate.
- If a clone's source is deleted, the clone shows a broken-reference state ("[Source deleted]") with visual styling until the user removes it manually.
- In edit mode, each clone block shows a ✕ remove button to delete the clone from the page.
- Clones are stored as lightweight HTML markers (data attributes referencing source type and key) within the Documentation page content. No text is duplicated — source text is resolved live on each render. This keeps board size small and ensures clones always reflect current source content.
- Clone data is preserved automatically via the existing docContent persistence in saved boards, localStorage, Copy HTML Board, and DOVIEW-STATE snapshots.
- Measures and Evaluation Questions cloned onto a Documentation page appear as their ID and title/question text only (e.g. "M001 Revenue growth" or "EQ001 Did we achieve X?"). Clicking them opens their detail pane.
- This build does NOT include clones inside box notes or Measure/EQ notes — only on Documentation pages.

Global search (v1):
- A board-level search feature accessible from the "🔍 Search" link in the header bar.
- Opens a modal with a search input field. Results appear as the user types (minimum 2 characters).
- Search scope (v1): page titles, This–Then box titles, How box titles, Final Outcome titles, box main text (detail text), Measure titles and main text, Evaluation Question text and main text, Documentation page content, link Main text.
- Does NOT search: notes fields (So far, My notes, Notes 1, Notes 2, Notes 3) in v1.
- Search results show: item type (e.g. "This–Then box", "Measure", "Documentation"), page name, object ID where relevant (e.g. H003, M012, EQ004), and a short snippet of matching text.
- Clicking a search result navigates to the relevant page and opens/focuses the relevant item or pane where practical. For Measures and Evaluation Questions, clicking opens the detail pane. For boxes, clicking navigates to the page and selects the box.
- Results are capped at 50 items for performance. This is the limited v1 scope only.

Interactivity:
- Click any box to open entry panel below
- So far entries (append by default, edit with ✎ pencil)
- My notes entries (append by default, edit with ✎ pencil)
- Notes 1, Notes 2, Notes 3 entries (three additional note categories, each with their own pill and colour, append by default, edit with ✎ pencil)
- Auto-save when clicking any pill: clicking any pill (So far, My notes, Notes 1, Notes 2, Notes 3) saves any text in the textarea to that category. The Save button saves to whichever mode is currently active and stays on that mode.
- Traffic light manual override (clickable dots)
- Priority selector (A/B/C/D/E/BAU toggle buttons)
- URLs are automatically clickable in display mode only — entries, chat, under-box main text, Documentation page content (including bare domains typed in the WYSIWYG editor), clone displays, and (V1.0.13.1 (Fix 4)) popup display text in the This–Then / How link detail popup, Measure detail popup, Evaluation Question detail popup, and Board info / Page info popups all linkify URLs. In Fix 4 popups, the linkified text is shown in a small read-only display block placed above each editable textarea and is rendered only when the saved field has content; the textarea below remains the edit surface (URLs are not active while the user is editing text), and the stored text is not altered by linkification. This includes full URLs (https://...) and bare domains (e.g. doviewplanning.org). On Documentation pages, bare domains and URLs within the saved HTML content are automatically detected and made clickable in read mode even if the user did not insert them as explicit links in the editor. While editing (textareas, inline edits), raw text is shown to prevent accidental navigation. Only safe schemes are allowed (https, http, mailto). Bare domains are linked as https://. Dangerous schemes (javascript, data, etc.) are not linkified.
- Traffic light cascade based on dependency map
- Silent AI classification of So far entries (defer/blocker/green)
- Double-click any box to edit its label text inline (Enter to save, Escape to cancel). While inline editing is active, clicks inside the editable text reposition the cursor without exiting edit mode — the box click handler is suppressed when the target is contentEditable. If the user deletes all text so that the label is empty, then clicks away or presses Enter, the box label persists as intentionally empty — it does not repopulate with the previous text or silently restore a placeholder from the previous saved value. This applies to both This–Then boxes and How boxes.
- Double-click any column heading to edit it inline (Enter to save, Escape to cancel)
- Double-click any final outcome to edit its label text inline (Enter to save, Escape to cancel)
- Double-click any page tab to rename it inline (Enter to save, Escape to cancel). While inline editing is active, clicks inside the editable text reposition the cursor without triggering page navigation — the tab click handler uses a short delay that is cancelled by the double-click.
- Double-click the page name in a This–Then, How, or Documentation page's top page bar to rename that page inline (Enter to save, Escape to cancel). This edits the same underlying page label used by the tab, so the tab and bar stay in sync after rename, and the rename persists across save/reload.
- Double-click the board title in the header to rename it inline (Enter to save, Escape to cancel)
- Two "+" buttons to add a new page: one on the left side of the tab bar immediately after Final Outcomes (always visible even when tabs scroll), and one at the far right end of the tab bar. Both open the same page-type chooser with three options: This–Then Page, How Page, or Documentation Page. After the user selects one, the page is created with that type and the board navigates to it. Page type is fixed once created (no conversion). Default page titles use the pattern "This→Then: New page", "How: New page", or "Documentation: New page" — but the user can edit the entire title freely afterward (the prefix is only a default, not enforced).
- Page info bar below the header shows the page name with a ◄ triangle to move the page left in the tab order (cycles to the end when at the start), a "Page info" link, a "Delete" link for subpages (requires confirmation), and an ↩ undo button (greyed out when nothing to undo). The bar does not include a "Page:" prefix. For How pages, the page info bar also shows the page's How level (e.g. "Level 2" or "No level") with ▲ and ▼ controls to move the page up or down in the How level hierarchy, and a toggle control to set or remove the level. A How page with no level does not participate in the normal vertical up/down hierarchy logic but still loads and works safely. Moving a page's level swaps it with the adjacent How page at the target level. The controls are disabled at the boundaries (▲ disabled at level 1, ▼ disabled at the highest level). The level concept is browser-editable — users can understand and change the relative order of How pages without using board chat.
- Entry panel structural buttons: when a box is selected, the entry panel includes structural editing buttons allowing changes directly from the board without using the chat. For This–Then subpage boxes: "+ Add box", "+ Add column", "+ Add main text", "Move up", "Move down", "Delete box", "Delete column". For How boxes: "+ Add box after", "+ Add main text", "Move earlier", "Move later", "Move to first", "Move to last", "Delete box". For final outcome boxes: "+ Add", "+ Add main text", "Move up", "Move down", "Delete". All delete actions require confirmation. "+ Add column" creates a column called "New column" with one starter box called "New box" to the right of the current column. "+ Add box" adds a "New box" at the top of the column (or a new final outcome). The Final Outcomes page always shows a page-level "+ Add final outcome" button next to the heading, so final outcomes can be added even when the list is empty. "+ Add main text" creates an empty main text field for the box, editable from the detail pane. "Move up" and "Move down" reorder the selected box within its column or the final outcomes list; they are disabled when the box is already at the top or bottom respectively. Double-click new boxes, column headings, and page tabs to rename them.
- Deleting a box removes that box's This–Then links, How links, and any links from that box to Measures or Evaluation Questions. It does not delete the board-level Measure or Evaluation Question objects themselves. Never renumber after deletion.
- Deleting a page removes that page's boxes, page info, documentation content for that page, and any links attached to that page's boxes. It does not delete board-level Measures or Evaluation Questions. Never renumber after deletion.
- Deleting a Measure or Evaluation Question: if the item is linked to boxes, the user is warned and shown the linked boxes. If confirmed, those links are automatically removed from all boxes. Any attachments of the item on This–Then links are also removed automatically as part of the same delete. The item is deleted from the board-level registry. IDs are never renumbered after deletion.
- If a clone source is deleted, the clone remains visible on Documentation pages as a broken reference marker until the user removes it manually later.
- Safe deletion and broken-clone states survive save board, reload board, copy board as HTML, and reopening saved HTML.
- Undo fully restores all state including links, measures, evaluation questions, board info, page info, and documentation content.
- Older boards without pageType still load as This–Then boards.

Board info and Page info:
- Board info: a clickable link in the top header opens a modal for notes about the board as a whole — overall assumptions, cross-page links, evidence, caveats, definitions, and URLs. This is a lightweight enhancement to the DoView minimum spec that gives users a place to record evidence about relationships without complicating the core box structure. More advanced formal linking systems may be built by others later, but are not part of the minimum spec.
- Page info: a "Page info" link in a bar below the header (showing "Page: [page name] · Page info") opens a modal for notes about that specific page — evidence about links between boxes on this page or between this page and other pages. Every page including Overview and Final Outcomes has its own Page info.
- (V1.0.14.1a (Fix 5)) Both Board info and Page info popups use a simple view/edit pattern in normal editable boards. They open in rendered view mode by default — the saved text is rendered once with clickable URLs (via the existing linkify display block), and an Edit button is shown alongside Close. Clicking Edit replaces the rendered view with the editable textarea and shows Save / Cancel controls. Save commits the new text and returns to rendered view (the modal stays open so the user can immediately see the saved text rendered with clickable URLs). Cancel discards unsaved changes and returns to rendered view, preserving the previous saved text. Empty fields show a modest empty-state line ("No board info yet. Click Edit to add some.") rather than a duplicate textarea-plus-rendered-block, keeping the popup clean for never-filled-in fields. In read-only copies, only the rendered view is shown — Edit, Save, and the textarea are hidden, URLs in the rendered view remain clickable, and the popup can be dismissed with Close. This replaces the previous behaviour where the popup showed both the rendered display and the editable textarea side by side, which was confusing because the same saved text appeared twice.
- (V1.0.14.1a (Fix 4 + Fix 5)) Board info edit mode also includes a compact "Top right text (optional)" field below the main Board info textarea. This single-line input edits the same topRightText saved-state value as the existing Top right text editor modal, so users can add or change Top right text from Board info without needing the page-info-bar affordance. The "Top right text (optional)" field appears in Board info edit mode only — it does NOT appear in Page info edit mode (Page info is per-page; Top right text is board-level).
- Both are plain text fields, not structured data. Users can simply name relevant boxes or pages in free text.
- Both are saved in board state, preserved in downloaded HTML, and included when syncing to the main AI chat.
- The board chat AI can read and write Board info and Page info via ACTION commands: [ACTION:setBoardInfo:TEXT] and [ACTION:setPageInfo:PAGE_ID:TEXT].

Chat with board:
- Floating "Chat with board" button (no emoji)
- AI can modify the board via [ACTION:command:args] tags
- State commands execute immediately (past tense in chat: "Done"); structural commands require user confirmation (future tense in chat: "will be added", NOT "has been added")
- Thinking indicator: animated dots show while waiting for AI response
- Undo system (type "undo" or click ↩ button)
- Supports the Anthropic API (auto-detected when available) and external OpenAI-compatible APIs
- Full command reference included in system prompt automatically
- Token-saving optimisation: by default the board chat only sends the current subpage state to the AI, with summary lines for other subpages. If the user includes "whole board", "all pages", or similar in their message, the full board state is sent for that request. Overview and Final Outcomes pages always send full state. Chat history is limited to 10 messages.
- DoView methodology awareness: the board chat AI always receives a compressed summary of DoView methodology rules (outcome phrasing, This→Then logic, vary structure by domain). When the user requests structural changes (adding pages, boxes, columns), the full DoView methodology rules are sent so the AI builds properly structured content.
- Auto-colour subpages: when the board chat adds a new subpage, the engine automatically assigns an unused colour from the palette. The AI is instructed to never ask the user to choose colours.
- Page type awareness (refined V1.0.14.0 (Fix 2)): the board chat uses [ACTION:addSubpage:LABEL:PAGE_TYPE] where PAGE_TYPE is this_then, how, or documentation. When the user asks to add a page WITHOUT specifying a page type AND without a populated-page trigger ("full DoView", "complete DoView", "full logic", "full populated page", "populate it", "add DoView This–Then boxes", "create a This–Then logic for X", "add a This–Then page", or "add a page and fill it in"), the board chat asks the page-type clarification question and waits for the user's answer before creating anything. When the user has clearly specified the type (e.g. "add a This–Then page", "add a How page", "add a Documentation page") OR has clearly triggered populated-page behaviour, the board chat goes ahead with the appropriate proposal without asking. Explicit type wins: "make a Documentation page with a full DoView" still creates a Documentation page.
- Populated vs blank This–Then page creation in the board chat (V1.0.13.2 (Fix 1) reliability rule, refined V1.0.14.0, refined V1.0.14.1b (Fix 7, Fix 8)): when the user asks the board chat to add a NEW This–Then page about a topic — phrasings such as "add a This–Then page about X", "add a new This–Then page about X", "populate it", "add DoView This–Then boxes", "create a This–Then logic for X", "add a page and fill it in", AND (V1.0.14.0 (Fix 3)) any phrasing using "full DoView", "complete DoView", "full logic", or "full populated page" (e.g. "add a new page called X which includes a full DoView on it", "add a complete DoView page about X", "create a full logic for X", "add a full populated page about X", "make a page on X with the full DoView logic"), AND (V1.0.14.1b (Fix 7)) any phrasing using "do/make the full map (for X)", "map this out", "build the full logic", "build out (a/this/the) page", "fill out this page", or "turn this into a full page" — this is treated as a POPULATED-page request. The board chat must emit, in ONE response, the FULL set of action tags so the engine can apply the populated page in a single OK confirmation step: one [ACTION:addSubpage:LABEL:this_then], plus enough [ACTION:renameColumn:NEW_PAGE_ID:0:HEADING] / [ACTION:addColumn:NEW_PAGE_ID:COLUMN_INDEX:HEADING] tags to reach AT LEAST 3 columns total (the engine creates a default first column called "First Stage" which can be renamed via renameColumn), plus enough [ACTION:addBox:NEW_PAGE_ID:COLUMN_INDEX:LABEL] tags to reach AT LEAST 6 boxes total spread across those columns, all using compact DoView outcome phrasing for box labels (no auxiliary-verb forms) and column headings that name real causal stages for the domain (not generic "Stage 1, Stage 2", "Column 1, Column 2", "Step 1, Step 2", or "Phase 1, Phase 2", and (V1.0.14.1b (Fix 8)) NOT a generic logic-model "Inputs / Activities / Outputs" template — single occurrences of those words are allowed if they are genuinely the best heading for that stage, but a page whose headings are dominated by Inputs / Activities / Outputs together will be rejected). Boxes flow left-to-right in plausible This→Then causal order; final/right-hand boxes represent later or higher-level outcomes for the page. (V1.0.14.0 (Fix 4) DOMAIN-SHAPED PAGE STRUCTURE: when creating a populated This–Then page, the number of columns and the number of boxes in each column must reflect the actual causal structure of the domain. Use as many causal stages as are genuinely needed; vary the number of boxes per column according to how many distinct outcomes/conditions/enabling steps exist at that stage. The 3-columns / 6-boxes minimum is a FLOOR, not a target — do NOT default to a mechanical 3x2 or 3x3 template.) The new page id is predicted as p<SP.length+1> based on the existing SP entries shown in the board state. The OK / Cancel confirmation message renders a structured summary that shows the page title, the column headings, and the actual box count for each new page so the user can see what will be added; (V1.0.14.0 (Fix 5)) for This–Then page proposals it also shows a small POPULATED or BLANK/STARTER status pill so it is obvious whether a full page is being proposed or only a shell; (V1.0.14.1b (Fix 4)) destructive structural actions (removeBox, removeColumn, removeSubpage, removeFinalOutcome, deleteMeasure, deleteEQ, removeTTLink, removeHowLink, unlinkMeasure, unlinkEQ) render in the preview as plain-language descriptive lines that name the affected object so the user understands exactly what will be removed before clicking OK. After OK, the engine applies the FULL set of pendingStructural actions (addSubpage + addColumns + addBoxes) carried through from the proposal. A one-box placeholder (just an addSubpage with no addColumn/addBox tags) is NOT acceptable for a populated-page request: (V1.0.14.0 (Fix 6) / V1.0.14.1b (Fix 8)) the engine validates the proposed structural actions against the user's message, and rejects the change before queuing if a populated request would produce fewer than 3 columns, fewer than 6 boxes, column headings that are mechanical numbered placeholders ("Stage 1", "Column 2", "Step 3", "Phase 4"), or column headings dominated by the generic Inputs / Activities / Outputs logic-model template (2 or more of those words used together as headings); a system message in the chat then asks the AI/user to retry with a properly populated, domain-shaped page. Only when the user EXPLICITLY asks for a "blank", "starter", "empty", or "page shell" page does the board chat emit just an addSubpage with no populating tags; in that case the engine creates a blank starter page as before. (V1.0.14.0 (Fix 1) BOARD-CHAT INTENT CLASSIFICATION: before generating any actions, the board chat classifies the request silently into one of: question/explanation only, state-only edit, structural edit on existing pages/boxes, blank This–Then page creation, populated This–Then page creation, How page creation, Documentation page creation, ambiguous page creation requiring clarification, or invalid/incomplete proposal. Pure questions and explanations are answered without any ACTION tags.) (V1.0.14.0 (Fix 2) AMBIGUOUS PAGE CREATION ASKS PAGE TYPE, refined V1.0.14.1a (Fix 2): when the user asks to add/create/make a page but does NOT specify the page type and does NOT clearly imply a populated This–Then page, the board chat asks: "What type of page do you want? - This–Then page — shows what you want to happen in the world, and what leads to what. - How page — shows what is going to be done. - Documentation page — holds supporting notes, explanation and basic rich-text documentation about the DoView Board." It does NOT create the page yet. After the user answers, the board chat proposes the appropriate page. The engine also enforces this safety net: if any addSubpage tag is emitted for an ambiguous request, it is rejected before queuing. V1.0.14.1b (Fix 7) extends the page-creation phrasing recognition to also include "make/add another tab", "set up a section (for X)", "build out (a/this/the) page", and "turn this into a full page" so common informal page-creation phrasings reliably trigger the same classification path; the bare words "section", "tab", and "map" by themselves still do NOT trigger automatic edits, and question-only requests still answer in plain language with no edits.) (V1.0.14.0 EXPLICIT TYPE WINS: if the user explicitly requests a How Page or Documentation Page, that page type is respected even if "full DoView" or similar phrasing also appears in the message — e.g. "make a Documentation page with a full DoView" creates a Documentation page, not a This–Then page.) (V1.0.14.1b (Fix 5, Fix 6, Fix 9, Fix 10, Fix 11, Fix 12, Fix 13) BOARD-CHAT VALIDATION AND TRANSACTIONS: the engine now runs a preflight validation pass over the entire proposed action set BEFORE snapshot, BEFORE any state-action execution, and BEFORE showing OK/Cancel. The validator checks that every proposed action references a real or earlier-in-set page, column, box, Measure, Evaluation Question, final outcome, structural link, or association; that page types in addSubpage are supported (this_then, how, documentation); that column indices are valid for the resolved page; that new labels/headings are non-empty; that addBox actions have valid page+column targets; that the action tag's argument count meets the minimum for that command; and that the action type itself is supported by the engine. If any action fails validation, the WHOLE action set is rejected with a concise plain-language message that always states explicitly that no changes were made (e.g. "I could not apply that change because the target box could not be found. No changes were made.", "Unsupported action: \"moveMeasure\". I could not apply that change. No changes were made.", "I could not apply that change because the action format was incomplete or malformed. No changes were made."). When a chat response contains BOTH state changes and structural changes, both kinds are queued and the OK / Cancel preview shows them together; on OK, all changes are applied as one transaction; on Cancel, none are applied (the engine rolls back via the snapshot). State-only responses still apply directly with a snapshot, preserving the existing safe direct-apply behaviour. Structural-only responses still flow through OK/Cancel as before.) This is a targeted reliability fix; the existing pendingStructural / confirmStructural / execAction code paths are otherwise unchanged, no schema change is made beyond the additive optional sourcesInitialized flag added in V1.0.14.1b (Fix 1), and other board-chat actions (state changes, removals, renames, How-page actions, link/measure/EQ actions, etc.) behave as before. The later security-hardening build and other later items remain deferred.

Persistence:
- localStorage for session persistence
- Save HTML Board button saves complete {B, SP, FO} state as self-contained HTML; filename format is BoardName_2026-03-28_9-30am.html (name + date + time); after saving, a toast message appears confirming the save (or warning if save may have failed)
- Copy HTML Board button opens a popup with the full board HTML that the user can copy and paste into a text file saved with a .html extension, as a fallback when Save HTML Board does not work reliably
- Create and Save New Empty Board button (V1.0.12.3) opens a modal that creates a fresh starter board and immediately saves it as a new HTML file, then switches to that new empty board in the browser. The starter board contains an Overview page, a Final Outcomes page, a This–Then Page 1 with one minimal starter box in the top-left, a How Page 1 with default starter How boxes each labelled with the standard How-page empty/new wording "How (action, person, team, unit, organisation, capability, …)", and a blank Documentation Page 1. The board title resets to "Untitled DoView Board". The modal offers a suggested filename (Untitled_YYYY-MM-DD_HH-MM.html; .html added automatically if omitted; invalid characters sanitized quietly), and three buttons: "Save HTML Board First", "Create and Save New Empty Board", and "Cancel". The destructive moment is confirmation in the modal, not the initial button click. Creating a new empty board resets board info, page info, links, measures, evaluation questions, documentation content, transient selection/highlight state, undo stack, temporary source-reference box, and jump/reveal/inspect state, and creates a fresh board instance identity. The user does not need to click Save HTML Board separately afterward.
- Create Read-Only Copy button (V1.0.12.9b, refined V1.0.12.9c, refined V1.0.14.1a (Fix 1), refined V1.0.14.1b (Fix 3)) produces a separate read-only distributed copy of the current board as a new HTML file, for sharing an agreed board with a funder, manager, contractor, or other external party as a fixed reference version. The normal editable board in the current browser is unaffected — the read-only copy is a separate file, not a conversion of the working board. The saved file carries a marker in its embedded state that tells the engine, on load, to disable content editing through the UI (box editing, link editing, page editing or reordering, traffic light changes, priority changes, board-chat editing, structural edits, etc.). Navigation, links and jumps, search, print, and page View settings still work in the read-only copy. Page View controls are fully usable in read-only mode — opening, changing, and saving View settings (on This–Then, How, and Final Outcomes pages where View exists) applies the visible change in the session; changing View is treated as a viewing/display control, not a content edit, and does not re-enable editing. Board chat is hidden in the read-only copy. The read-only copy is clearly labelled in the top bar with a modest "Read-Only Board" mode label (V1.0.14.1b (Fix 3)) — rendered as a small subtle-outline pill inline with the top utility links (alongside Board info, Measures, …, Training?), not under the title. The label is intentionally low-contrast and reads as a status pill, not as a clickable utility link or a warning banner. The label carries the tooltip "Editing is disabled through the board interface. This is not security protection; someone with technical skills could still edit the HTML." so users are not misled into treating read-only copies as tamper-proof. The label only appears in read-only copies (gated by body.is-readonly); normal editable boards do not show it. There is exactly one read-only label in the UI (no duplicate "Read-Only Copy" elsewhere in the chrome). The browser tab title gets a " — Read-only copy" suffix. The read-only bottom bar is cleaned up — edit-oriented actions (Update board changes to Main AI Chat, Create and Save New Empty Board, Create Read-Only Copy itself) are hidden; only Save HTML Board, Copy HTML Board, Print Board, the Sources link, and the save-state indicator remain. The save-state indicator in the read-only copy reads " · Read-only copy" rather than any misleading dirty/unsaved wording. Popups still open and are viewable in the read-only copy (Board info, Page info, Measure detail, Evaluation Question detail, Link detail, Sources, Clone chooser, etc.), but their text/content fields are clearly non-editable — textareas and text inputs are in a read-only state, contenteditable regions lose editability, and edit/add/delete/save controls inside those popups are hidden or disabled as appropriate; the visual styling of non-editable fields (dimmed background, default cursor, neutralised focus) makes it obvious that the field cannot be typed into or deleted. Links in popup/display content remain clickable in display mode. The global search input remains a live filtering input in the read-only copy (it is a viewing/filtering control, not a content field). The Sources popup is view-only in the read-only copy (V1.0.14.1a (Fix 3): rows render as view-only — title shown as plain text, URL shown as a clickable Open link or as the URL itself if there is no title; Add source, Save, and per-row remove are hidden; Close remains), while remaining editable in the normal board. This is a simple read-only distributed copy/export mode only — not a permissions/roles system, not a partial-edit/annotation mode. The "Read-Only Board" label is a UI-only mode indicator and is not a security guarantee — broader security hardening remains deferred to the later security build. Existing saved boards without the read-only marker continue to load as normal editable boards.
- Print Board button generates a landscape print view in a new tab showing the board title, Final Outcomes, overview tile grid, and each subpage on its own page with This→Then layout (or How grid layout for How pages, or Documentation content for Documentation pages), traffic lights, priority badges, page-type labels in each subpage title block (This→Then, How, Documentation, Final Outcomes), printed page numbers and page titles (shown as a footer line at the bottom of each printed page, e.g. "Page 1 · Final Outcomes & Overview", "Page 2 · Subpage Name"), and a standard footer. All subpages use uniform scaling (based on the largest diagram) so fonts are consistent across pages. The print view uses @page { size: 297mm 210mm } for reliable landscape orientation including on Mac. Page breaks happen at the print-page-wrap boundary so there are no blank pages between intended printed board pages/subpages (UPDATED V1.0.12.7). The footer repeats on every printed page in Chrome via position:fixed in print CSS, and always appears at the bottom in screen view. Footer text: "DoView® Board prototype. Concept by Dr Paul Duignan. This board has not been created, endorsed, or approved by Dr Paul Duignan or by those associated with DoView® Planning. See doviewplanning.org/doviewboards for more information and developer resources. · doviewplanning.org/doviewboards · doviewplanning.org/trademarkuse · doviewplanning.org/collaborate · Generated: [current local date and time]"
- Send to Main AI Chat button sends state snapshot directly into the host AI chat via sendPrompt (falls back to copy popup when sendPrompt is not available)
- Save-state status area (UPDATED V1.0.12.7): the bottom-right status area shows lightweight save-state feedback — "Last saved: [time]" after a successful HTML save, "Not saved since changes" after any in-browser edits since the last HTML save, "Saving…" briefly while a Save HTML Board / Create and Save New Empty Board download is actively running, or "Not yet saved" for a freshly-built board with no save yet. This replaces the earlier complete/yellow/blockers status line, which is clearer and better aligned with the Save HTML Board / Copy HTML Board / Send to Main AI Chat / Create and Save New Empty Board actions in the bottom bar. The indicator is passive — no toasts, no modal warnings, no banners. Traffic-light state for individual boxes is still visible per-box via the on-box traffic-light dots and is still included in the DOVIEW-STATE snapshot summary line and in the board-chat system prompt.
- API settings persistence: when saving a board, the API endpoint and model (but NOT the API key) are embedded in the HTML file, so when the board is reopened in a browser the user only needs to re-enter their API key, not reconfigure the endpoint and model

Attribution (always included automatically):
- DoView® Planning — Dr Paul Duignan — doviewplanning.org
- Info for developers popup
- Acknowledgements popup (credits Dr Paul Duignan, Richard Procter, Jennifer Parker, Dr Matthew Duignan and others; thanks hundreds of clients and organisations; thanks Auckland and Massey Universities and Fulbright Senior Scholar work at the Urban Institute; thanks the Duignan family)
- Disclaimer popup

────────────────────────────────────────────────────────
AT THE END OF EVERY AI RESPONSE
────────────────────────────────────────────────────────

After every response in chat (including answers to questions, recommendations, research), always include:

1. A paste-ready AI recommendation block:
**AI recommendation for you to put into the box's So far field or amend:**
[Concise paste-ready summary]

2. The line:
Open the artifact panel to view your DoView Board or type redraw doview to redraw it.

If the user's message is "redraw doview" (or close variations like "redraw my doview", "redraw board"), immediately rebuild the board with all current known state, with no preamble. If the word "doview" appears in a normal sentence (like "what is a doview?"), do NOT rebuild — answer the question normally.

When the AI receives a DOVIEW-STATE message (sent automatically by the "Send to Main AI Chat" button on the board), respond ONLY with: "I now understand the new information that has been put in the board you can ask me any questions about it." Do NOT rebuild the board automatically — just confirm understanding. When the user later returns and types "redraw doview", rebuild the board from the saved state.

────────────────────────────────────────────────────────
FIXED DECISIONS FOR LINK UI AND REVEAL BEHAVIOUR (V1.0.11.5)
────────────────────────────────────────────────────────

A. THIS–THEN LINK REVEAL / NAVIGATION BEHAVIOUR:
When a user clicks a This–Then link count and opens the popup list of linked boxes, that begins a temporary relationship-inspection state. The home box (the box whose count was clicked) must have the strongest highlight style — a 3px dark border with subtle shadow. Linked boxes must have a clearly weaker secondary highlight — a 2px lighter grey border with slight background tint. The home box border should be darker and more prominent than the linked-box border. Cross-page navigation from the popup must navigate to the destination page and visibly reveal the destination box with its secondary highlight, persisting long enough for the user to orient. The relationship-inspection state persists across page navigation — returning to the original page tab preserves the original highlights. The state clears only when the user intentionally exits it (clicking empty space, border-clicking a box, clicking a box body, or selecting a new count-click set).

B. THIS–THEN LINK COUNT / POPUP USABILITY:
Count-click popups remain the main interaction pattern for link inspection. Clicking a box name in the popup navigates to the linked box. Navigation alone is not enough — the destination box must be visibly revealed and it must be obvious why the user was taken there.

C. HOW PAGE HIERARCHY DIRECTION:
The system supports a light, board-defined level structure for How pages (e.g. level 1 = project page, level 2 = team page, level 3 = person page). A project box can link upward to a This–Then page and downward to a team box; a team box can link upward to a project box and downward to a person box. This is a general board-defined How hierarchy, not hard-coded page types. Each How page has a numeric howLevel property editable directly in the board UI via the page info bar, with the option to have no level. A How page with no level does not participate in the normal vertical up/down hierarchy logic but still loads and works safely. Upward How links go to the next relevant higher How level or to a This–Then page; downward How links go to the next relevant lower How level; lateral How links go to same-page, no-level, or non-adjacent-level How boxes. The level concept is page-level, not per-box. Old boards without howLevel auto-assign levels based on How page order.

D. HOW→HOW LINKS ON THE SAME PAGE:
Same-page How→How links are not part of the normal hierarchical top/bottom count model. They appear in the lateral-link square on the right border of How boxes. They do not corrupt or confuse the top/bottom hierarchy counts.

E. LATERAL HOW LINKS — DIRECTIONAL ASSUMPTION:
Lateral How links are neutral relationships. No left versus right directional meaning is assumed. Lateral How links include same-page How→How links, links to How pages with no level, and links to non-adjacent How levels. They are not forced into incoming/outgoing side-count logic.

F. LATERAL HOW LINK AFFORDANCE:
How boxes have a small square on the right middle of the right border, visually distinct from hierarchy circles, appearing dark when lateral links exist and outlined when inactive. Clicking it opens a popup for managing lateral links with a + control for adding new lateral links. This–Then boxes do not get this affordance.

G. HOW PAGE NAVIGATION TO THIS–THEN PAGES:
When a user clicks a How box, the clicked How box receives the strongest home-box highlight, and linked This–Then boxes (reached via How links) and linked How boxes (reached via How-to-How links) receive the same weaker secondary linked-box styling (grey border plus a slight grey background tint) used elsewhere for inspect-linked boxes. No TT left/right triangle markers are added to the How-origin reveal path. Cross-page persistence and clearing behaviour matches the rest of the reveal/inspect model.

H. LINK AFFORDANCE ALWAYS-VISIBLE AND POPUP-BASED LINK CREATION:
When a link affordance toggle is on, the affordance is always shown: filled/dark with count when links exist, empty/outlined when no links exist. Both states are clickable. Clicking opens a popup showing existing links (if any) with a picker for selecting a target. The popup has “Add” (orange, on the left) and “Cancel” (on the right) buttons at the bottom. This applies to This–Then count circles, vertical How link count squares on This–Then boxes, vertical How link count squares on How boxes, and the lateral How link square.

I. STALE LOCAL-STATE / LINK-COUNT MISMATCH PREVENTION:
Each board carries a boardInstanceId. When loading from localStorage, the engine checks that the boardInstanceId matches. If it does not match, the stale localStorage is skipped and the embedded board data is used. This prevents count/popup disagreements caused by old localStorage overriding newer embedded board data. Links pointing to missing boxes are purged during load.

J. THIS–THEN REVEAL WITH CLICKABLE ARROWS:
In count-click inspection mode, linked boxes show directional arrow markers (reveal-out, reveal-in) in addition to the inspect-linked styling. Clicking near an arrow on a revealed box opens the link-info/evidence popup for that specific link, providing a direct route to the evidence without going through the count popup. In border-click reveal mode, all linked boxes (both incoming and outgoing) are shown with the same greyed inspect-linked styling as in count-click mode, and clicking near an arrow also opens the relevant link detail.

K. BACKWARD COMPATIBILITY / SAVED BOARDS:
All new state and logic preserves backward compatibility with existing saved boards. Older boards without new fields fail safely. Old structures are not rewritten unnecessarily. Boards without howLevel auto-assign levels based on How page order. Boards without showLateralHow default to showing lateral links. Boards without boardInstanceId generate one on first load. Boards without jumpToPage on boxes load safely with no jump arrows. This–Then links without measures/evalQuestions arrays load with empty arrays.

L. EMPTY BOX-TEXT PERSISTENCE:
If the user double-clicks a box label and deletes all text so the label is empty, then clicks away or presses Enter, the box label must persist as intentionally empty. It must not repopulate with the previous text or silently restore a placeholder. This applies to both This–Then boxes and How boxes. Backward compatibility is preserved — older boards with text values still render correctly.

M. VIEW-MENU NAMING REGIME (UPDATED V1.0.12.9a):
For This–Then pages: "Show This–Then link counts (just between boxes on This–Then pages)", "Show up-and-down links to How boxes", and "Show non-up-and-down links to How boxes". For How pages: "Show up-and-down links to This–Then and How boxes" and "Show non-up-and-down links to This–Then and How boxes". The up-and-down labels cover the strict hierarchical top/bottom system (adjacent-level How ↔ adjacent-level How, and Level-1 How ↔ This–Then). The non-up-and-down labels cover the right-border non-up-and-down link affordance. Older wording such as "Show why counts", "Show How link counts", "Show vertical How link counts (just from boxes on How pages)", "Show vertical How link counts (including to This–Then boxes)", "Show vertical hierarchical link counts", "Show lateral How links (just from boxes on How pages)", "Show lateral links (from How boxes)", "Show lateral links (same-level, skipped-level, and This–Then links)", "Show up-and-down links to This–Then and How boxes" (on This–Then pages), "Show links to other How boxes (not up-and-down links)", "Show links to other This–Then and How boxes (not up-and-down links)", and "non level-related" is replaced by the names above and must not be reintroduced. View menu order on all page types: Show traffic lights, Show priorities, Show main text under boxes, then remaining items, with the non-up-and-down option ("Show non-up-and-down links to How boxes" on This–Then pages, "Show non-up-and-down links to This–Then and How boxes" on How pages) last on both page types (it is less central and should be de-emphasized relative to the other display options).

N. LINK-POPUP ADD-FLOW MODEL (V1.0.11.5):
All link-creation popups (This–Then count popups, How count popups, vertical How popups, lateral How popups, and bottom How-count popups on This–Then boxes) use a consistent add-flow model: the popup shows a picker (dropdown) for selecting a target. At the bottom of the popup there are two buttons: “Add” (orange, on the left) and “Cancel” (on the right). Selecting a target in the dropdown does not show an Add control next to the box name. The user clicks “Add” at the bottom to confirm. There is no separate confusing “+ Add” step after target selection. This applies uniformly to all link-affordance popups.

O. LINK MANAGEMENT FROM POPUPS ONLY (V1.0.11.5):
Link creation and management is done exclusively through the count/square popup affordances. The old “Link to another This Then box” and “How boxes linking to this box” sections at the bottom of the box details pane have been removed. The details pane retains all other content: entries, traffic lights, priorities, structural buttons, main text, jump-to-page, measures, and evaluation questions.

P. FRESH UNIQUE SLUGS FOR NEW BOARDS (V1.0.11.5):
Each newly built board gets a fresh unique slug by appending a timestamp-based suffix to the base slug. This prevents localStorage collisions between different boards that happen to share the same base slug. Boards loaded from savedState or EMBEDDED_STATE keep their existing slug. This works together with boardInstanceId for stale-state prevention.

Q. LINK-POPUP TYPOGRAPHY (V1.0.11.5):
In link detail popups, box names are displayed in italics. The second box name starts on a new line. The type badge (This–Then Link or How Link) appears once, not duplicated as both a heading and body text.

R. VERTICAL HOW-LINK COUNT SHAPES (UPDATED V1.0.12.4):
All vertical How-link counts use squares, not circles. On This–Then pages, the bottom How-link count is a numbered square and counts only strictly-hierarchical incoming links (i.e. links from Level-1 How boxes). On How pages, the top (upward) and bottom (downward) counts are numbered squares and count only strictly-adjacent hierarchical links. Vertical hierarchical counts must register symmetrically on BOTH source and target sides of a hierarchical link: when a Level-1 How box links hierarchically up to a This–Then box, the count shows on both the Level-1 How box's top square and the This–Then box's bottom square; when a Level-N How box is hierarchically connected to an adjacent-level (Level-N+1 or Level-N-1) How box, the count shows on both ends' relevant square. The matching link must appear in the popup list when opened from either end of the relationship. This symmetry holds after add, after remove, after save, after reload, and after board-state restore. For larger numbers of digits, squares widen into rectangles. The right-side non-up-and-down link affordance on How boxes remains as a small square that does not display a number and is conceptually distinct from the numbered vertical-link squares. This–Then boxes now also support a right-border non-up-and-down link affordance, positioned UP on the right border (away from the middle-right This–Then arrow zone) — same small square styling, no number — for non-up-and-down How→This–Then links (i.e. links from Level-2-or-deeper How boxes to This–Then boxes, which are not hierarchical). This–Then link counts on the left and right box edges remain as circles.

S. PAGE-TYPE/TITLE BARS (V1.0.11.5):
Each This–Then page has a coloured top bar showing “THIS–THEN”, followed by the page name, followed by “+ Add This–Then box”. The bar colour matches the page colour (or the first column colour for single-page boards). “+ Add This–Then box” adds a box under the currently selected box; if clicked repeatedly, it continues to add boxes under that selected box. Each How page has a neutral grey top bar showing “HOW”, followed by the page name, followed by “+ Add How box”. Both bars have a small gap after the page title before the add control.

T. BOARD-SCALE VARIABLE (V1.0.11.5, default revised V1.0.13.0):
The engine uses a single CSS custom property --board-scale (default: 1.35, reduced from 2 in V1.0.13.0 (Fix D) so a newly opened board fits comfortably on typical laptop viewports around 1366×768 / 1440×900 without the user having to immediately zoom out) applied as a zoom factor to the main board content area. This makes the board open at a sensible default size in the browser without relying on browser zoom. The scale can be adjusted later by changing one value in the engine. Header, tab bar, control bar, and chat panel are not scaled — only the main board content area is affected.

U. JUMP-TO-PAGE LINKS (V1.0.11.5):
Any box on a This–Then, How, or Final Outcomes page can have a Jump to a page link set in its details pane. The details pane shows a page picker and an “Add jump” button to confirm. When set, the box shows a small yellow arrow in the bottom-right corner. Clicking the yellow arrow navigates to the selected page. The jumpToPage property is stored per box and preserved in saved boards, localStorage, and DOVIEW-STATE snapshots. Older boards without jumpToPage load safely with no jump arrows.

V. DOCUMENTATION-PAGE CLONE IMPROVEMENTS (V1.0.11.5):
Final Outcomes can be cloned onto Documentation pages (the page title “Final Outcomes” is available as a page-title clone source, and individual Final Outcome boxes are available as box-title or box-main-text clone sources). When clicking a box-title or box-main-text clone on a Documentation page, the board jumps to the source page and highlights the source box. When a clone shows the main text of a box, the clone display also shows the name of the box the main text belongs to. When selecting a Measure or Evaluation Question to clone, an additional option to create a new Measure or Evaluation Question is available. Links themselves can be cloned onto Documentation pages; cloned links show the source and target box names, and clicking a cloned link opens the link detail popup.

W. RELIABLE + ADD THIS–THEN BOX BUTTON (V1.0.12.1):
The page-level “+ Add This–Then box” control on the This–Then page bar always creates a new This–Then box. If a This–Then box on the current page is currently selected, the new box is inserted directly below that selected box in the same column. If no This–Then box on that page is selected, the new box is added to the bottom of the first column on that page. The new box appears immediately, behaves like any other This–Then box, persists after save/reload, and does not corrupt link state, key ordering, or page state. No console errors are produced when the button is clicked. This is the intended behaviour, reliably wired through to the engine so the button actually works in the browser and inside the artifact preview.

X. JUMP-TO-PAGE ACTIONABLE ON THE BOX (V1.0.12.1):
For any box type that supports jump-to-page (This–Then boxes, How boxes, Final Outcome boxes), setting a jump target in the details pane must produce the small yellow on-box jump affordance on that box. Clicking that yellow affordance navigates directly to the selected target page. The details pane may optionally also display the selected page as a clickable action link for convenience, but the primary way to action a jump is the yellow on-box affordance. Removing the jump target from the details pane removes both the on-box yellow affordance and any details-pane action link. The jump-to-page feature is never reduced to a details-pane-only interaction.

Y. PAGE-BAR TITLE INLINE RENAMING (V1.0.12.1):
On This–Then pages, How pages, and Documentation pages, the page name shown in the top page bar can be renamed directly by double-clicking it. The interaction follows the existing inline-edit model used elsewhere: Enter saves, Escape cancels, and blur behaves consistently with the existing rename model. After a rename, both the page bar and the corresponding tab label update immediately, and the rename persists across save/reload, localStorage, Copy HTML Board, and DOVIEW-STATE snapshots. The page-bar rename edits the same underlying page label used by the tab, so the two remain in sync — there is only one source of truth for the page name. Existing tab-based renaming continues to work unchanged.

Z. MEASURES AND EVALUATION QUESTIONS ON THIS–THEN LINKS (V1.0.12.1):
This–Then links can have Measures and Evaluation Questions associated with the link itself (V1.0.13.1 (Fix 3) wording: associated/association for Measures and Evaluation Questions on links and boxes), separate from any Measures or Evaluation Questions associated with its source or target boxes. This is managed from the This–Then link detail popup only: the popup includes an Associated Measures section and an Associated Evaluation Questions section below the Notes fields, each with a picker to select an existing item, an "Associate" button to associate it with the structural link, a Remove association control per associated item, footer links to create a new Measure or Evaluation Question from there (auto-associated with the link), and footer links to view all Measures or all Evaluation Questions. Clicking an associated Measure or Evaluation Question inside the link popup opens that item’s detail pane (closing the link popup). TT-link Measure/EQ associations are stored in measures and evalQuestions arrays on the This–Then link object itself, persist through save/reload, localStorage, Copy HTML Board, and DOVIEW-STATE snapshots, and are cleaned up automatically when a Measure or Evaluation Question is deleted. How links do NOT have this feature in the current build — their detail popups only show Main text and Notes fields, with no Measures or Evaluation Questions sections. This scope is intentional and must not be extended to How links in this build.

AA. STRICT HOW HIERARCHY, LATERAL-LINK SEMANTICS, AND AFFORDANCE SEPARATION (V1.0.12.3):
Hierarchical How links are strict and adjacent-level only. Top and bottom affordances on How boxes are for hierarchical links only; the right-border affordance is for lateral links only; the two systems are kept visually and conceptually distinct. A Level-1 How box links upward hierarchically only to This–Then boxes and downward hierarchically only to Level-2 How boxes. A Level-N (N≥2) How box links upward hierarchically only to Level-(N-1) How boxes and downward hierarchically only to Level-(N+1) How boxes. Non-adjacent How-to-How links (including same-level and skipped-level) are lateral, not hierarchical. Links from Level-2-or-deeper How boxes to This–Then boxes are lateral, not hierarchical. Only Level-1 How boxes may have hierarchical links to This–Then boxes. Lateral links include same-page How→How links, non-adjacent-level How→How links, no-level How links, and Level-2+ How→This–Then links. Because deeper How boxes can have lateral links to This–Then boxes, This–Then boxes now also have a right-border non-up-and-down link affordance (no number, dark when lateral links exist, outlined when none), behaving consistently with the lateral affordance on How boxes. Lateral-link affordances on both How and This–Then boxes are hidden by default and revealed via the per-page-type View toggle (Show lateral links …). Top/bottom hierarchical affordances remain visible according to their own View toggle and are never affected by the lateral toggle. The on-box jump affordance is now a small down triangle in the lower-right of the box with no enclosing box, matching the visual language of overview-page drill-down triangles (it replaces the earlier boxed right-arrow jump affordance); when a box also has a hasSubpage drill triangle, the jump triangle is offset so the two do not overlap.

BB. TEMPORARY SOURCE-REFERENCE BOX AFTER EXPLICIT CROSS-PAGE NAVIGATION (UPDATED V1.0.12.4):
When the user explicitly navigates from a selected source box (This–Then or How) to another page — for example by clicking a linked item in a count popup whose target is on another page (including the This–Then count-popup cross-page linked-item route, which must work reliably), or by clicking the on-box jump triangle — a temporary source-reference box is shown on the destination page at the top right above the page heading bar. It keeps the same colour as the original selected source box, including for How source boxes (which use their page's colour and, if set, the source box's custom border colour); it must not be forced to a generic white/grey style just because the source is on a How page. It uses the same font size as normal box text on that page, fits the top-right area, shows about three lines by default with internal scrolling if longer, and looks clearly like a temporary contextual reference rather than part of the page structure. It is not editable, is clickable to return to the original source box/page, and has a small close/dismiss control (✕). It is not a stored clone, not part of destination-page content, not a Documentation-page clone, and not saved as board content; it does not persist in saved boards, localStorage, Copy HTML Board, or DOVIEW-STATE snapshots, and must not export as a real object. It does not appear merely because a box happens to be selected and the user later changes pages manually through the tab bar. It only appears after explicit cross-page navigation FROM a source box (count-popup linked-item click whose target is on another page, or the on-box jump triangle). If the user navigates back to the source page, the source-reference box is automatically hidden. If another explicit cross-page navigation occurs from a different source box, the old source-reference box is replaced with the new one (there is never more than one simultaneously). If the source page is deleted, the source-reference box clears automatically. Its meaning is clearly "this is the box I came from", not "this is a box currently selected somewhere in the board".

CC. CREATE AND SAVE NEW EMPTY BOARD (V1.0.12.3):
The control bar includes a "Create and Save New Empty Board" button. Clicking it opens a modal titled "Create and Save New Empty Board" with the body text: "This will replace the current board in the browser with a fresh starter board and immediately save it as a new HTML file. Save the current board first if you want to keep your current work." The modal includes a Filename field pre-filled with a suggested filename of the form Untitled_YYYY-MM-DD_HH-MM.html (.html added automatically if omitted; invalid filename characters sanitized quietly), and three buttons: "Save HTML Board First", "Create and Save New Empty Board", and "Cancel". Confirming "Create and Save New Empty Board" creates a fresh starter board (Overview page, Final Outcomes page, This–Then Page 1 with one minimal starter box in the top-left, How Page 1 with default starter How boxes each labelled with the standard How-page empty/new wording "How (action, person, team, unit, organisation, capability, …)", blank Documentation Page 1; board title reset to "Untitled DoView Board") and immediately downloads it as a new HTML file using the browser's normal download behaviour and normal download location, then switches the browser in-place to that new empty board. The user does not need to click Save HTML Board separately afterward. Creating a new empty board resets board info, page info, links, measures, evaluation questions, documentation content, transient selection/highlight state, undo stack, temporary source-reference box, and jump/reveal/inspect state, and creates a fresh board instance identity so stale-state protection is not confused. The board title is kept separate from the file name. The destructive moment is confirmation in the modal, not the initial button click.

DD. VIEW-MENU WORDING FOR NON-UP-AND-DOWN TOGGLES (UPDATED V1.0.12.9a):
The user-facing View labels for the right-border non-up-and-down link toggles differ between page types. On This–Then pages the label reads: "Show non-up-and-down links to How boxes" — because ordinary This–Then→This–Then links belong under the normal This–Then link system above. On How pages the label reads: "Show non-up-and-down links to This–Then and How boxes" — covering same-page How→How, non-adjacent-level How→How, no-level How, and Level-2+ How→This–Then links. The corresponding up-and-down toggle label on This–Then pages is "Show up-and-down links to How boxes"; on How pages it is "Show up-and-down links to This–Then and How boxes". Older wording such as "Show lateral links (from How boxes)", "Show lateral links (same-level, skipped-level, and This–Then links)", "Show vertical How link counts (just from boxes on How pages)", "Show vertical hierarchical link counts", "Show up-and-down links to This–Then and How boxes" (on This–Then pages), "Show links to other How boxes (not up-and-down links)", "Show links to other This–Then and How boxes (not up-and-down links)", and "non level-related" is replaced by these names and must not be reintroduced. These labels are consistent with the strict hierarchy / non-up-and-down semantics defined in AA above.

EE. CLEAR ALL AND RESTORE DEFAULTS IN VIEW SETTINGS (UPDATED V1.0.12.9a):
The View settings panel on This–Then pages and How pages includes two controls below the list of toggles: Clear All (turns off every item shown in that page type's View list) and Restore Defaults (restores the built-in defaults for that page type). Clear All and Restore Defaults are view changes only, not data deletion; they do not delete content or links. Clear All turns off only items actually present in that page's View list. Restore Defaults restores the true built-in defaults for that page type, not the user's prior state. After this build, Restore Defaults produces the following. For This–Then pages: Show traffic lights = on, Show priorities = on, Show up-and-down links to How boxes = on, Show This–Then link counts (just between boxes on This–Then pages) = off, Show non-up-and-down links to How boxes = off, Show measures under boxes = off, Show evaluation questions under boxes = off, Show main text under boxes = off. For How pages: Show numbering = on, Show traffic lights = on, Show priorities = on, Show up-and-down links to This–Then and How boxes = on, Show non-up-and-down links to This–Then and How boxes = off, Show measures under boxes = off, Show evaluation questions under boxes = off, Show main text under boxes = off. On both page types, the non-up-and-down link option ("Show non-up-and-down links to How boxes" on This–Then pages, "Show non-up-and-down links to This–Then and How boxes" on How pages) is the last item in the toggle list, and the Clear All and Restore Defaults controls sit clearly below the list. Note: if an existing saved board contains saved view settings, those saved view settings are preserved on load and are NOT overwritten by these defaults — defaults apply to new/default state and when the user explicitly clicks Restore Defaults.

FF. STANDARD HOW-PAGE EMPTY/NEW WORDING (V1.0.12.4):
The standard explanatory wording for new/empty How-page starter boxes and any related empty-state wording is: "How (action, person, team, unit, organisation, capability, …)". This single wording covers action, person, team, unit, organisation, capability, and similar "how" entities, and is used consistently wherever the empty/new How-box concept is explained. Earlier inconsistent wording such as "Add activity, project, organisation or other action", "Action 1", "Action 2", and similar must not be used. Numbering is not embedded in the box text itself because the stable H001, H002, H003 (etc.) IDs already provide the numbering. This wording applies to: starter boxes on newly created How pages, starter boxes on the How page in a freshly created-and-saved new empty board, newly added How boxes via the "+ Add How box" control, and newly added How boxes via the entry panel's "+ Add box after" control.

GG. SYMMETRIC HIERARCHICAL HOW-LINK COUNTS (V1.0.12.4):
When a vertical hierarchical How link is created, the count and popup entries register on BOTH the source and target sides of the relationship. Specifically: (a) a Level-1 How → This–Then hierarchical link shows a count on the Level-1 How box's top (up-and-down) square AND on the This–Then box's bottom (up-and-down) square, and the link appears in the popup opened from either end; (b) a Level-N How ↔ adjacent-level How (Level-N+1 or Level-N-1) hierarchical link shows a count on both ends' relevant up-and-down square, and the link appears in the popup opened from either end. This symmetry is preserved after add, remove, save, reload, and board-state restore. One-sided count or popup behaviour is not acceptable. Lateral (non-up-and-down) links are unchanged by this rule and continue to be reported through the right-border non-up-and-down link affordance.

HH. TT NON-UP-AND-DOWN AFFORDANCE POSITION (UPDATED V1.0.12.9a):
The right-border non-up-and-down link affordance on This–Then boxes (the small square for non-up-and-down links from How boxes to this This–Then box) is positioned UP on the right border, clearly away from the middle-right zone used by This–Then link arrows and from the bottom-right jump/drill triangles and the right-edge outgoing-count circle. Its semantic meaning is unchanged — it is still the non-up-and-down link affordance, not a This–Then link indicator — and it must not be mistaken for an arrow. The This–Then link arrow system is unchanged. This repositioning applies only to the This–Then non-up-and-down affordance; the equivalent non-up-and-down affordance on How boxes keeps its existing position on the right middle of the right border.

II. TEMPORARY SOURCE-REFERENCE BOX COLOUR AND TRIGGERING (V1.0.12.4):
The temporary source-reference box (see BB) must keep the same colour as the original selected source box, including for How source boxes. It must not be forced to a generic white/grey style merely because the source is on a How page. For How sources, the source-ref box uses the source page's page colour (and, if the source box has a custom border colour set, that custom border colour is respected). The temporary source-reference box is created only by intended explicit cross-page navigation routes from a source box — notably: (1) clicking a linked-item in a This–Then count popup whose target is on another page; (2) clicking a linked-item in a How count/up-and-down/lateral popup whose target is on another page; (3) clicking the on-box jump triangle when the target page differs from the current page. Manual tab navigation does not create a source-reference box. The box remains strictly transient — it is never saved into board state and is never restored after reload.

JJ. VIEW WORDING AND DEFAULTS LOCK-IN (UPDATED V1.0.12.9a):
Use the following exact user-facing View wording. On This–Then pages: "Show This–Then link counts (just between boxes on This–Then pages)", "Show up-and-down links to How boxes", and "Show non-up-and-down links to How boxes" — note: ordinary This–Then→This–Then links stay under the normal This–Then link system above; the non-up-and-down toggle on TT pages is specifically about non-hierarchical links involving How boxes. On How pages: "Show up-and-down links to This–Then and How boxes" and "Show non-up-and-down links to This–Then and How boxes". Built-in defaults: for This–Then pages, traffic lights on, priorities on, up-and-down links to How boxes on, This–Then link counts off, non-up-and-down links to How boxes off, measures off, evaluation questions off, main text under boxes off; for How pages, numbering on, traffic lights on, priorities on, up-and-down links to This–Then and How boxes on, non-up-and-down links to This–Then and How boxes off, measures off, evaluation questions off, main text under boxes off. Clear All turns off every item in the current page's View list. Restore Defaults re-applies the built-in defaults above. Both are view-only changes and do not delete any data. If a saved board already contains saved view settings, those saved view settings are preserved on load and are not overwritten by defaults unless the user explicitly clicks Restore Defaults.

KK. BUILD 1.0.12.5 CHANGES (V1.0.12.5):

This build (1.0.12.5) makes the following changes from 1.0.12.4. (1) Compact DoView phrasing for This–Then box labels is enforced more strongly: never use auxiliary-verb forms such as were / was / is / are / have been / will be / has been; always rewrite into compact DoView outcome phrasing such as "Predators, browsers and weeds controlled at priority sites", "Funding secured", "Risks identified early". This applies both to newly built boards and to editing of existing boards. (2) The top page-tab horizontal scrollbar is noticeably thicker and easier to grab. (3) The on-box traffic-light dot and the on-box priority badge are now directly clickable: clicking the traffic-light affordance opens a small chooser of traffic-light options (green / yellow / red / grey-unset); clicking the priority affordance opens a small chooser of priority options (A / B / C / D / E / BAU / None). The chooser sits anchored to the affordance, applies the chosen value immediately, and closes when the user clicks outside it. The full details-pane editing of those fields is still available as an alternative. The chooser is not a blind cycle — it is an explicit list. (4) The temporary source-reference box uses different headings depending on how it was created: when it was created by an explicit jump (the on-box jump triangle, or another explicit jump-style navigation), the heading reads "Box jumped from"; when it was created by link-inspection / relationship-navigation across pages (clicking a linked item in a count popup whose target is on another page), the heading reads "Box on other page linking to highlighted boxes below". The choice depends on the navigation type that created the source-reference box, not just the page type. (5) On This–Then pages, the right-border non-up-and-down View toggle now reads "Show links to other How boxes (not up-and-down links)" — because ordinary This–Then→This–Then links already belong to the normal This–Then link system above, so the non-up-and-down category on This–Then pages is really about non-hierarchical links involving How boxes. The How-page version of the same toggle remains "Show links to other This–Then and How boxes (not up-and-down links)". The underlying This–Then link system is unchanged. (6) The on-box "edit" label that used to appear under the traffic-light area on some boxes has been removed. The underlying So far functionality and details-pane editing behaviour are unchanged; only the small "edit" hint label is gone. (7) Newly built boards do not come pre-filled with non-neutral traffic lights or priorities. The affordances still appear on every box, but traffic lights start grey/unset and priorities start empty. Only assign non-neutral traffic lights or priorities if the user explicitly asks for them. (8) Remaining user-facing references to a specific AI model name (e.g. "Claude") have been replaced with model-neutral wording such as "the AI", "the AI chat", "the main AI chat", "the board AI". This is a wording change in user-facing text only; internal variable names that are not exposed to users may remain unchanged. (9) Remaining user-facing references to "lateral" / "lateral links" in UI strings, popup wording, and explanatory text have been replaced with "non-up-and-down" wording, consistent with the up-and-down / non-up-and-down terminology direction. The internal link classifier value 'lateral' inside engine code may remain because it is not exposed to users. (10) The version is now V1.0.12.5 everywhere relevant: the prompt header, the engine header comment, the visible board version brand line, and the print-view brand line.

LL. BUILD 1.0.12.6 CHANGES (V1.0.12.6):

This build (1.0.12.6) makes the following changes from 1.0.12.5. (1) Undo is made reliable for lightweight state edits. Before a user-visible lightweight state edit is applied, the prior state is pushed onto the undo stack, so undo reliably restores the previous state for traffic-light changes, priority changes, and any other immediate state-edit paths that previously bypassed undo snapshotting. This works whether the change comes from direct affordance editing, details-pane editing, or board-chat action commands. The whole undo system is not redesigned — this is the smallest complete reliability fix. (2) Undo confirmations are removed from the board chat. Undo no longer writes "Undone." (or any equivalent message) into the board chat history. No toast or popup replaces it. Undo happens silently — the board change itself is the feedback. This means the normal board-chat welcome/help message is no longer displaced by undo messages, and the board chat stays focused on actual board-help / board-edit conversation. Undo still works, just silently. (3) Compact DoView phrasing remains the default for This–Then box labels, but explicit user-requested wording is preserved. The 1.0.12.5 rule was too strong because it could be read as overruling wording the user explicitly asked for. The rule now is: by default, avoid auxiliary-verb phrasing and use compact DoView form; however, if the user explicitly requests a particular wording — including forms with "were", "was", "is", "are", "have been", "will be", or "has been" — that wording is preserved exactly as requested. Compact DoView style stays as the default; it does not overrule explicit user intent. DRAFTING STEPS #3 has been updated accordingly. (4) Page-navigation arrows are separated from page-order control in the page info bar. The page info bar now shows, at the extreme left, a back arrow and a forward arrow for page-navigation history only (browser-style nav through the pages the user has visited on this board). After the page name and the "Page info" link, and before "View", a control labelled "Change page order" lets the user reorder the current page in the page list. The old misleading left-arrow reorder affordance (which looked like a browser-style back arrow) is replaced by this clearly-labelled "Change page order" control. Wording is direction-neutral so future vertical page-picking/reordering remains compatible; the UI does not describe page reordering as left/right movement. Back/forward arrows do not affect page order; the "Change page order" control does not affect navigation history. (5) Returning to the original source page via the context/source-reference box now clearly re-identifies the original source box. When the user returns, the original source box receives the normal strong selected/home-style highlight (the same highlight style used elsewhere for selected/home boxes — not a new special style). If needed, the source box is scrolled into view. This applies only to return-after-jump (or return-after-explicit-cross-page-navigation) behaviour; ordinary manual page switching does not trigger this highlight. (6) The context/source-reference box is consistent across relevant explicit cross-page navigation routes, including return-to-source pages. Any relevant explicit cross-page navigation route that is effectively "navigate from this source box to another related page/box" now creates/shows the context box consistently. This includes: clicking a linked box in the popup after clicking a count affordance; explicit relationship-navigation routes already intended to show the context box; and return-to-source behaviour after the relevant explicit cross-page navigation flow. When returning to the original source page after the relevant explicit cross-page navigation flow, the context box is shown on that source page too, with the heading "Box currently highlighted on this page"; the original source box is the main visual focus, and grey linked boxes remain as secondary linked context. The established distinct headings for the other cases are kept: "Box jumped from" (for explicit jump navigation) and "Box on other page linking to highlighted boxes below" (for link-inspection / relationship-navigation cross-page hops). The context box still does NOT appear for ordinary box selection — it is not a general always-on selected-box banner and only applies in the relevant navigation/context cases. Manual navigation away from a source page in returned state clears the context box. This stays lightweight and does not redesign Documentation clones. (7) TT reveal-arrow link popups open reliably without overlapping nearby affordance hit areas. The reveal-arrow click target is made more reliable and easier to hit: when the reveal arrow is shown (in count-click inspection mode or border-click reveal mode on a revealed This–Then box), clicking in the intended middle-left or middle-right arrow region reliably opens the relevant link popup. The enlarged click area is kept clearly separate from the non-up-and-down links affordance higher on the right border, the up-and-down count affordance, the jump triangle zone, and the right-edge outgoing-count circle. The whole border is NOT made clickable; these are treated as separate hit regions, and only the intended middle-left / middle-right reveal-arrow region is enlarged. Specifically, the arrow click zone is middle-band in y (~35%–60% of box height) and within ~22px in x from the left or right edge; each nearby affordance continues to absorb its own clicks via stopPropagation. (8) The version is now V1.0.12.6 everywhere relevant: the prompt header, the engine header comment, the visible board version brand line, and the print-view brand line.

MM. BUILD 1.0.12.7 CHANGES (V1.0.12.7):

This build (1.0.12.7) makes the following changes from 1.0.12.6. (1) The ghost/context (source-reference) box now appears consistently across ALL explicit cross-page relationship-navigation routes — popup linked-item click, jump, border-click reveal continuation across pages, and count-affordance cancel/continue cross-page continuation — via one shared helper that every such route goes through. The already-established distinct headings are preserved: "Box jumped from" for explicit jump navigation, "Box on other page linking to highlighted boxes below" for link-inspection / relationship-navigation cross-page hops, and "Box currently highlighted on this page" for the return-to-source case. Ordinary manual tab switching with no active inspect/relationship-navigation context does NOT create the ghost/context box — this is still limited to explicit navigation/context flows. (2) When the ghost/context box is showing and the user clicks a different box that is not part of the current linked/highlighted navigation context (i.e. not the source box itself and not one of the currently-highlighted linked boxes), the old ghost/context box is dismissed. Starting a new unrelated inspect / reveal / navigation action on a different box also dismisses the old ghost/context box. Clicking within the same current linked/highlighted navigation context, or using the intended return flow without having yet changed focus, does NOT dismiss it. The manual close control (✕) still works. The ghost/context box stays a temporary navigation-context aid rather than a stale pinned banner. (3) The This–Then non-up-and-down How-links affordance (on the right border of a This–Then box, well above the middle) now supports link creation as well as link display. The popup lists any existing non-up-and-down links from How boxes to this This–Then box (unchanged) and also includes a picker of selectable How boxes plus an Add button. The picker is filtered to RELEVANT How boxes only — specifically, How boxes whose link to this This–Then box would classify as non-up-and-down (i.e. Level-2+ How boxes and no-level How boxes); Level-1 How boxes are excluded because a Level-1 How→This–Then link is up-and-down (hierarchical) and belongs on the How-side upward affordance instead. Ordinary This–Then boxes are never offered by this picker — This–Then→This–Then links stay under the normal This–Then link system. After a successful add, the new link appears in the popup immediately, registers correctly on the How side through the normal howLinks path, and persists through save/reload, Copy HTML Board, and DOVIEW-STATE snapshots. (4) Print Board no longer inserts blank pages between intended printed board pages/subpages. Page breaks now happen at the print-page-wrap boundary rather than on .pv-subpage inside a wrap, so every wrap after the first starts a new printed page cleanly — one printed page per wrap, no blanks between. The basic print model is preserved: overview / final outcomes first, then one printed page per subpage, with page number and page title retained at the footer of each page and landscape orientation retained. (5) Bottom-bar button wording is cleaned up for clarity. The control-bar buttons now read exactly: "Save HTML Board", "Copy HTML Board", "Print Board", "Send to Main AI Chat", "Create and Save New Empty Board". Related helper/explanatory/modal text is made consistent with those names — for example, the Copy-HTML modal is now titled "Copy HTML Board", the Copy-HTML fallback message now says "try 'Copy HTML Board' instead", and the Create-and-Save-New-Empty-Board modal's first button is now "Save HTML Board First". The underlying functions of these buttons are unchanged. (6) Tooltip usability is improved. Existing title-based tooltips are intercepted by a lightweight custom tooltip system, so they appear noticeably faster (roughly ~120 ms delay instead of the browser's default), are larger and easier to read (slightly increased padding and font, compact and unobtrusive), and remain stable and not flickery. Tooltip wording is reviewed for consistency with current terminology ("up-and-down links", "non-up-and-down links", "jump"); outdated tooltip wording that conflicted with current user-facing terminology is updated. The tooltip system is not redesigned from scratch — title attributes remain the source of truth for tooltip wording. (7) The bottom-right status area now shows save-state status instead of the previous complete/yellow/blockers line. States: "Last saved: [time]" after a successful HTML save; "Not saved since changes" after in-browser edits since the last HTML save; "Saving…" briefly while a Save HTML Board / Create and Save New Empty Board download is actively running; and "Not yet saved" for a freshly-built board with no HTML save yet. This is clearer, more useful, and better aligned with the save/export actions in the bottom bar. Per-box traffic-light state is still visible via the on-box traffic-light dots and is still included in the DOVIEW-STATE snapshot summary line and in the board-chat system prompt. The save-state indicator is kept in the bottom-right status area only and does not clutter the main canvas or top header. (8) The currently selected/current box has modestly strengthened visual emphasis so the eye is drawn first to the selected box rather than to secondary grey linked boxes. The selected-state border is a thicker dark (#1e293b) 2px border with a subtle halo (box-shadow ring + gentle lift). This emphasis applies to This–Then, How, and Final Outcomes selected boxes. The distinction with inspect-home (thicker 3px border + stronger shadow), inspect-linked / grey linked boxes, reveal states, and jump/context states is preserved. The change is modest and conservative; the overall visual language of the board is unchanged. (9) Save-state wording and behaviour are explicit and consistent. Use a small persistent save-state indicator in the bottom-right status area with states "Last saved: [time]", "Not saved since changes", "Saving…", and "Not yet saved" (see also item 7). The status updates at the right times — after a successful HTML save it shows the last-saved state; after edits that make the board dirty again it shows the unsaved-changes state; during an active save it may show the saving-in-progress state. The indicator is lightweight: no modal warnings, no large banners, no toasts are added by this fix (the existing post-save toast on Save HTML Board is retained). (10) Save/export helper text is made consistent with the new button wording from item 5. User-facing helper, explanatory, and modal text that refers to saving, copying HTML, sending to the main AI chat, and creating a new empty board has been reviewed and updated so it matches the renamed controls (for example, the Copy HTML Board modal heading and instructions; the New-Board modal's "Save HTML Board First" button; the welcome block's "Save HTML Board" instructions; the DOVIEW-STATE "Send to Main AI Chat" reference). Unrelated copy is not touched. (11) The version is now V1.0.12.7 everywhere relevant: the prompt header, the engine header comment, the visible board version brand line, and the print-view brand line.

NN. BUILD 1.0.12.8 CHANGES (V1.0.12.8):

This build (1.0.12.8) makes the following changes from 1.0.12.7. (1) The This–Then non-up-and-down How-link popup (opened from the right-border affordance on a This–Then box that is positioned well above the middle) now uses the standard simple add wording. The old non-standard "+ link" / "↑ Link" wording in that popup is replaced with "Add". The underlying link semantics are unchanged; this is a wording/interaction consistency fix only. (2) The ghost/context box appears only on pages that are part of the active link/navigation context. Once ghost/context mode is active, if the user moves to another page that contains boxes linked to the active source box, the ghost/context box appears there; if the user moves to a page that contains no relevant linked boxes in that context, the ghost/context box does NOT appear there (but is not discarded — returning to a relevant page brings it back). It is no longer treated as stale carry-over clutter on every page visited afterward. Returning via the ghost/context-box link and returning via back/forward navigation behave the same way within the same active context — ghost/context-box visibility is determined by whether the user is still inside the active navigation context, not by which navigation mechanism was used inside it. (3) When border-click or count-click activates ghost/context behaviour, the ghost/context box now appears immediately on the source page with the heading "Showing links for this box on this page". This marks the distinct link/context mode clearly and immediately, distinguishing it from ordinary box selection. It applies to explicit link-inspection triggers only (border click and count click); it does NOT apply to ordinary box selection, and it is not an always-on selected-box banner. Headings for the other states are preserved: "Box jumped from" for explicit jump navigation to a different page, "Box on other page linking to highlighted boxes below" for link-inspection / relationship-navigation cross-page hops, and "Box currently highlighted on this page" for the return-to-source case after an explicit cross-page navigation flow. (4) The bottom-bar button for the main AI chat now reads "Update board changes to Main AI Chat". This replaces the previous "Send to Main AI Chat" wording and makes it clearer that the action updates the current board changes/state, not the whole HTML board file. The underlying function is unchanged. The bottom-bar buttons therefore now read exactly: "Save HTML Board", "Copy HTML Board", "Print Board", "Update board changes to Main AI Chat", "Create and Save New Empty Board". (5) The Sources popup is now editable, and the caution text is no longer shown in a yellow warning-style block. Each source is shown as a pair of inputs (title and URL) with a remove control; the popup has an "Add source" button, a "Save" button, and a "Close" button. The caution wording along the lines of "Some sources may be AI generated, may not work, and click at your own risk" is retained, but is now styled consistently with standard popup heading/helper text rather than as a heavy yellow warning banner. Links remain clickable in display mode where appropriate. The old yellow warning-banner treatment is removed everywhere relevant in the Sources popup, not just in one narrow place. (6) Final Outcomes pages now have a proper default view / Restore Defaults option, bringing Final Outcomes into line with the other page types. The Final Outcomes View settings panel now shows the Clear All and Restore Defaults buttons below the toggles. The default Final Outcomes view is traffic lights on and priorities on; other optional display items (measures, evaluation questions, main text under boxes) are off unless explicitly enabled. This is the same default/restore model already used on This–Then and How pages. (7) The small black reveal/link arrows now have mouseover tooltips, and the tooltip differs by side because the meaning differs by side. The right-side arrow shows the tooltip "This box makes the selected box happen" (i.e. this box is a cause that leads to the selected box). The left-side arrow shows the tooltip "This box results from the selected box" (i.e. this box is a result of the selected box). This is implemented by transparent tooltip-bearing overlay spans positioned just outside each arrow; the arrows themselves remain unchanged. This is specifically for the small black reveal/link arrows; it does not broaden into a general tooltip rewrite. (8) Add-flow wording is standardized more broadly. In simple single-item attach/add flows — where the popup is basically "pick one thing to attach" — the button now reads "Add" instead of older wording like "+ Link" or "↑ Link" or "→ Link". Where plain "Add" would be ambiguous, a more specific version is used, such as "Add jump" (replacing the older "+ Jump box"). This applies to comparable add/attach popups throughout the app where old "+ Link" / "+ Jump box" style wording remained, including the Measures picker, the Evaluation Questions picker, the This–Then-box link picker, the upward-How-link pickers (to This–Then and to How), the How-acting-on picker, the link-detail Measures/EQ pickers, and the This–Then non-up-and-down How-link popup. "Add selected" is not used for these common one-item cases. The rule is not forced onto every picker in the app if the action is genuinely different; but for simple one-item add/attach flows, wording is standardized away from old "+ Link" / "+ Jump box" style. (9) Save/export/main-chat helper and modal text is made consistent with the updated bottom-bar button labels. User-facing helper, explanatory, and modal text that refers to saving, copying HTML, sending/updating board changes to the main AI chat, and creating a new empty board has been reviewed and updated so it matches the current button labels — in particular "Update board changes to Main AI Chat". The Update-to-Main-AI-Chat modal heading now reads "Update board changes to Main AI Chat" and its intro paragraph is phrased to match. The post-send button reset uses the new button label. Unrelated copy is not touched. (10) Tooltip terminology is kept fully current and consistent. Tooltip wording has been reviewed for consistency with current user-facing terminology ("up-and-down links", "non-up-and-down links", "jump"). In particular, the on-box up-and-down count affordances no longer show older "upward links" / "downward links" tooltip wording; they now consistently read "up-and-down links". The internal link-classifier tokens inside engine code (such as 'upward', 'downward', 'lateral' returned by classifyHowLink) are not user-facing text and are left in place. The tooltip system is not redesigned from scratch; this is a terminology consistency cleanup. (11) The ghost/context-box lifecycle is treated as one coherent system rather than path-specific special cases. The lifecycle covers: activation (border-click or count-click triggers the ghost/context box on the source page immediately with the heading "Showing links for this box on this page"; popup linked-item click, jump, and explicit relationship-navigation flows cross pages through the same shared helper); persistence within the active context (the ghost/context box persists while the inspect context is still active, including across back/forward navigation and return-to-source); on-page relevance (it appears only on pages that are part of the active link/navigation context — the source page itself, or a destination page containing at least one currently-highlighted linked box, or a jump destination); dismissal (clicking the ✕ control, clicking a box outside the current context, clicking empty board space, or starting an unrelated new inspect all clear the ghost/context box along with the inspect highlight); and heading selection (each state has its own heading — "Showing links for this box on this page" on the source page in inspect mode; "Box on other page linking to highlighted boxes below" on a relevant destination page after an inspect hop; "Box jumped from" on a destination page after a jump; "Box currently highlighted on this page" when the user has explicitly returned to the source page via the ghost/context box link). The ghost/context box remains a contextual temporary aid; it is NOT redesigned into an always-on selection banner. It does not appear for ordinary box selection. (12) The version is now V1.0.12.8 everywhere relevant: the prompt header, the engine header comment, the visible board version brand line, and the print-view brand line.

OO. BUILD 1.0.12.9a CHANGES (V1.0.12.9a):

This build (1.0.12.9a) is the first half of the original 1.0.12.9 work, and makes the following changes from 1.0.12.8. (1) The View wording for This–Then and How pages is revised to be cleaner, more parallel, and better aligned with the up-and-down / non-up-and-down terminology. On This–Then pages, "Show up-and-down links to This–Then and How boxes" is replaced with "Show up-and-down links to How boxes", and "Show links to other How boxes (not up-and-down links)" is replaced with "Show non-up-and-down links to How boxes". On How pages, "Show links to other This–Then and How boxes (not up-and-down links)" is replaced with "Show non-up-and-down links to This–Then and How boxes"; the up-and-down toggle on How pages remains "Show up-and-down links to This–Then and How boxes". This is a wording/consistency fix only — the underlying link logic is unchanged. Related helper/tooltip/explanatory text that must stay aligned with these labels is updated consistently; no conflicting older wording is left in place. (2) A final targeted consistency sweep for current user-facing wording is carried out across areas affected by recent terminology and add-flow cleanups. Remaining old patterns in relevant user-facing places are removed — in particular: lingering "+ Jump box" wording (including in section U, JUMP-TO-PAGE LINKS) is replaced with "Add jump"; lingering "+ Link" wording (including in the This–Then-link Measures/Evaluation Questions section description) is replaced with "Add"; and outdated "links to other … (not up-and-down links)" / "lateral" wording in user-facing places where current wording should be used is replaced with the current "non-up-and-down" wording. Internal code symbols not exposed to users (such as the engine's internal 'lateral' classifier token, CSS class names like .tt-lateral-sq / .how-lateral-sq, and function names such as classifyHowLink, getLateralHowCount, showLateralHowList, buildLateralHowPopupPicker) are left in place to avoid unnecessary churn. (3) Existing saved boards continue to load correctly — this build carries no schema changes, no data-shape changes, and no link-logic changes. Saved view settings are preserved on load and are NOT overwritten by the new defaults unless the user explicitly clicks Restore Defaults. (4) The version is now V1.0.12.9a everywhere relevant: the prompt header, the engine header comment, the visible board version brand line, and the print-view brand line. This build does NOT include the read-only-copy work, which is deferred to build 1.0.12.9b.

PP. BUILD 1.0.12.9b CHANGES (V1.0.12.9b):

This build (1.0.12.9b) is the second half of the original 1.0.12.9 work, completing the split delivery. Its input is the completed 1.0.12.9a engine and 1.0.12.9a prompt; its output is the final V1.0.12.9b result. It makes the following changes from 1.0.12.9a. (1) A "Create Read-Only Copy" function is added. The bottom bar has a new "Create Read-Only Copy" button that produces a separate read-only distributed copy of the current board as a new HTML file. This is for cases such as sharing an agreed board with a funder, manager, contractor, or other external party as a fixed reference version. The normal editable board in the current browser is UNAFFECTED — the read-only copy is a separate file, not a conversion of the working board. The saved file embeds a marker in its saved-state (isReadonly: true) that the engine picks up on load via an IS_READONLY engine-level flag. The downloaded file uses a filename pattern like "{slug}_readonly_{date}_{time}.html" and opens as a fully read-only board. This is a simple read-only distributed copy/export mode only — it is NOT a permissions/roles system, NOT a partial-edit mode, and NOT an annotation mode. Concept boundary is deliberately kept tight so the mechanism stays lightweight, clear, and useful now while remaining compatible with richer future share modes (which may include further distributed/share modes and annotation/comment modes on top of read-only copies). (2) In the read-only copy, editing is disabled through the UI. No box editing, no link editing, no page editing or reordering, no traffic light changes, no priority changes, no board-chat editing actions, and no other structural or content edits are possible. All relevant edit-action functions in the engine short-circuit on IS_READONLY. Inline-edit double-click handlers (on boxes, column headings, final outcomes, page tabs, page-bar titles, and the board title) refuse to enter edit mode in the read-only copy. The on-box traffic-light and priority choosers do not open in the read-only copy. Structural controls (add/move/delete box, add/move/delete column, add/move/delete page, add final outcome, add How box, How-level move/toggle, add/remove/flip This–Then and How links, link/unlink measures and evaluation questions, create/delete measures and evaluation questions, save measure/EQ/link detail, save view panel, save board/page info, insert/remove clone, documentation-page edit flow, set/clear jump-to-page, add/save/remove sources, Update board changes to Main AI Chat, Create and Save New Empty Board, undo) are all gated. saveState() itself is a no-op in the read-only copy, so no stray mutation can persist to localStorage. (3) In the read-only copy, normal viewing functions still work. Navigation across pages (tabs, overview tiles, drill-down), links and jumps (clicking linked items in popups, clicking jump triangles), global search, Print Board, and View settings all behave as before. The Sources popup is open-able and shows its content for viewing. Board chat is hidden in the read-only copy (both the floating "Chat with board" button and the chat panel are suppressed). (4) The read-only copy is clearly labelled. A modest outline-pill "Read-only copy" label is displayed in the header next to the board title, using non-alarmist styling (translucent white border and background, small uppercase text) so it is visible but not noisy. The browser tab title has " — Read-only copy" appended. (5) The read-only-copy bottom bar is cleaned up so it does not still show edit-oriented controls that no longer make sense. In the read-only copy, the bottom bar keeps only actions that still make sense for viewing/distribution: "Save HTML Board", "Copy HTML Board", "Print Board", the Sources link, and the save-state indicator. "Update board changes to Main AI Chat", "Create and Save New Empty Board", and "Create Read-Only Copy" itself are hidden in the read-only copy. The header or bottom bar is not otherwise redesigned beyond what is needed for a clear read-only mode. (6) Save-state wording in the read-only copy is sensible. Editing is disabled through the UI, so the save-state indicator does not show editing-oriented "Not saved since changes" wording that could mislead; instead it shows a plain static " · Read-only copy" label. No new warning banner or modal warning is added as part of this fix — the change stays lightweight. (7) The Sources popup is view-only in the read-only copy. In the normal editable board, the Sources popup remains editable exactly as introduced in V1.0.12.8: each source is shown as a pair of inputs (title and URL) with a remove control; the popup has an "Add source" button, a "Save" button, and a "Close" button; caution wording is styled consistently with standard popup helper text. In the read-only copy, the "Add source" and "Save" buttons are hidden, the per-row remove (✕) control is hidden, and saveSources / addNewSourceRow / removeSourceRow short-circuit. The Close button, the URLs, and the caution wording remain. URLs remain clickable in display mode. This keeps the Sources experience mode-appropriate without breaking the editable Sources popup in the normal board. (8) Backward compatibility is preserved. Existing saved boards continue to load correctly. Boards saved before this build do not carry the isReadonly marker and therefore load as normal editable boards. Data shape is additive only: a single optional isReadonly flag is added to savedState, and only when the file is a Create Read-Only Copy output. No schema changes are made to the B / SP / FO / ttLinks / howLinks / measures / evalQuestions / pageInfo / docContent arrays or objects. (9) The version is now V1.0.12.9b everywhere relevant: the prompt header, the engine header comment, the visible board version brand line, and the print-view brand line. The print-view brand line, which was still reading V1.0.12.7 in V1.0.12.9a due to an earlier oversight, is now brought fully current to V1.0.12.9b as part of the version-consistency pass explicitly called for by this build. Unrelated parts of the engine and prompt are not touched by this build.

QQ. BUILD 1.0.12.9c CHANGES (V1.0.12.9c):

This build (1.0.12.9c) is a refinement pass on the 1.0.12.9b read-only-copy work. Its input is the completed 1.0.12.9b engine and 1.0.12.9b prompt; its output is V1.0.12.9c. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It implements only the three fixes below and otherwise leaves the engine and prompt unchanged. (1) The "Read-only copy" label in the read-only copy is moved and enlarged so it reads clearly as a mode/state label. Previously, the label sat on the left next to the board title and was not prominent enough. It is now placed on the right-hand side of the top bar (inside the right-hand header block, above the "SEE. PLAN. DO.™" tagline and the version brand lines) and rendered at a larger, more visible size — a modest translucent outline pill with uppercase spaced letters — so the read-only state is obvious at a glance without competing with the board title and without reading as an alarm banner. The rest of the top bar is not redesigned: only the label's position and size change, styling stays consistent with the rest of the app. (2) Page View controls are allowed to work in read-only mode. Previously, saveViewPanel() short-circuited on IS_READONLY, so the View panel Save button did nothing in the read-only copy. Changing View is a display/viewing control, not a content edit — it changes how the board is displayed to the viewer, not the board data. saveViewPanel() therefore now runs in read-only mode, users can open and use View controls on any page type where View exists (This–Then, How, and Final Outcomes), and their chosen view is applied to the current session. Because saveState() is still a no-op in read-only mode, view choices do not persist to localStorage past this session, which is the correct behaviour for a view toggle in a shared read-only copy. This does NOT re-enable editing — content-editing functions remain gated on IS_READONLY as before, and the hide rules on edit-oriented View-related buttons (content Save buttons, add/remove controls) are unchanged. The CSS that previously hid the View-panel Save button in read-only mode is removed so the View-panel Save button is visible and usable in the read-only copy. (3) Popup text/content fields are made clearly non-editable in read-only mode, so the UI honestly reflects what is and is not editable. Previously, popup windows could open in read-only mode (correctly), but users could still type into or delete text inside some popup text fields (Board info, Page info, Measure detail, Evaluation Question detail, Link detail, Sources), creating a false edit affordance because those changes did not persist anyway. A small shared helper, applyReadonlyToModal(modalEl), is now called from openModal() whenever a modal is opened in read-only mode. The helper sets the readOnly attribute on textareas and text inputs inside the modal, turns off contenteditable on contenteditable regions, and is compatible with dynamically-inserted modal content (Measure detail, EQ detail, Link detail, Sources list, Clone chooser, etc.) because those functions set their innerHTML before calling openModal. CSS gives these readonly fields a clearly non-editable appearance (dimmed background, default cursor, neutralised focus ring) so they visually read as non-editable. The save/add/delete/edit controls inside popups that would edit the board remain hidden or disabled as before. A small .ro-keep-editable escape-hatch class is honoured for viewing/filtering inputs (specifically the global search input), so genuine viewing inputs stay live in the read-only copy. Links inside popup/display content remain clickable in display mode, and popup navigation actions (clicking a linked box name, closing the modal, etc.) still work. In the normal editable board, popup fields remain fully editable exactly as before — applyReadonlyToModal is a no-op when IS_READONLY is false. Backward compatibility is preserved. Existing saved boards continue to load correctly; boards saved before this build are unaffected. No data-shape, schema, or link-logic changes are made. (4) The version is now V1.0.12.9c everywhere relevant: the prompt header, the engine header comment, the visible board version brand line, and the print-view brand line. Unrelated parts of the engine and prompt are not touched by this build.

RR. BUILD 1.0.12.10 CHANGES (V1.0.12.10):

This build (1.0.12.10) is a corrective build on top of V1.0.12.9c. Its input is the completed V1.0.12.9c engine and V1.0.12.9c prompt; its output is V1.0.12.10. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It implements only the one fix below and otherwise leaves the engine and prompt unchanged. (1) Non-box UI/chrome text now scales coherently with the board scale. Previously, the board area (.main) was scaled via zoom:var(--board-scale) so boxes and on-board chrome (page bars, under-box detail panes, etc.) scaled together, and this was working acceptably. However, the non-box UI/chrome sat outside .main at 1x — so when the whole board experience was zoomed out, the boxes and some board elements such as the page bar and details pane scaled acceptably, but other non-box UI/chrome text (top-page / header-area text, board-info and other helper/chrome text, popup-window text, and other interface text) became disproportionately small and harder to read relative to the scaled boxes. This was not about browser zoom — the fix is about the board/app's own scaling behaviour, and users are not instructed to use browser zoom. The fix makes the whole board experience scale more coherently so the boxes and non-box interface text feel part of the same scaling system. Each of the non-box chrome containers — the top orange header (.hdr), the page-tab bar (.tab-bar), the page-info bar (.page-info-bar), the bottom control bar (.ctrl-bar), the bottom attribution footer (.attr), the modal popup content box (.modal-box, including the measure/evaluation-question detail modal sizing), the page-type chooser box (.ptc-box), the custom tooltip (.dv-tip), the save-state toast (.save-toast), the chat floating-action button (.chat-fab), and the chat panel (.chat-panel) — now participates in the same scaling logic via zoom:var(--board-scale). Fixed-size popups/modals/tooltip/chat panel divide their max-width / max-height / width / height by --board-scale so their visible on-screen footprint stays consistent with pre-fix sizing while the text inside them scales coherently with the boxes. Popup text does not become comically large: it now lands at a size that is proportionate to box text rather than disproportionately tiny. Top-page text and other interface text remain readable and visually proportionate relative to the boxes. The on-board box-chooser popup is unchanged because it is appended inside #main and therefore already inherits .main's zoom — it already scales correctly with the boxes. CSS zoom is already in use for .main in the engine, so relying on zoom here is consistent with the existing approach, is coherent rather than scattered, and avoids sprinkling many unrelated magic-number font-size tweaks across the CSS. The fix is conservative and reversible: it touches only CSS in a contained block, does not redesign the UI, does not enlarge things arbitrarily in unrelated places, does not alter click/layout/scroll/modal-positioning/interaction behaviour, does not change any engine logic, state, schema, link, view settings, read-only, print, save, board-info, page-info, measures, evaluation-questions, clone, search, jump, or other behaviour, and preserves backward compatibility so existing saved boards continue to load correctly. (2) The version is now V1.0.12.10 everywhere relevant: the prompt header, the engine header comment, the visible board version brand line, and the print-view brand line. Unrelated parts of the engine and prompt are not touched by this build.

SS. BUILD 1.0.12.13 CHANGES (V1.0.12.13):

This build (1.0.12.13) is a corrective build on top of V1.0.12.12. Its input is the completed V1.0.12.12 engine and V1.0.12.12 prompt; its output is V1.0.12.13. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It implements only the four fixes below and otherwise leaves the engine and prompt unchanged. The deferred 1.0.13.0 items (border colour in the details pane, expanded traffic-light states, Links vs Associations terminology sweep, clickable URLs in popup display text) are NOT implemented in this build. (Those items are implemented in V1.0.13.1 — see TT and UU below.)

(1) Detail popup inner width — fixes the inner edit/detail popup so it shows its full internal content without horizontal scrolling, with right-side controls such as Add reachable without sideways scrolling. The fix targets only the inner edit/detail popup content, not the outer .modal-box width (which was already widened in V1.0.12.12). Inner panels (.ep) are forced to width:100% / max-width:none; textareas (.ep-ta) at width:100% (already so); control rows (.ep-add, .ep-struct, .meq-add) keep their existing flex-wrap:wrap and now also have max-width:100% so they wrap rather than overflow horizontally; .meq-section is constrained to max-width:100%. No outer modal width change, no popup-scale change, no board-scale change. No schema, state, link, read-only, or interaction changes. Popup text remains readable and the popup is not redesigned.

(2) Optional board-level Top right text — adds an optional short plain-text annotation/disclaimer shown right-justified in the page-info bar across all pages. Useful for labels such as "Draft", "Illustrative only", "Confidential", "Version for contractor review", "Not yet approved". The same Top right text appears on every page (board-level, not page-level). Plain text only — no rich text, no links. In normal editable boards, clicking the Top right text opens a small modal with a text input plus Save / Clear / Cancel buttons; if no Top right text exists, a modest "+ Add top right text" affordance appears in the same right-hand area instead. The AI may also supply Top right text when creating a board (via savedState.topRightText). In read-only copies, the Top right text is visible but not clickable/editable, and the "+ Add" affordance is hidden. Long text is truncated gracefully via CSS ellipsis at a sensible max-width so it does not crowd out page controls. The text is saved with the board (savedState.topRightText), persists through Save HTML Board, Copy HTML Board, Create Read-Only Copy, localStorage, and DOVIEW-STATE snapshots (TOP RIGHT TEXT section), and defaults to '' for older boards (backward compatible). The page-info bar is restructured into a flex row (.pi-bar-flex with .pi-bar-left and .pi-bar-right) so the existing page name, Page info, View, etc. stay on the left and the Top right text / + Add affordance sits on the right. This is not a large annotation system; it is intentionally a single short plain-text field at board level.

(3) Text-only Official DoView® Standards-Compliant Board Structure Badge — adds a non-editable text-only badge in the top-right of the orange header, on the second and third lines below the existing SEE. PLAN. DO.™ + version line. The first right-hand line keeps SEE. PLAN. DO.™ V1.0.12.13. Underneath, the badge has two lines of text: "Official DoView®" (slightly larger) and "Standards-Compliant Board Structure Badge" (slightly smaller). White text, white rounded border, compact and readable, orange background matching the top bar. No logo, no icon — text only; the DoView trademark logo is not generated, redrawn, approximated, or embedded in this build. The badge is non-editable, appears in normal editable boards and read-only copies, travels with saved/exported boards, has no user control to remove or change it, and prints if the header/branding area prints. Clicking the badge opens a non-editable info popup titled "Official DoView® Standards-Compliant Board Structure Badge" with a Close button only and no editable fields. The popup body explains that this DoView Board app's structure (not its content) has received the badge, that the DoView methodology is open and may be implemented elsewhere with acknowledgement, that the badge and DoView trademark may not be used or implied as endorsed/official without authorisation, and how to get in touch about badge assessment. The popup ends with two clickable links — visible text "doviewplanning.org/trademarkuse" linking to https://doviewplanning.org/trademarkuse, and visible text "doviewplanning.org/trademark" linking to https://doviewplanning.org/trademark. Boundary: this is the reserved official badge for this official DoView release; this build does not create a broader certification system, does not imply third-party boards/tools may use the badge unless authorised, and does not add logo/image handling.

(4) Bottom attribution link target — in the very bottom attribution bar, the visible "doviewplanning.org" link (next to "DoView® Planning — Dr Paul Duignan —") now targets https://doviewplanning.org/doviewboards. The visible link text "doviewplanning.org" is unchanged. The footer otherwise stays clean — no other footer wording changes.

(5) The version is now V1.0.12.13 everywhere relevant: the prompt header, the engine header comment, the visible board version brand line in the orange header, and the print-view brand line. Unrelated parts of the engine and prompt are not touched by this build. Backward compatibility is preserved: existing saved boards continue to load correctly (topRightText defaults to '' on older boards), read-only copies still work correctly, normal editable boards remain editable where they should, and unrelated behaviour is not changed by this build. The deferred items listed above are not implemented in this build.

TT. BUILD 1.0.13.0 CHANGES (V1.0.13.0):

This build (1.0.13.0) is a small corrective build on top of V1.0.12.13. Its input is the completed V1.0.12.13 engine and V1.0.12.13 prompt; its output is V1.0.13.0. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It implements only the four UI cleanup fixes below and otherwise leaves the engine and prompt unchanged. The deferred 1.0.13.1 items (border colour in the details pane, expanded traffic-light states, Links vs Associations terminology sweep, clickable URLs in popup display text) are NOT implemented in this build. (Those items are implemented in V1.0.13.1 — see UU below.)

(1) (Fix A) This–Then / How link popup overflow — the V1.0.12.13 popup-width fix targeted the .ep / detail-panel classes, but the remaining overflow problem was specifically the This–Then / How link popup, where the inner row containing the dropdown/select and the Add button could still push the Add button off to the right and require horizontal scrolling. The fix targets the actual link-popup rows that hold the picker + Add button: .tt-link-add and .how-link-add now have flex-wrap:wrap + max-width:100% (parallel to the V1.0.12.13 .ep-add / .ep-struct / .meq-add fix) so they wrap onto further lines at narrow widths rather than overflowing horizontally; .tt-link-section and .how-link-section are constrained to max-width:100% as their containers; and .tt-link-sel, .how-link-sel, and .meq-sel get min-width:0 + max-width:100% so the select itself can shrink below its widest option text rather than forcing the row wider than its container. The inner Link detail popup (linkDetailModal) inherits these rules so its picker rows also wrap and its Add button stays visible. The full link popup content is therefore visible without horizontal scrolling, and the Add button is reachable. No outer .modal-box width change here, no popup-scale change, no schema, state, link, read-only, or interaction changes. The popup is not redesigned; popup scale and board scale are unchanged by this fix.

(2) (Fix B) Training? link in the top utility/header — adds a modest "Training?" link after the existing "🔍 Search" link in the orange header. Clicking "Training?" opens https://doviewplanning.org/training in a new tab/window. Plain link only — no popup, no modal, no sales text inside the board. The tooltip/title reads "Want training? Get help using DoView Boards". The link is non-editable, appears in normal editable boards and in read-only copies, and is kept visually consistent with the other top utility links (same .hdr-binfo styling). No other promotional UI is added. The link is rendered as an HTML anchor (<a>) with class .hdr-binfo so it shares the look of the other top utility links; a small CSS rule keeps a.hdr-binfo, a.hdr-binfo:link, a.hdr-binfo:visited, and a.hdr-binfo:active at the same colour as the other top utility links so the browser-default :visited purple does not appear after the user has clicked it. DoView Boards remain free and open; this link is intentionally modest and offers a route to training without turning the board into an advertisement.

(3) (Fix C) Official DoView® badge popup trademark URL cleanup — the popup opened by clicking the Official DoView® Standards-Compliant Board Structure Badge previously ended with two clickable links: "doviewplanning.org/trademarkuse" (linking to https://doviewplanning.org/trademarkuse) and "doviewplanning.org/trademark" (linking to https://doviewplanning.org/trademark). The second URL ("doviewplanning.org/trademark") is removed; only "doviewplanning.org/trademarkuse" remains as the final clickable link, still linking to https://doviewplanning.org/trademarkuse. Badge wording, styling, placement, click behaviour, popup heading, and broader certification/badge behaviour are unchanged. The badge popup description in this prompt has been updated accordingly to keep the prompt consistent with the engine: it now describes the popup as ending with one clickable link rather than two.

(4) (Fix D) Laptop-friendly default board scale — the default --board-scale is reduced from 2 to 1.35 so a newly opened board fits comfortably on typical laptop viewports (around 1366×768 / 1440×900) without the user having to immediately zoom out. The existing zoom/scale mechanism is preserved; the only change is the default value of the --board-scale CSS custom property in :root. --popup-scale is unchanged at 1.75; popups remain readable and not oversized. Header, page tabs, page-info bar, and useful board content are visible on a normal laptop viewport without immediate zoom-out. Text remains readable. No layout redesign. No schema or state changes. The board-scale variable description elsewhere in this prompt has been updated to reflect the new default.

(5) The version is now V1.0.13.0 everywhere relevant: the prompt header, the engine header comment, the visible board version brand line in the orange header, and the print-view brand line. Backward compatibility is preserved: existing saved boards continue to load correctly, read-only copies still work correctly, normal editable boards remain editable where they should, and unrelated behaviour is not changed by this build. The deferred 1.0.13.1 items listed above are not implemented in this build. (They are implemented in V1.0.13.1 — see UU below.) Unrelated parts of the engine and prompt are not touched by this build.

UU. BUILD 1.0.13.1 CHANGES (V1.0.13.1):

This build (1.0.13.1) is a small corrective build on top of V1.0.13.0. Its input is the completed V1.0.13.0 engine and V1.0.13.0 prompt; its output is V1.0.13.1. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It implements only the four targeted UI/terminology fixes below — the items deferred from V1.0.13.0 — and otherwise leaves the engine and prompt unchanged. The later security-hardening build, the later board-chat redesign / structured-action reliability work, the later read-only annotation mode, and other later items remain deferred to future builds and are NOT implemented in this build.

(1) (Fix 1) Border color control in the box details pane — a compact Border color preset row is added in the box details pane, sitting near the existing Status (traffic light) and Priority controls. It applies to This–Then boxes, How boxes, and Final Outcomes boxes. Presets only — no free-form HTML colour picker in this first version: Default, Black, Grey, Orange, Blue, Green, Red, Purple. Default clears the custom border colour and returns the box to its normal style. The custom border colour applies only in the normal/resting state; selected, highlighted, reveal, inspect, search, and ghost/context states still override the custom border colour where needed. Custom border is rendered at 2px when set; the default border is rendered at 1px in the page colour. The custom border colour persists after save/reload, travels with read-only copies, and remains visible in read-only copies but is not editable there (the picker is hidden in read-only mode by the existing read-only gating on the entry panel). The board chat continues to be able to set custom borders via the existing [ACTION:setBoxBorder:BOX_ID:COLOR] / [ACTION:clearBoxBorder:BOX_ID] / [ACTION:setSubpageBorders:SUBPAGE_ID:COLOR] / [ACTION:clearSubpageBorders:SUBPAGE_ID] commands. The borderColor field is already plumbed through buildInitialState, saveState, boxHTML, howBoxHTML, rebuildBoxKeys, rebuildFinalKeys, and the AI chat state, so this is purely a UI addition; no schema change is needed and existing saved boards without a custom border continue to load correctly.

(2) (Fix 2) Two intermediate traffic-light states — Green/yellow and Yellow/red are added as additive states. The full set of traffic-light options is now: Green, Green/yellow, Yellow, Yellow/red, Red, Grey/unset. The two mixed states display as vertically split circles: Green/yellow = left half green, right half yellow; Yellow/red = left half yellow, right half red. Internal values are 'greenYellow' and 'yellowRed'. Existing 'green', 'yellow', 'red', 'grey' values continue to load and save correctly. The two new states are applied consistently in the on-box traffic-light dot, the details-pane traffic-light picker, and the on-box left-click chooser; user-facing labels are "Green/yellow" and "Yellow/red". The new states persist after save/reload. In read-only copies, traffic lights remain visible (including the two new mixed states) but cannot be changed. The cascade/auto-light logic is unchanged for the existing four states; the two new mixed states are user-set only and are not produced by automatic cascade. Existing boards that pre-date this build and contain only green/yellow/red/grey continue to load correctly. The default for new boxes remains grey/unset unless the user explicitly asks for a non-neutral traffic light.

(3) (Fix 3) Use links for structural box-to-box relationships and associated/association for Measures and Evaluation Questions — a targeted user-facing wording sweep that distinguishes structural relationships from Measure/EQ relationships. Structural box-to-box relationships keep links / linked terminology: This–Then links, How / up-and-down links, and non-up-and-down structural box-to-box links. Measures and Evaluation Questions connected to boxes or to structural links now use associated / association terminology in user-facing wording: Link Measure → Associate Measure, Link Evaluation Question → Associate Evaluation Question, Linked Measures → Associated Measures, Linked Evaluation Questions → Associated Evaluation Questions, Unlink → Remove association (where it refers to a Measure/EQ), Link to box / Link to this box (where the subject is a Measure/EQ) → Associate with box, Link to link (where the subject is a Measure/EQ) → Associate with structural link. This applies across the details pane, Measure/EQ popups, Measure/EQ registries, link-detail Measures/EQ sections, headings, helper text, tooltips, and AI chat help wording. Internal code symbols (function names like linkMeasureToBox / unlinkMeasureFromBox, action commands like linkMeasure / unlinkMeasure / linkEQ / unlinkEQ, internal CSS classes such as .meq-item-rm) are intentionally left unchanged to avoid unnecessary churn — the wording change is user-facing only, and existing AI chat actions continue to work. The top Links menu/list now lists structural box-to-box links only and uses three badges: "This–Then" (orange) for This–Then links, "How" (blue) for How / up-and-down links (upward and downward), and "Non-up/down" (slate) for non-up-and-down structural links. The "Other links" wording is intentionally not used. Boards saved with How links that classify as non-up-and-down (lateral) continue to load correctly and now appear under the Non-up/down badge in the top Links list.

(4) (Fix 4) Clickable URLs in popup display text — when user-entered text is shown in display/view mode in popups, URLs are rendered as clickable links via the existing linkify helper. The popups covered are: the This–Then / How link detail popup, the Measure detail popup, the Evaluation Question detail popup, and the Board info / Page info popups. Implementation is small and display-focused: a small read-only display block is placed above each editable textarea, shown only when the saved field has content; the textarea below remains the edit surface, and URLs are not active while the user is editing text. The stored text is not altered by linkification — only its rendered display picks up linkify. Links open in a new tab/window using the existing target=_blank convention. The existing safe-scheme rules (https, http, mailto only; bare domains linked as https://; dangerous schemes such as javascript: / data: not linkified) continue to apply via the existing linkify helper. The broader security-hardening safeUrl/sanitiser work is NOT implemented in this build and remains deferred.

(5) The version is now V1.0.13.1 everywhere relevant: the prompt header (first line), the engine header comment, the visible board version brand line in the orange header ("SEE. PLAN. DO.™ V1.0.13.1"), and the print-view brand line. Backward compatibility is preserved: existing saved boards continue to load correctly (including boards saved before the new traffic-light states or before any custom border colour), read-only copies still work correctly (the new traffic-light states and any custom border colour remain visible but are not editable in read-only copies), normal editable boards remain editable where they should, and unrelated behaviour is not changed by this build. The deferred items listed in MASTER (security/privacy hardening, safeUrl helper, documentation HTML sanitising, API key persistence changes, board-chat structured-action reliability work, board-chat new-page population redesign, read-only annotation mode, broader permissions/access-control systems, further stable page-type indicators in page tabs, This–Then links between This–Then boxes including across This–Then pages with full UI for cross-page edges, How links upward to higher-level How boxes beyond what the current adjacent-level system supports, board-level Measures and Evaluation Questions UI beyond what already exists, Documentation Page clones of selected board elements beyond what already exists, page-title clones, stable numbering for How boxes / Measures / Evaluation Questions beyond the current schemes, clone references stored lightly rather than duplicating source text, richer distributed/share modes beyond simple read-only copies, annotation/comment modes on top of read-only copies, clearer public standards/specification wording for DoView Board structures) are NOT implemented in this build. Unrelated parts of the engine and prompt are not touched by this build.

VV. BUILD 1.0.13.2 CHANGES (V1.0.13.2):

This build (1.0.13.2) is a very small corrective build on top of V1.0.13.1. Its input is the completed V1.0.13.1 engine and V1.0.13.1 prompt; its output is V1.0.13.2. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It implements only the two targeted fixes below (a board-chat populated-page reliability fix and a small popup-scale adjustment) and otherwise leaves the engine and prompt unchanged. The later security-hardening build, the later full board-chat structured-action redesign, the later read-only annotation mode, and other later items remain deferred to future builds and are NOT implemented in this build. In particular, this build does NOT implement: security/privacy hardening, the safeUrl helper, documentation HTML sanitising, API key persistence changes, the startup active-content warning, the full board-chat structured-action redesign, broad board-chat architecture changes, new permissions/access-control systems, or read-only annotation mode.

(1) (Fix 1) Board-chat populated-page reliability — when the user asks the board chat to add a new This–Then page and populate it with DoView This–Then boxes, the board chat must now create a meaningful populated page rather than a one-box placeholder. The previous behaviour was erratic: the board chat would correctly show an OK / Cancel confirmation, but the change actually applied could be only an addSubpage with the engine's default single-column / minimal-box shell. This is not acceptable when the user asked for a populated DoView This–Then page. The fix has three small, targeted parts and is deliberately NOT a full board-chat redesign.

  (a) The board-chat system prompt (buildBoardStatePrompt) now distinguishes blank/starter page requests from populated This–Then page requests, and the structural section explicitly requires that a populated This–Then page be emitted in ONE response with: one [ACTION:addSubpage:LABEL:this_then]; AT LEAST 3 columns total (the engine creates the new page with a default first column called "First Stage", which can be renamed via [ACTION:renameColumn:NEW_PAGE_ID:0:HEADING], and at least 2 more columns are added via [ACTION:addColumn:NEW_PAGE_ID:COLUMN_INDEX:HEADING]); AT LEAST 6 boxes total spread across those columns via [ACTION:addBox:NEW_PAGE_ID:COLUMN_INDEX:LABEL] tags; compact DoView outcome phrasing for box labels (no auxiliary-verb forms); column headings that name causal stages (not generic "Stage 1, Stage 2"); and a plausible left-to-right This→Then causal flow with later/higher-level outcomes on the right. The prompt explains how to predict the new page id (p<SP.length+1>) from the SP entries already present in the board state, and gives a worked example so the AI emits the full set of addSubpage + renameColumn + addColumn + addBox actions together rather than emitting addSubpage alone. Trigger phrasings for populated-page requests include: "add a page about X", "add a new This–Then page about X", "populate it", "add DoView This–Then boxes", "create a This–Then logic for X", and "add a page and fill it in". Trigger phrasings for blank/starter page requests include: "blank", "starter", "empty", and "page shell".

  (b) processAIResponse now detects populated-page intent in the user message via small regex helpers (isPopulatedThisThenPageRequest and isBlankPageRequest) and validates the AI's structural actions before queuing them. If the user clearly asked for a populated This–Then page (and did not explicitly ask for a blank/starter page), the engine predicts the new page id(s) for any addSubpage:this_then actions, counts the addColumn / removeColumn / addBox actions targeting each predicted id, and rejects the change when any predicted new this_then page would have fewer than 3 columns total (1 default + addColumns − removeColumns) or fewer than 6 boxes. Rejection happens BEFORE the snapshot, BEFORE any state actions execute, and BEFORE the OK / Cancel confirmation appears, so a one-box placeholder is never silently applied. A system message in the chat lists the under-populated page(s) and asks the AI/user to retry with full population, with the option to ask for a "blank/starter page" if a shell really was wanted. Other action shapes (How pages, Documentation pages, removals, renames, link/measure/EQ actions, etc.) are unaffected; the validation is scoped to populated this_then page requests only.

  (c) showConfirmation now renders a clean structured summary that groups addSubpage + renameColumn + addColumn + addBox actions for each new page (by predicted new page id) and shows the page title, the column headings, and the actual box count for each new page, so the user sees a meaningful description of what will be added before clicking OK. The summary line for a populated page reads, for example: "Add new This–Then page \"Staff retention\" — 3 columns: Causes addressed → Conditions established → Outcomes secured — 6 boxes". Other structural actions render as one bullet line each. After OK, the engine applies the FULL set of pendingStructural actions (addSubpage + addColumns + addBoxes) in order via the existing confirmStructural / execAction code path — the proposed columns and boxes are carried through to the apply step exactly as queued. pendingStructural and the existing confirm/apply flow are otherwise unchanged.

  No schema change is made by Fix 1. Existing saved boards continue to load correctly. Existing read-only copies continue to work correctly. Normal manual page creation outside the board chat is not affected. How page logic is not affected. Other board-chat actions behave as before.

(2) (Fix 2) Popup scale set to 1.25 — after V1.0.13.0 (Fix D) reduced the default --board-scale from 2 to 1.35 so a newly opened board fits comfortably on a normal laptop screen, popup windows scaled by --popup-scale=1.75 became a bit too large for the smaller board context. The default --popup-scale is now 1.25 so popups read at a more proportionate size relative to the board. --board-scale is unchanged at 1.35. Popup layout, modal width rules, popup-content widths, popup-content typography, and the overall scaling system are unchanged. Popup text remains readable. Existing popup content still fits and works. No schema, state, link, read-only, or interaction changes.

(3) The version is now V1.0.13.2 everywhere relevant: the prompt header (first line), the engine header comment, the visible board version brand line in the orange header ("SEE. PLAN. DO.™ V1.0.13.2"), and the print-view brand line. Backward compatibility is preserved: existing saved boards continue to load correctly, read-only copies still work correctly, normal editable boards remain editable where they should, and unrelated behaviour is not changed by this build. The deferred items listed in MASTER (security/privacy hardening, safeUrl helper, documentation HTML sanitising, API key persistence changes, startup active-content warning, full board-chat structured-action redesign, broad board-chat architecture changes, read-only annotation mode, broader permissions/access-control systems, further stable page-type indicators in page tabs, This–Then links between This–Then boxes including across This–Then pages with full UI for cross-page edges, How links upward to higher-level How boxes beyond what the current adjacent-level system supports, board-level Measures and Evaluation Questions UI beyond what already exists, Documentation Page clones of selected board elements beyond what already exists, page-title clones, stable numbering for How boxes / Measures / Evaluation Questions beyond the current schemes, clone references stored lightly rather than duplicating source text, richer distributed/share modes beyond simple read-only copies, annotation/comment modes on top of read-only copies, clearer public standards/specification wording for DoView Board structures) are NOT implemented in this build. Unrelated parts of the engine and prompt are not touched by this build.

WW. BUILD 1.0.14.0 CHANGES (V1.0.14.0):

This build (1.0.14.0) is the main board-chat structured-action reliability build on top of V1.0.13.2. Its input is the completed V1.0.13.2 engine and V1.0.13.2 prompt; its output is V1.0.14.0. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It is NOT a security-hardening build, NOT the review-only fragility audit, and NOT a broad UI redesign. It implements seven targeted fixes that make the internal board chat more reliable when deciding whether to answer a question, ask a clarification question, or propose board edits, especially when creating new pages. The later security-hardening build, the later read-only annotation mode, and other later items remain deferred to future builds and are NOT implemented in this build. In particular, this build does NOT implement: security/privacy hardening, the safeUrl helper, documentation HTML sanitising, API key persistence changes, the startup active-content warning, broad board-chat architecture changes beyond this targeted reliability work, new permissions/access-control systems, or read-only annotation mode.

(1) (Fix 1) Board-chat intent classification — buildBoardStatePrompt now instructs the AI to classify the user's request silently before generating any actions, into one of nine intents: (1) question / explanation only, (2) state-only edit, (3) structural edit on existing pages/boxes/columns, (4) blank This–Then page creation, (5) populated This–Then page creation, (6) How page creation, (7) Documentation page creation, (8) ambiguous page creation requiring clarification, and (9) invalid or incomplete action proposal. The classification is invisible to the UI — there is no new UI control or label — but it drives the chat's behaviour reliably. Pure questions and explanations are answered in plain language and do NOT propose structural edits. State edits behave as before. Structural edits on existing structure behave as before. The four page-creation intents (blank This–Then, populated This–Then, How, Documentation) and the two new control flows (ambiguous → ask page type; invalid/incomplete → ask for clarification) are described in Fixes 2–6 below.

(2) (Fix 2) Ask page type when page creation is ambiguous — when the user asks to add/create/make a page but does NOT specify the page type AND does NOT clearly imply a populated This–Then page (via the populated triggers in Fix 3 below), the board chat must reply with this exact wording (or preserve it very closely):

  What type of page do you want?
  - This–Then page — shows what you want to happen in the world, and what leads to what.
  - How page — shows what is going to be done.
  - Documentation page — holds supporting notes, explanation and basic rich-text documentation about the DoView Board.

After asking, the board chat stops and waits for the user's answer. It does NOT create the page and ask the question at the same time. It does NOT default ambiguous page requests to This–Then. It does NOT default ambiguous page requests to Documentation. The clarification fires for ambiguous requests such as "add a page called leadership development", "create a new page about leadership development", "make a page for leadership development", "add leadership development as a page", and "add a starter page called leadership development" (page type missing — still ask). The clarification is bypassed when the user has clearly specified the page type ("add a This–Then page", "add a How page", "add a Documentation page", "add a blank This–Then page"), when the user uses a populated trigger (Fix 3), or when the user uses any of the legacy populated triggers ("populate it", "add DoView This–Then boxes", "create a This–Then logic for X", "add a page and fill it in"). New helper functions in the engine (isPageCreationRequest, isExplicitPageType, isAmbiguousPageCreationRequest) classify the request consistently with the prompt; processAIResponse now rejects any addSubpage emitted for an ambiguous request before queuing pendingStructural — i.e. before snapshot, before any state-action execution, and before showing OK/Cancel — so a page is never silently created with the wrong type. This is implemented within the existing board-chat interaction style; no new wizard or multi-step UI is added.

(3) (Fix 3) "Full DoView" and related phrases trigger populated This–Then page behaviour — isPopulatedThisThenPageRequest now also triggers on "full DoView", "complete DoView", "full logic", and "full populated page". This means phrasings such as "add a new page called leadership development which includes a full DoView on it", "add a complete DoView page about leadership development", "create a full logic for leadership development", "add a full populated page about leadership development", and "make a page on leadership development with the full DoView logic" reliably propose a populated This–Then page with columns and boxes — not a one-box placeholder, not a blank shell, and not a page-type clarification question. The previous broad "add a page about X" matcher (without an explicit this-then keyword and without a populated trigger) is REMOVED, so a bare "add a page about X" is now classified as ambiguous and asks the page-type clarification question (Fix 2). Explicit type wins: "make a Documentation page with a full DoView" is treated as a Documentation page request even though the message contains "full DoView". This fix is made both in the board-chat system prompt (so the AI emits the right action set) and in the engine-side detection helper (so validation is consistent with the user's intent).

(4) (Fix 4) Domain-shaped page structure reminder for board chat — buildBoardStatePrompt now explicitly tells the AI that when creating a populated This–Then page, the number of columns and the number of boxes in each column MUST reflect the actual causal structure of the domain being modelled. Use as many causal stages as are genuinely needed; vary the number of boxes per column according to how many distinct outcomes, conditions, or enabling steps exist at that stage; column headings must name real causal stages — not generic labels such as "Stage 1", "Stage 2", "Inputs", "Activities", "Outputs" — unless those words are genuinely the best domain-specific headings. The "at least 3 columns and at least 6 boxes" rule remains as a FLOOR, not a target or a template. Mechanical "3 columns x 2 boxes" or "3 columns x 3 boxes" templates are explicitly discouraged. This reminder applies when the board chat creates a whole populated This–Then page; it does not affect single-box adds or small local edits. The compact DoView outcome phrasing rule and the existing populated-page minimum are preserved.

(5) (Fix 5) Clearer OK / Cancel preview for structural actions — _buildStructuralChangeSummaryHTML now shows a small POPULATED or BLANK/STARTER status pill for This–Then page proposals, in addition to the existing page title, page type, column count, column headings, and box count. The user can therefore see at a glance whether a full populated page is being proposed or only a shell. The OK / Cancel pattern is unchanged; no large modal redesign. Other structural actions (existing-page renames, removals, single-box adds, How-page actions, link/measure/EQ actions, etc.) still render as one bullet line each and are unchanged.

(6) (Fix 6) Validation before applying structural changes — _findInsufficientThisThenPageProposals now also flags populated This–Then page proposals whose column headings are mechanical numbered placeholders ("Stage 1", "Stage 2", "Column 1", "Column 2", "Step 1", "Step 2", "Phase 1", "Phase 2") and rejects them with a clear message asking for domain-specific column headings. The existing populated-page validation (≥ 3 columns, ≥ 6 boxes) remains. Validation runs in processAIResponse BEFORE snapshot, BEFORE any state-action execution, and BEFORE showing OK / Cancel — so a populated-page request never partially applies. For populated This–Then page requests, the engine continues to reject one-box placeholders. For blank/starter requests, the engine continues to allow a blank shell when blank/starter/empty/page-shell intent is explicit. For How and Documentation page requests, the engine preserves the current behaviour (How pages are seeded with 8 starter How boxes; Documentation pages are created as blank by default), and the engine does NOT fake a Documentation page by creating an unrelated This–Then page. This build prepares the board-chat wording and clarification logic for the three-page-type direction described in MASTER, but it does not invent any fragile partial implementation of features that the engine does not already support safely. Backward compatibility is preserved: existing saved boards continue to load correctly; the saved-state schema is unchanged.

(7) (Fix 7) Existing board-chat behaviour preserved — this build is targeted at the page-creation reliability path. It does not break, change, or remove: state-only edits (setLight, setPriority, addSofar, addNote, addNote1, addNote2, addNote3, setDetail, clearDetail, setBoxBorder, clearBoxBorder, setSubpageBorders, clearSubpageBorders, setBoardInfo, setPageInfo), traffic-light cascade, status changes, priority changes, custom border colour behaviour from V1.0.13.1 (Fix 1), box text edits, page renaming, column renaming, single-box adds via the chat, the existing structural confirmation flow, read-only gating, saved/exported board behaviour, the Create Read-Only Copy flow, board search, Measures and Evaluation Questions (including the V1.0.13.1 (Fix 3) associated/association wording), This–Then and How links, popup linkification from V1.0.13.1 (Fix 4), the popup-scale value of 1.25 from V1.0.13.2 (Fix 2), or the board-scale value of 1.35 from V1.0.13.0 (Fix D). Internal function names, action commands, and CSS classes are intentionally left unchanged unless strictly required by this build. The chat UI itself is not redesigned beyond the small POPULATED / BLANK/STARTER pill in the OK / Cancel preview.

(8) The version is now V1.0.14.0 everywhere relevant: the prompt header (first line), the engine header comment, the visible board version brand line in the orange header ("SEE. PLAN. DO.™ V1.0.14.0"), and the print-view brand line. Backward compatibility is preserved: existing saved boards continue to load correctly, read-only copies still work correctly, normal editable boards remain editable where they should, and unrelated behaviour is not changed by this build. The deferred items listed in MASTER (security/privacy hardening, safeUrl helper, documentation HTML sanitising, API key persistence changes, startup active-content warning, broad board-chat architecture changes beyond this targeted reliability work, read-only annotation mode, broader permissions/access-control systems, further stable page-type indicators in page tabs, This–Then links between This–Then boxes including across This–Then pages with full UI for cross-page edges, How links upward to higher-level How boxes beyond what the current adjacent-level system supports, board-level Measures and Evaluation Questions UI beyond what already exists, Documentation Page clones of selected board elements beyond what already exists, page-title clones, stable numbering for How boxes / Measures / Evaluation Questions beyond the current schemes, clone references stored lightly rather than duplicating source text, richer distributed/share modes beyond simple read-only copies, annotation/comment modes on top of read-only copies, clearer public standards/specification wording for DoView Board structures) are NOT implemented in this build. Unrelated parts of the engine and prompt are not touched by this build.

XX. BUILD 1.0.14.1a CHANGES (V1.0.14.1a):

This build (1.0.14.1a) is a small corrective UI / read-only clarity build on top of V1.0.14.0. Its input is the completed V1.0.14.0 engine and V1.0.14.0 prompt; its output is V1.0.14.1a. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It is intentionally narrower than the previous larger fragility build attempted as V1.0.14.1, and implements only the five small, self-contained user-visible clarity fixes below. It is NOT the larger board-chat validation/transaction build, NOT a unique-IDs build, and NOT a security-hardening build — those remain deferred to later builds (likely V1.0.14.1b for the board-chat validation/transaction work, a later build for unique IDs, and a later build for security hardening). The wider deferred items listed in MASTER also remain deferred and are NOT implemented in this build.

(1) (Fix 1) Read-Only Copy label and tooltip — when a board is opened as a read-only copy, a modest "Read-Only Copy" label is shown left-justified directly under the board title (moved from the right-hand side of the header). The label is small, low-contrast, single line, and not styled as a pill or warning banner — it reads as a quiet mode indicator rather than a security guarantee. The label carries the exact tooltip "Editing is disabled through the board interface. This is not security protection; someone with technical skills could still edit the HTML." This is an honest tooltip: read-only copies disable editing through the UI, but they are not tamper-proof — broader security hardening remains deferred to the later security build. The label only appears in read-only copies (gated by body.is-readonly); normal editable boards do not show it. The CSS rule for .ro-label is updated to render under the title in .hdr-left rather than as a translucent uppercase pill in .hdr-right; a new small wrapper .hdr-title-wrap is added inside .hdr-left to hold the title plus the label without disturbing the existing top-utility-link layout. Existing read-only gating behaviour is unchanged; saved/exported behaviour is unchanged; isReadonly:true continues to drive read-only mode. Note: the previous V1.0.14.1 attempt incorrectly indicated this label change was deferred. It is NOT deferred — V1.0.14.1a implements it.

(2) (Fix 2) Documentation Page wording updated — wherever the user sees a Documentation Page description as part of choosing a page type, the wording now reads exactly:

  Documentation page — holds supporting notes, explanation and basic rich-text documentation about the DoView Board.

This replaces the older wording:

  Documentation page — holds supporting documentation, notes, or rich-text material about the DoView Board.

This is a wording correction only. No advanced clone features are promised here, no Documentation Page features are broadened, and the engine continues to use the existing safe behaviour for Documentation pages (Documentation pages are created as blank by default; existing rich-text editing, saved-state shape, clone blocks, and rendering behaviour are all unchanged). The wording change appears in the board-chat clarification question (buildBoardStatePrompt) and any other place in the engine or this prompt where the user sees a list of page-type descriptions for choosing. The board chat continues to use the current safe behaviour for Documentation pages and does not pretend that advanced or distributed Documentation Page features are implemented unless they actually are.

(3) (Fix 3) Sources URL clickability — the Sources popup gains an "Open" link beside each non-empty URL in editable boards so the saved URL can be opened in a new tab without entering edit mode, while the title and URL inputs remain editable, and Add source / Save / Remove / Close behaviour is unchanged. In read-only copies, source rows render as view-only — the source title is shown as plain text, the URL is shown as a clickable "Open" link (or, if no title is set, the URL itself is shown as the link), and editable inputs, Add source, Save, and per-row remove are hidden; the Close button remains so the popup can always be dismissed. The sources data shape is unchanged; existing saved boards continue to load correctly. The links continue to use the existing target=_blank convention. Broader URL/safeUrl/sanitiser hardening is NOT implemented in this build and remains deferred to the later security build.

(4) (Fix 4) Top right text affordance — when topRightText is empty, the engine no longer shows the modest "+ Add top right text" affordance in the right-hand page-info bar; this affordance is hidden in both editable boards and read-only copies. This stops finished boards looking unfinished when the board creator did not ask for a top-right annotation. When topRightText is non-empty, the existing right-justified display behaviour is preserved (clickable in editable boards to open the existing Top right text editor, plain non-clickable in read-only copies, long text truncated via CSS ellipsis). To preserve a low-key way to add or edit Top right text from a sensible existing place, the Board info edit mode now includes a compact single-line "Top right text (optional)" field that edits the same topRightText saved-state value used by the existing Top right text editor modal. The topRightText saved-state field is unchanged; existing saved Top right text remains visible and editable; the data shape is unchanged. No new main-toolbar item is added for this. Top right text remains a board-level optional short plain-text annotation, not a large annotation system.

(5) (Fix 5) Board info / Page info view-edit pattern — Board info and Page info popups now use a simple view/edit pattern. They open in rendered view mode by default in editable boards. The saved text is rendered once with clickable URLs (via the existing linkify display block), an Edit button is shown alongside Close, and the editable textarea is hidden until the user clicks Edit. Clicking Edit replaces the rendered view with the textarea and shows Save / Cancel controls. Save commits the new text, returns to rendered view, and re-renders the page-info bar so any Top right text changes (Board info only) take effect immediately; the modal stays open so the user can immediately see their saved text rendered with clickable URLs. Cancel discards unsaved changes and returns to rendered view, preserving the previous saved text. Empty fields show a modest empty-state line ("No board info yet. Click Edit to add some." or the Page info equivalent) rather than a duplicate textarea-plus-rendered-block. In read-only copies, only the rendered view is shown — the Edit, Save, and Cancel buttons are hidden via the existing body.is-readonly button[onclick=...] CSS rule pattern, the textarea is not shown, and URLs in the rendered view remain clickable. Board info edit mode also includes the compact "Top right text (optional)" field from Fix 4 (Board info only — Page info does not show this field). This replaces the previous behaviour where the popup showed both the rendered display block and the editable textarea side by side, which was confusing because the same saved text appeared twice. The saved data shape is unchanged; existing Board info / Page info content continues to load correctly; this is not a Documentation Page-style rich-text editor; broader sanitiser/security hardening remains deferred. The board chat AI continues to read and write Board info and Page info via the existing [ACTION:setBoardInfo:TEXT] and [ACTION:setPageInfo:PAGE_ID:TEXT] commands.

(6) The version is now V1.0.14.1a everywhere relevant: the prompt header (first line), the engine header comment (first-line version stamp and a new V1.0.14.1a comment block summarising this build), the visible board version brand line in the orange header ("See. Plan. Do.™ V1.0.14.1a"), the print-view brand line, and any embedded board prompt text or branding/version string that previously showed an earlier version. Backward compatibility is preserved: existing saved boards continue to load correctly (the saved-state schema is unchanged — no new fields, no removed fields, no shape changes), read-only copies still work correctly (existing read-only gating, the Create Read-Only Copy flow, and saveState/localStorage/Copy HTML Board/Save HTML Board behaviour are all unchanged), normal editable boards remain editable where they should, and unrelated behaviour is not changed by this build.

(7) Items explicitly NOT implemented in this build, deferred to later builds:
  - Board-chat validation/transaction work (deferred to a later build, likely V1.0.14.1b): destructive-action plain-language previews, full preflight validation for all structural actions, mixed state + structural actions as one transaction, malformed action handling, unsupported action handling, broad target validation for every action, improved rejection messages for all board-chat failure paths, stronger board-chat transaction engine, broad board-chat parser changes. The existing V1.0.14.0 board-chat behaviour is preserved unchanged in this build, with only the small Documentation Page wording update from Fix 2 above.
  - Unique IDs (deferred to a later build): board object UUIDs, stable IDs for every page / column / box / final outcome / link / Measure / Evaluation Question, provenance fields such as sourceId or sourceBoardId, copy/import ID regeneration, ID migration for old boards, board-chat ID targeting.
  - Security hardening (deferred to the later security build): broad safeUrl hardening, broad sanitiser replacement, active-content startup warnings, API key storage redesign, permission redesign, public share security redesign, clone security work, Documentation Page rich-text sanitisation, large URL sanitisation changes, CSP or sandbox redesign. The Read-Only Copy tooltip in this build deliberately clarifies that read-only copies are NOT security protection precisely because broader security work remains deferred.
  - Other later items already listed in MASTER and not implemented in this build: further stable page-type indicators in page tabs beyond the existing dot/underline indicators, This–Then links between This–Then boxes including across This–Then pages with full UI for cross-page edges, How links upward to higher-level How boxes beyond what the current adjacent-level system supports, board-level Measures and Evaluation Questions UI beyond what already exists, Documentation Page clones of selected board elements beyond what already exists, page-title clones, stable numbering for How boxes / Measures / Evaluation Questions beyond the current schemes, clone references stored lightly rather than duplicating source text, richer distributed/share modes beyond simple read-only copies, annotation/comment modes on top of read-only copies, and clearer public standards/specification wording for DoView Board structures.

Unrelated parts of the engine and prompt are not touched by this build. This is the smallest complete set of changes needed to implement Fixes 1–5 above.

XX. BUILD 1.0.14.1b CHANGES (V1.0.14.1b):

This build (1.0.14.1b) is the corrective fragility build that was deferred from the earlier V1.0.14.1a small-corrective build. Its input is the completed V1.0.14.1a engine and V1.0.14.1a prompt; its output is V1.0.14.1b. It is a corrective build from the current state, not a fresh redesign, and preserves all existing working behaviour unless this build explicitly changes it. It implements thirteen targeted fixes — three small UI tweaks plus ten board-chat validation / transaction / safety fixes — and otherwise leaves the engine and prompt unchanged. It is NOT a unique-IDs build, NOT a security-hardening build, and NOT a broad architecture change — those remain deferred to later builds (the next planned build, likely V1.0.14.2, is the stable unique-ID build; security hardening comes after that). The wider deferred items listed in MASTER also remain deferred and are NOT implemented in this build.

(1) (Fix 1) Default Sources entry — when a board has no explicit sources state (a fresh board, or an older board with no saved sources), the engine seeds a single default source { title: "Information on DoView Boards", url: "https://doviewplanning.org/doviewboards" } so the Sources popup is never empty/unfinished-looking. The seed runs once on init when sources is empty AND a new sourcesInitialized flag is not yet set in the loaded saved state. After seeding, sourcesInitialized is set to true and persisted in saveState, in localStorage, and in every copy/save/read-only-copy/new-empty-board export, so the user's later edits or removals of the default source are respected — the default is NOT re-added on reload. The seed never runs in read-only copies (a read-only viewer should see exactly what was saved). The seed defensively skips if a source with the default URL already exists. The sources data shape (an array of {title, url}) is unchanged; sourcesInitialized is a new additive optional saved-state flag that defaults to false for older boards (backward compatible). Existing saved boards continue to load correctly.

(2) (Fix 2) Sources URL input no longer misleads with grey "https://" — the URL input previously used placeholder="https://...", which looked like an automatic prefix that would be applied to whatever the user typed (it was not). The placeholder is now "Enter full URL, e.g. https://example.org" and a small helper line ("Enter the full URL, including https://") sits inside the Sources popup so the user understands they should type the complete URL. URL editability and the "Open" affordance from V1.0.14.1a (Fix 3) are unchanged in editable boards, and read-only Sources rows remain view-only. The engine does NOT silently prepend https:// or rewrite user-entered URLs; broader URL/safeUrl/sanitiser hardening remains deferred to the later security build.

(3) (Fix 3) Read-Only label is now a modest pill inline with the top utility links — replaces the V1.0.14.1a quiet text label under the title. The label text is "Read-Only Board" (replacing "Read-Only Copy" in the on-screen label); it sits inline with the top utility links such as "Training?" in the orange header; it is styled as a modest pill (subtle white outline + transparent background, small horizontal padding, slightly larger font than the previous tiny label so it stands out modestly without dominating the header), not a warning banner; it does not look like a clickable utility link; it carries the exact tooltip "Editing is disabled through the board interface. This is not security protection; someone with technical skills could still edit the HTML."; and it appears only in read-only copies (gated by body.is-readonly). The label markup moves out of .hdr-title-wrap and back into the row of top utility links inside .hdr-left, and the now-unused .hdr-title-wrap reverts to a simple title row. There is exactly one read-only label in the UI (no duplicate "Read-Only Copy" elsewhere). Existing read-only gating behaviour is unchanged; this is a UI-only mode indicator and not a security guarantee — broader security hardening remains deferred to the later security build. The Read-Only Copy popup label, browser tab title suffix, save-state indicator wording, bottom-bar cleanup, view-only Sources behaviour, and other read-only-copy details from V1.0.12.9b / V1.0.12.9c / V1.0.14.1a are otherwise unchanged.

(4) (Fix 4) Plain-language OK / Cancel previews for destructive and other actions — _buildStructuralChangeSummaryHTML now renders friendly, descriptive lines for individual structural actions (and for state actions when they appear inside a mixed transaction — see Fix 6). Examples of the new preview lines:

  Remove box "Funding secured" from page "Leadership Development".
  Remove column "Early Conditions" from page "Leadership Development".
  Remove page "Leadership Development".
  Remove final outcome "Community wellbeing improved".
  Delete Measure "Attendance rate" (and remove all its associations).
  Delete Evaluation Question "How well did the programme reach its target audience?" (and remove all its associations).
  Remove structural link from "Funding secured" to "Programme delivered".
  Remove How link from "Train staff" to "Run workshops".
  Remove association between Measure "Attendance rate" and box "Participation increased".
  Remove association between Evaluation Question "..." and box "Participation increased".
  Add new box "Funding secured" to column "Early Conditions" on page "Leadership Development".
  Rename column "First Stage" on page "Leadership Development" to "Early Conditions".

The existing addSubpage grouped line (with addColumn / renameColumn / addBox children grouped per predicted new page id, plus the V1.0.14.0 (Fix 5) POPULATED / BLANK/STARTER status pill) is preserved. If the engine cannot resolve the target object (for example a missing box id), the action is rejected by Fix 5 (preflight validation) before OK / Cancel is shown. The OK / Cancel pattern itself is unchanged. The confirmation banner heading text changes from "Structural change(s) proposed:" to "Change(s) proposed:" because mixed transactions can now include both kinds of action.

(5) (Fix 5) Preflight validation for all action targets and shapes — a new helper _validateActionSet runs in processAIResponse BEFORE snapshot, BEFORE any state-action execution, and BEFORE showing OK / Cancel. It performs a dry-run over a working "shadow" model that mirrors the parts of execAction's behaviour that affect target resolution (addSubpage adds a new page with the next predicted id and a default first column "First Stage" for this_then / documentation, no columns for how pages; addColumn / removeColumn / moveColumn updates the working column list; addBox / removeBox updates the working box list; addFinalOutcome / removeFinalOutcome updates the working FO list; createMeasure / createEQ / deleteMeasure / deleteEQ / linkMeasure / unlinkMeasure / linkEQ / unlinkEQ updates the working Measure / EQ id list and association set; addTTLink / addHowLink / removeTTLink / removeHowLink updates working link id maps). The validator does NOT execute real actions; it checks that every action references a valid object (existing in the current state OR created earlier in the same set), that page types in addSubpage are supported (this_then, how, documentation), that column indices are valid for the resolved page, that new labels/headings are non-empty, that addBox actions have valid page+column targets, that the action tag's argument count meets the minimum for that command, and that the action type itself is supported by the engine (matches a real dispatch arm in execAction). If any action fails validation, the WHOLE action set is rejected: nothing is applied, no snapshot is taken, no OK / Cancel is shown, and the user receives a concise plain-language rejection message. Examples of rejection messages:

  I could not apply that change because the target box could not be found. No changes were made.
  I could not apply that change because the action format was incomplete or malformed. No changes were made.
  Unsupported action: "moveMeasure". I could not apply that change. No changes were made.
  I could not add that box because the target column could not be found. No changes were made.

This validation is conservative — it does NOT replace the existing populated-page validation (_findInsufficientThisThenPageProposals), which still runs alongside it (see Fix 8). Stack traces, raw internal errors, and parser internals never reach the chat. Positional references (pX, pX-cY, pX-cY-bZ, final-bI, MNNN, EQNNN) continue to be the references used; this build does NOT introduce stable unique IDs.

(6) (Fix 6) Mixed state + structural changes apply as one transaction — when a chat response contains BOTH state changes (setLight, addNote, setPriority, etc.) and structural changes (addBox, removeColumn, addSubpage, etc.), the engine no longer applies state changes immediately and structural changes after OK. Instead, both kinds are queued and the OK / Cancel preview shows them together; on OK, all changes are applied as one transaction; on Cancel, none are applied. State-only responses still apply directly as before (preserving the existing safe direct-apply behaviour); structural-only responses still go through OK / Cancel as before. The implementation uses a small new pendingStateActions queue alongside the existing pendingStructural queue, and a snapshot taken before any apply, so Cancel rolls back to a clean state without partial application. The OK / Cancel preview rendered by _buildStructuralChangeSummaryHTML covers ALL queued actions (structural and state) so the user sees the full change set before confirming.

(7) (Fix 7) Extended phrase coverage for board chat without over-broadening — isPageCreationRequest now also detects "make/add another tab", "set up a section (for X)", "build out (a/this/the) page", and "turn this into a full page" as page-creation requests; isPopulatedThisThenPageRequest now also detects "do/make the full map (for X)", "map this out", "build the full logic", "build out (a/this/the) page", "fill out this page", and "turn this into a full page" as populated triggers. Ambiguous page creation (no explicit type, no populated trigger) continues to ask the page-type clarification question from V1.0.14.0 (Fix 2) (with the V1.0.14.1a (Fix 2) Documentation Page wording). Question-only requests still answer in plain language with no edits. Broad words like "map", "section", or "tab" only trigger when used in tight phrasings (e.g. "set up a section for X", "do the full map for X", "make another tab"); they do NOT trigger automatic edits in question-only contexts such as "what does this map say about X". The existing V1.0.14.0 triggers ("full DoView", "complete DoView", "full logic", "full populated page", and the legacy populated triggers) all continue to work unchanged.

(8) (Fix 8) Stronger populated This–Then page validation — _findInsufficientThisThenPageProposals continues to enforce the ≥3 columns / ≥6 boxes floor and continues to reject mechanical numbered headings ("Stage 1", "Stage 2", "Column 1", "Column 2", "Step 1", "Step 2", "Phase 1", "Phase 2"). It now also flags a populated This–Then page proposal whose column headings are dominated by the generic logic-model template Inputs / Activities / Outputs (specifically: 2 or more of those generic words used as headings together), so a mechanical "Inputs / Activities / Outputs" page is rejected unless the AI uses a domain-specific shape. Single occurrences of those words are allowed (a domain may genuinely have one stage called "Outputs"). The minimum is still treated as a floor, not a target — the V1.0.14.0 (Fix 4) domain-shaped page structure rule remains in force. The rejection message for this case names the generic Inputs / Activities / Outputs template explicitly so the AI knows to retry with domain-specific column headings.

(9) (Fix 9) / (Fix 10) Validation requires valid page and column targets for new boxes, and valid targets for ALL action shapes — addBox actions whose target page does not exist (current SP or an earlier addSubpage in the same set) are rejected; addBox actions whose target column does not exist (current sp.cols OR an earlier addColumn in the same set targeting that page, OR for a new this_then / documentation page the implicit "First Stage" default first column at index 0) are rejected; removeBox / renameBox / removeColumn / renameColumn / moveColumn / removeSubpage / removeFinalOutcome / deleteMeasure / deleteEQ / linkMeasure / unlinkMeasure / linkEQ / unlinkEQ / addTTLink / removeTTLink / flipTTLink / addHowLink / removeHowLink all require their target to resolve in current state or earlier in the same set. Positional references (pX, pX-cY, pX-cY-bZ, final-bI, MNNN, EQNNN, H001…) continue to be the references used; this build does NOT introduce stable unique IDs (those remain deferred to V1.0.14.2). When the engine cannot resolve a referenced target, the WHOLE action set is rejected with a plain-language message that names what could not be found.

(10) (Fix 11) Malformed action tags handled safely — if any [ACTION:...] tag is parsed but its argument count is below the minimum for that command (CMD_MIN_PARTS), or it has an unknown / garbled command name, the whole action set is rejected with a plain-language message rather than silently applying the rest. Internal parsing errors are not exposed; raw stack traces never reach the chat. The minimum argument counts are kept in lockstep with the &&parts.length>=N guards in execAction.

(11) (Fix 12) Unsupported action types rejected clearly — if an [ACTION:...] tag uses a command name the engine does not implement (for example "moveMeasure", or a command name that appears in legacy prompt wording but is not actually dispatched in execAction such as "addHowBox"), the whole action set is rejected with a clear "Unsupported action: ..." message. The engine does NOT add support for new action types in this build. The list of supported commands lives in a new SUPPORTED_CMDS constant which is the source of truth used by the validator and is kept in sync with the dispatch arms in execAction.

(12) (Fix 13) Improved rejection messages — every rejection path uses short, plain-language wording, always states explicitly that no changes were made, and never blames the user. The wording is consistent with the per-fix examples above. The engine never displays raw [ACTION:...] tag text in a rejection message; it describes what could not be done in user terms. The board chat continues to treat questions and explanations as text-only responses and emits no actions in those cases.

(13) The version is now V1.0.14.1b everywhere relevant: the prompt header (first line), the engine header comment (first-line version stamp and a new V1.0.14.1b comment block summarising this build, added after the existing V1.0.14.1a comment block), the visible board version brand line in the orange header ("See. Plan. Do.™ V1.0.14.1b"), the print-view brand line, and any embedded board prompt text or branding/version string that previously showed an earlier version. Backward compatibility is preserved: existing saved boards continue to load correctly (the saved-state schema is unchanged apart from the additive optional sourcesInitialized flag from Fix 1, which defaults to false for older boards), read-only copies still work correctly (existing read-only gating, the Create Read-Only Copy flow, and saveState/localStorage/Copy HTML Board/Save HTML Board behaviour are all unchanged), normal editable boards remain editable where they should, and unrelated behaviour is not changed by this build.

(14) Items explicitly NOT implemented in this build, deferred to later builds:
  - Unique IDs (deferred to a later build, likely V1.0.14.2): board object UUIDs, stable IDs for every page / column / box / final outcome / link / Measure / Evaluation Question, provenance fields such as sourceId or sourceBoardId, copy/import ID regeneration, ID migration for old boards, board-chat ID targeting beyond the existing positional references.
  - Security hardening (deferred to the later security build): broad safeUrl hardening, broad sanitiser replacement, active-content startup warnings, API key storage redesign, permission redesign, public share security redesign, clone security work, Documentation Page rich-text sanitisation, large URL sanitisation changes, CSP or sandbox redesign. The Read-Only Board pill in this build deliberately clarifies that read-only copies are NOT security protection precisely because broader security work remains deferred.
  - Other later items already listed in MASTER and not implemented in this build: further stable page-type indicators in page tabs beyond the existing dot/underline indicators, This–Then links between This–Then boxes including across This–Then pages with full UI for cross-page edges, How links upward to higher-level How boxes beyond what the current adjacent-level system supports, board-level Measures and Evaluation Questions UI beyond what already exists, Documentation Page clones of selected board elements beyond what already exists, page-title clones, stable numbering for How boxes / Measures / Evaluation Questions beyond the current schemes, clone references stored lightly rather than duplicating source text, richer distributed/share modes beyond simple read-only copies, annotation/comment modes on top of read-only copies, and clearer public standards/specification wording for DoView Board structures.

Unrelated parts of the engine and prompt are not touched by this build. This is the smallest complete set of changes needed to implement Fixes 1–13 above.

────────────────────────────────────────────────────────
END OF PROMPT
────────────────────────────────────────────────────────

Please redo the prompt with all of that in it

Rules:
- Return the complete revised prompt in one code block
- Do not abbreviate
- Do not omit any part
- Do not change anything else unless strictly required by this fix
- Preserve wording everywhere else as far as possible
```
